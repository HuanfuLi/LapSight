---
phase: 04-ghost-lap-live-delta
plan: 3
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DeltaDisplayStateTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
autonomous: true
requirements: [GHOST-02, GHOST-03]
must_haves:
  truths:
    - "D-10: Delta formatting preserves explicit '+' for slower and '-' for faster."
    - "D-11: Delta is suppressed when no current lap, no reference, poor GPS quality, or no confident match exists."
    - "D-13: Delta is the second core readout after Current Lap."
    - "D-14: Moving dashboard delta copy is value-only sign plus seconds, e.g. '-0.218s' or '+0.421s'."
    - "D-15: Faster/negative is green, slower/positive is orange/red, unavailable is neutral."
    - "D-16: No map ghost, telemetry chart, dense UI, or new navigation destination is added."
    - "D-17: No-reference and unavailable states render '--'."
    - "D-18: The UI never keeps stale delta when the engine reports unavailable."
    - "D-19: Current lap, last lap, best lap, speed, accuracy, and lap count keep working when delta is '--'."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt"
      provides: "TimingRunSnapshot / delta display state exposed to UI"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      provides: "Mounted-phone current lap + value-only live delta surface"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DeltaDisplayStateTest.kt"
      provides: "Formatting/color/unavailable-state tests"
  key_links:
    - from: "ui/DriveScreen.kt"
      to: "session/SessionController.kt"
      via: "read-only timing snapshot"
      pattern: "UI renders derived state"
---

<objective>
Expose live ghost delta on the mounted-phone Drive/Timing surface with minimal value-only UI, while preserving current lap as the primary readout and keeping unavailable states safe.
</objective>

## Phase Goal

**As a** track user, **I want to** compare my current lap against a saved best lap, **so that** I can see whether I am ahead or behind without waiting until the lap ends.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-ghost-lap-live-delta/04-CONTEXT.md
@.planning/phases/04-ghost-lap-live-delta/04-UI-SPEC.md
@.planning/phases/04-ghost-lap-live-delta/04-VALIDATION.md
@.planning/phases/04-ghost-lap-live-delta/04-PATTERNS.md
@.planning/phases/04-ghost-lap-live-delta/04-02-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapDashState.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapModels.kt
</context>

<interfaces>
Expose a production-safe snapshot for Drive UI, e.g.:

- `TimingRunSnapshot.currentLapMillis`
- `TimingRunSnapshot.lastLapMillis`
- `TimingRunSnapshot.bestLapMillis`
- `TimingRunSnapshot.lapCount`
- `TimingRunSnapshot.speedMetersPerSecond`
- `TimingRunSnapshot.accuracyMeters`
- `TimingRunSnapshot.deltaDisplay`
- `TimingRunSnapshot.source`

Do not make `DriveScreen` use `recorderForTest()` for production UI state after this plan.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 tests for delta display state</name>
  <read_first>
    @.planning/phases/04-ghost-lap-live-delta/04-UI-SPEC.md
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/LapDashStateTest.kt
  </read_first>
  <files>
    shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DeltaDisplayStateTest.kt
  </files>
  <behavior>
    - `-218ms` formats as `-0.218s` and faster/green.
    - `421ms` formats as `+0.421s` and slower/orange-red.
    - unavailable formats as `--` and neutral.
    - unavailable after available clears the previous value.
    - display state contains no words like `ahead`, `behind`, `faster`, `slower`, or `ghost` for active timing UI.
  </behavior>
  <action>Create failing display-state tests before UI implementation. Keep them pure/common; do not snapshot-test Compose rendering.</action>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*DeltaDisplayStateTest*"</automated>
  </verify>
  <acceptance_criteria>
    - Tests encode exact copy contract from 04-UI-SPEC.md.
    - Tests cover colors/states without requiring Compose screenshot infrastructure.
  </acceptance_criteria>
  <done>Delta display state tests exist and fail only because production formatting state is missing.</done>
</task>

<task type="auto">
  <name>Task 2: Expose timing run snapshot from session layer</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DeltaDisplayStateTest.kt
  </read_first>
  <files>
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
  </files>
  <action>Add a read-only production timing snapshot that includes current/last/best lap state, lap count, latest speed/accuracy, source metadata, and delta display state. Keep `recorderForTest()` test-only and move Drive UI toward the snapshot. Ensure the snapshot remains useful when delta is unavailable.</action>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*DeltaDisplayStateTest*" --tests "*TimingGhostIntegrationTest*"</automated>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*SessionControllerTest*"</automated>
  </verify>
  <acceptance_criteria>
    - UI can render current/last/best/laps/speed/accuracy without peeking into recorder internals.
    - Delta unavailable does not erase non-delta timing metrics.
    - Existing draft snapshot behavior remains compatible with Review/save flows.
  </acceptance_criteria>
  <done>Session layer exposes a complete timing/delta snapshot for Drive UI.</done>
</task>

<task type="auto">
  <name>Task 3: Render value-only live delta on Drive/Timing surface</name>
  <read_first>
    @.planning/phases/04-ghost-lap-live-delta/04-UI-SPEC.md
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapDashState.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DeltaDisplayStateTest.kt
  </read_first>
  <files>
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  </files>
  <action>Update `TimingRunSurface` so current lap time is primary, live delta is second and value-only, and last/best/laps/speed/accuracy remain compact. Use semantic colors from UI-SPEC. Preserve existing Stop/orientation controls and DEMO badge. Do not add maps, charts, ghost controls, or new tabs.</action>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*DeltaDisplayStateTest*"</automated>
    <automated>.\gradlew.bat :shared:testAndroidHostTest</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Active timing surface shows delta as `--`, `+0.421s`, or `-0.218s` only.
    - Delta appears less dominant than current lap time but more prominent than secondary metrics.
    - Color follows faster/slower/unavailable semantics.
    - Existing Drive actions, full-screen behavior, and Demo badge remain intact.
  </acceptance_criteria>
  <done>Mounted-phone timing UI renders live delta safely and minimally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session snapshot -> UI | Snapshot values could be missing or unavailable mid-lap. |
| Delta color/copy -> user interpretation | UI can mislead if it implies precision or direction incorrectly. |
| Moving user -> controls/text | Dense UI increases distraction risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Safety | Drive delta UI | mitigate | Value-only display, large typography, semantic color, no stale delta, no dense charts/maps. |
| T-04-10 | Integrity | sign formatting | mitigate | Tests assert explicit sign and color for faster/slower semantics. |
| T-04-11 | DoS/Robustness | missing snapshot fields | mitigate | Render `--` for unavailable delta and continue rendering other metrics. |
</threat_model>

<verification>
Run:

- `.\gradlew.bat :shared:testAndroidHostTest --tests "*DeltaDisplayStateTest*"`
- `.\gradlew.bat :shared:testAndroidHostTest`
- `.\gradlew.bat :androidApp:assembleDebug`
</verification>

<success_criteria>
- GHOST-03 is satisfied with a minimal live delta readout.
- D-13 through D-19 are implemented in UI/state.
- Current lap remains primary and delta is not text-heavy.
- No map ghost, telemetry chart, dense UI, new nav destination, real GPS provider, or glasses work is added.
</success_criteria>

<output>
Create `.planning/phases/04-ghost-lap-live-delta/04-03-SUMMARY.md` when done.
</output>
