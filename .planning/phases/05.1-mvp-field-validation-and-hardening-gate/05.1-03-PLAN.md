---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/ReadyGate.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/ReadyGateTest.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/RawRecordingController.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/RawRecordingControllerTest.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
autonomous: true
requirements: [GPS-01, GPS-02, GPS-03, GPS-04, SESS-01, LAP-01, LAP-04, LAP-05, SAFE-04]

must_haves:
  truths:
    - "A single pure aggregateReady function decides Ready / NotReady(reasons) over GPS fix, horizontal accuracy, sample freshness, sample rate/cadence, selected course/profile, confirmed start/finish, direction compatibility, and wrong-course preflight."
    - "Formal lap/ghost timing only starts when aggregateReady returns Ready; otherwise the user may start raw recording instead."
    - "Raw recording streams samples to a diagnostic export and constructs no LapEngine/TimingSessionRecorder/LiveDeltaEngine state; its output can never count as a valid timing session."
    - "A real run carries the live PhoneGps source in the saved session, not the Track's marking source."
    - "The live dash shows the Ready/not-Ready state and the raw-recording state."
    - "Decision coverage: D-13, D-14, D-15, D-16, D-17, D-18 (a session is valid only if the app showed Ready first; conservative aggregate Ready over all seven inputs; motion not a base Ready input; raw recording allowed when not Ready but never counts as a valid session; wrong-course preflight blocks Ready with override recorded as evidence) are realized by this plan."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/ReadyGate.kt"
      provides: "Pure aggregateReady decision + ReadyState sealed result + injectable thresholds"
      contains: "aggregateReady"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/RawRecordingController.kt"
      provides: "Diagnostic raw-recording seam that builds no lap/ghost state"
      contains: "RawRecording"
  key_links:
    - from: "session/SessionController.kt"
      to: "session/ReadyGate.kt"
      via: "startTiming gates on aggregateReady before formal timing"
      pattern: "aggregateReady"
    - from: "ui/AppShell.kt"
      to: "session/SessionController.kt"
      via: "SessionController constructed with sourceForTrack returning the live feed source"
      pattern: "sourceForTrack"
---

<objective>
Deliver the only feature-shaped work this gate permits: (1) a conservative pure `aggregateReady(...)` gate over all seven D-14 inputs plus wrong-course preflight (D-13/D-14/D-15); (2) a distinct raw-recording seam that records samples for diagnosis without entering lap/ghost timing and is excluded from valid-session counting (D-16/D-17); (3) the confirmed P1 evidence-integrity fix at `AppShell.kt:95` so real runs carry the live PhoneGps source rather than the Track's marking source (D-04/D-42). Surface Ready and raw-recording state on the dash (D-32).

Purpose: Without a single conservative Ready gate, a session can begin formal timing while GPS is poor, invalidating it as evidence (D-13). Without a raw-recording mode, there is no diagnostic capture path when not Ready. The source mislabel breaks simulated/real isolation and corrupts field evidence.
Output: `ReadyGate.kt` + `RawRecordingController.kt` + tests, gated `SessionController`, fixed `AppShell` wiring, and dash state on `DriveScreen`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-RESEARCH.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-PATTERNS.md
@AGENTS.md

<interfaces>
- session/WrongCoursePreflight.kt:17 — injectable `WrongCoursePreflightThresholds(blockDistanceMeters=250.0, maxHorizontalAccuracyMeters=100.0, maxFixAgeMillis=15_000)`; line 51 sealed `CoursePreflightResult { Ready, Blocked, Unavailable(reason) }`; pure, "bad input is data".
- GpsQualitySummary.kt:30 — `DEFAULT_DEGRADED_ACCURACY_METERS = 25.0`; line 49 `averageUpdateRateHz` (rate/cadence input).
- session/SessionController.kt:52-58 — default `sourceForTrack` returns the Track's marking source (the P1 bug); line 99-135 `startTiming(...)` chains preflight after a null-check; line 51 injectable `now`.
- ui/DriveMarkingController.kt:254 — capture-without-lap-state loop (analog for raw recording); :268-334 `stopMarking()`/`saveTrack()` (the Track-building path the raw seam must NOT use); :156-157 `canStartTiming` checks only selection presence.
- ui/AppShell.kt:95 — `SessionController(store = sessionStore)` with NO sourceForTrack override (confirmed P1).
- session/SessionModels.kt:142 — `LocationSample.toDto()`/`SourceMetadata`; androidApp AndroidFusedLocationSampleProvider.kt:118 emits `LocationSource.PhoneGps`.
- GpsProbeModels.kt — `LocationSample`, `GpsFixStatus`; track CurrentProfileResolution for the selection input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure aggregateReady gate (ReadyGate.kt) + threshold tests</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/ReadyGate.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/ReadyGateTest.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflight.kt (sealed-result + injectable-thresholds + finite/range guard pattern to mirror)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/GpsQualitySummary.kt (degraded-accuracy line + averageUpdateRateHz)
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflightTest.kt
    - 05.1-PATTERNS.md section "session/ReadyGate.kt"; 05.1-RESEARCH.md Code Examples "Aggregate Ready as a pure function"
  </read_first>
  <behavior>
    - Missing fix / non-finite coords / accuracy worse than threshold / fix older than freshness threshold / rate below cadence threshold / no selected course/profile / unconfirmed start/finish / direction-incompatible / wrong-course preflight Blocked → each returns NotReady with the corresponding ReadyBlocker reason.
    - All eight conditions satisfied → returns Ready.
    - Multiple failing conditions → NotReady listing every applicable reason.
    - Threshold values are conservative and injectable; constructing thresholds with non-finite/non-positive values throws via init require.
  </behavior>
  <action>Create `ReadyGate.kt` in `...shared.session`, zero Compose/platform imports. Define `sealed interface ReadyState { data object Ready; data class NotReady(val reasons: List<ReadyBlocker>) }`, an enum/sealed `ReadyBlocker` (MissingFix, NonFiniteFix, PoorAccuracy, StaleFix, LowSampleRate, NoCourseSelected, StartFinishUnconfirmed, DirectionIncompatible, WrongCourseBlocked, PreflightUnavailable), and an injectable `ReadyThresholds` data class with `init { require(...) }` validation. Derive conservative defaults from existing constants: `maxHorizontalAccuracyMeters = 25.0` (the engine degraded line, stricter than preflight's 100.0), `maxFixAgeMillis = 15_000` (preflight freshness), and a `minSampleRateHz` derived conservatively (e.g. 1.0 Hz — the observed real delivery floor from RESEARCH; record the chosen value). Implement `fun aggregateReady(latest: LocationSample?, nowElapsedMillis: Long, recentRateHz: Double?, selection: CurrentProfileResolution, startFinishConfirmed: Boolean, directionCompatible: Boolean, preflight: CoursePreflightResult, thresholds: ReadyThresholds = ReadyThresholds.Default): ReadyState` accumulating reasons (motion/speed/heading are NOT inputs per D-15). Non-finite/out-of-range GPS returns NotReady, never throws. KDoc must cite D-13/D-14/D-15 and "Pure ... owns no UI, storage, platform, matcher, or lap-engine state". Create `ReadyGateTest.kt` with one test per ReadyBlocker plus the all-satisfied Ready case and a multi-reason case. Record the chosen numeric thresholds in the SUMMARY (they will be transcribed into 5.1-UAT.md by Plan 06).</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest --tests "com.huanfuli.lapsight.shared.session.ReadyGateTest"</automated>
  </verify>
  <acceptance_criteria>
    - ReadyGateTest passes; every ReadyBlocker has a covering test and Ready is reachable when all conditions hold.
    - Grep of ReadyGate.kt finds zero `import androidx`/`import platform` lines; `aggregateReady` returns ReadyState (never a bare Boolean).
    - Non-finite GPS input returns NotReady (no exception).
    - Chosen thresholds recorded in SUMMARY.
  </acceptance_criteria>
  <done>A single pure conservative Ready gate decides Ready/NotReady over all seven D-14 inputs plus preflight, fully unit-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raw-recording seam (RawRecordingController.kt) + no-lap-state test</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/RawRecordingController.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/RawRecordingControllerTest.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt (tick/capture loop analog at :254; the Track-building path at :268-334 the seam must NOT use)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt (the lap/ghost machinery the seam must NOT construct)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/GpsQualitySummary.kt (from(samples) rollup for the diagnostic export)
    - 05.1-PATTERNS.md section "Raw-recording seam"
  </read_first>
  <behavior>
    - Starting raw recording and feeding samples accumulates LocationSamples and a GpsQualitySummary.
    - The seam constructs no TimingSessionRecorder / LapEngine / LiveDeltaEngine; its output reports lapCount == 0 and carries an explicit diagnostic/raw marker so it can never count as a valid timing session.
    - Stopping produces a diagnostic export payload (samples + GPS quality) reusing LocationSample.toDto().
  </behavior>
  <action>Create `RawRecordingController.kt` in `...shared.session`, zero Compose/platform imports, mirroring the `DriveMarkingController` provider/tick/stop loop but emitting a diagnostic raw record (NOT a Track and NOT a timing session). It must take a `LocationSampleProvider` + injectable `now`, accumulate samples on `tick()`, expose a snapshot with sample count + `GpsQualitySummary.from(samples)`, and on `stop()` return a raw diagnostic payload built from `LocationSample.toDto()` with an explicit raw/diagnostic flag. It must construct NO `TimingSessionRecorder`, `LapEngine`, or `LiveDeltaEngine`. KDoc cites D-16/D-17 ("records for diagnosis, never enters formal lap/ghost timing; excluded from the five valid sessions"). Create `RawRecordingControllerTest.kt` asserting: samples accumulate; the snapshot exposes GPS quality; there is no lap state / lapCount is 0 / no draft TimingSession is created; the stop payload carries the raw/diagnostic marker.</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest --tests "com.huanfuli.lapsight.shared.session.RawRecordingControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - RawRecordingControllerTest passes.
    - Grep of RawRecordingController.kt finds no reference to `TimingSessionRecorder`, `LapEngine`, or `LiveDeltaEngine` construction.
    - The raw payload is explicitly flagged diagnostic/raw and reports zero laps.
  </acceptance_criteria>
  <done>A diagnostic raw-recording seam captures samples + GPS quality without any lap/ghost state and is structurally excluded from valid-session counting.</done>
</task>

<task type="auto">
  <name>Task 3: Gate formal timing on Ready, fix source provenance, surface state on dash</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt (:52-58 sourceForTrack default, :99-135 startTiming, :51 now)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt (:95 SessionController construction)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt (existing GPS-state/feed-mode rendering branches; the dash readout layout)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt (:156-157 canStartTiming)
  </read_first>
  <action>(a) In `SessionController.startTiming`, gate formal timing on `aggregateReady(...)`: compute ReadyState from the latest sample/clock/rate/selection/start-finish/direction/preflight before constructing the TimingSessionRecorder; if NotReady, do not start formal timing and surface the reasons (the existing preflight Blocked/override path remains — wrong-course override still consumes the captured evidence per D-18). (b) Fix the P1 at `AppShell.kt:95`: construct `SessionController` with a `sourceForTrack` override that returns the live feed source (`LocationSource.PhoneGps` for the phone provider, Simulated for the simulated feed) so a real run on any Track is labeled by the live feed, not the Track's marking source (D-04/D-42). Do not let simulated data masquerade as real or vice-versa. (c) In `DriveScreen.kt`, surface the ReadyState (Ready vs NotReady-with-primary-reason) and the raw-recording-active state as part of the core dash readout (D-32), reusing the existing GPS-state color branches; when NotReady, expose the raw-recording start path instead of formal timing. Keep the manual orientation toggle; never introduce sensor rotation (project memory). Do not restructure the dash information hierarchy (UI tokenization is Plan 05).</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest && ./gradlew :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Full shared suite stays green and `:androidApp:assembleDebug` succeeds.
    - Grep of AppShell.kt shows `SessionController(` constructed with a `sourceForTrack` argument (no longer the bare default).
    - Grep of SessionController.kt shows `startTiming` references `aggregateReady`; existing WrongCoursePreflight override path is preserved.
    - DriveScreen renders a Ready/not-Ready indicator and a raw-recording-active indicator.
  </acceptance_criteria>
  <done>Formal timing only starts when Ready, real runs carry the live PhoneGps source, and the dash shows Ready + raw-recording state without changing the dash hierarchy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS hardware/provider → Ready gate | Untrusted live GPS (possibly non-finite, stale, low-rate) crosses into the Ready decision and timing start. |
| Track marking source vs live feed source | Source provenance must not be confused between simulated and real (evidence integrity). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-02 | Tampering / Spoofing | `aggregateReady` (V5 input validation) | mitigate | Non-finite/out-of-range/stale/low-rate GPS returns NotReady with a reason; never throws; thresholds validated via init require. |
| T-5.1-06 | Spoofing (evidence integrity) | `SessionController.sourceForTrack` wiring (D-04/D-42) | mitigate | Inject the live feed source so a real run is never labeled simulated and simulated never labeled real. |
| T-5.1-07 | Repudiation | raw-recording exclusion (D-17) | mitigate | Raw output carries an explicit diagnostic marker and zero laps so it cannot be mistaken for a valid session. |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages. |
</threat_model>

<verification>
- `./gradlew :shared:testDebugUnitTest` green (ReadyGateTest + RawRecordingControllerTest + existing suite).
- `./gradlew :androidApp:assembleDebug` succeeds.
- Grep confirms ReadyGate.kt and RawRecordingController.kt import no Compose/platform symbols.
</verification>

<success_criteria>
- Conservative pure Ready gate + raw-recording seam exist, unit-covered, clean-room.
- Formal timing gated on Ready; raw recording available when not Ready and excluded from valid sessions.
- P1 source provenance fixed so field exports carry the live feed source.
- Dash shows Ready + raw-recording state.
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-03-SUMMARY.md` when done. Record the chosen Ready thresholds (accuracy/age/rate) for transcription into 5.1-UAT.md.
</output>
