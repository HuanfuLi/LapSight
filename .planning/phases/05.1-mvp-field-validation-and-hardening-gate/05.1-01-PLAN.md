---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoder.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoderTest.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibraryTest.kt
autonomous: true
requirements: [GPS-05, LAP-06, LAP-03, GPS-04]
user_setup: []

must_haves:
  truths:
    - "An exported TimingSession JSON can be decoded back into LocationSamples and replayed through the existing engine, producing a lap/sector/state result."
    - "Malformed or corrupt exported bytes return a typed Corrupt result, never a thrown serialization exception."
    - "A good-to-poor mid-lap GPS degradation scenario exists as a deterministic fixture."
    - "A closed-course perpendicular start/finish low-frequency scenario exists as a deterministic fixture."
    - "The exact Gradle host-test task name and its runtime are recorded for downstream plans."
    - "Decision coverage: D-24 (phone-GPS limits handled honestly — the good-to-poor mid-lap degradation fixture exercises the suppress/degrade regime) is realized by this plan's deterministic fixtures."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoder.kt"
      provides: "Pure JSON-export to ReplayRunner decode helper (the missing field-evidence to replay link)"
      contains: "decodeForReplay"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt"
      provides: "degradationDuringTiming and closedCoursePerpendicular fixtures registered in scenario(id)"
      contains: "degradationDuringTiming"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoderTest.kt"
      provides: "round-trip + corrupt-path tests for the decoder"
  key_links:
    - from: "session/SessionReplayDecoder.kt"
      to: "lap/ReplayRunner.kt"
      via: "ReplayRunner(course, config).run(samples)"
      pattern: "ReplayRunner\\("
    - from: "session/SessionReplayDecoder.kt"
      to: "session/SessionModels.kt"
      via: "LocationSampleDto.toModel() + courseFromTrack(direction)"
      pattern: "toModel\\(\\)"
---

<objective>
Build the Wave-1 deterministic-replay foundation the whole gate rests on: a pure JSON-export to replay decode helper (the currently-missing link between a saved field session and the engine, D-25/D-28), plus the two missing GPS fixtures (good-to-poor mid-lap degradation for D-24, and a closed-course perpendicular start/finish low-frequency lap for D-19/D-24). Also pin down the exact Gradle host-test task name and runtime so every downstream plan uses a working command.

Purpose: Determinism (D-25..D-28) is the spine of the Go / Hardening-Required / No-Go decision. Without a decode-from-export path, field session exports cannot be replayed, and without the degradation/low-frequency fixtures the Nyquist input space is under-sampled where lap-count failures are most likely.
Output: `SessionReplayDecoder.kt` + tests, extended `GpsFixtureLibrary.kt` + tests, and a recorded Gradle task name/runtime note in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-RESEARCH.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-PATTERNS.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-VALIDATION.md
@AGENTS.md

<interfaces>
<!-- Reuse these existing seams; do NOT write a second parser or engine (AGENTS.md clean-room + Don't-Hand-Roll). -->
- export/JsonExportService.kt:23 — `private val json = FileSessionStore.canonicalJson` (the canonical config to reuse on decode so encode/decode are byte-symmetric).
- session/SessionModels.kt:154 — `fun LocationSampleDto.toModel(): LocationSample` (the existing mapper).
- session/TimingSessionRecorder.kt:80 — `fun courseFromTrack(startFinish, sectors, direction): CourseDefinition?` (direction-aware course rebuild; required so Reverse decodes correctly).
- lap/ReplayRunner.kt:54 — `ReplayRunner(course, config).run(samples)` (the consume side; exposes finalState + sectorEvents).
- session/SessionModels.kt — `TimingSessionPayloadV1` shape carries `samples`, `session.startFinish`, `session.sectors`, `session.direction`.
- fixtures/GpsFixtureLibrary.kt:178 — `withOutlierLoop(...)` span-mutation idiom; line 121 `ovalSession(...)`; lines 52-61 const-id block; line 455+ `scenario(id)` when-registry; `Lcg` seed pattern lines 94-106.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure SessionReplayDecoder (JSON export to ReplayRunner) + confirm Gradle task name</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoder.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoderTest.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/export/JsonExportService.kt (the encode mirror; reuse FileSessionStore.canonicalJson and the ExportFailedException/Corrupt error style)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt (TimingSessionPayloadV1 shape + LocationSampleDto.toModel())
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt (3-arg courseFromTrack with direction)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/ReplayRunner.kt (run(samples) consume side)
    - 05.1-PATTERNS.md section "session/SessionReplayDecoder.kt"
  </read_first>
  <action>Create `SessionReplayDecoder.kt` in package `...shared.session`, zero Compose/platform/Okio imports. Implement `decodeForReplay(exportedBytes: ByteArray, config: LapEngineConfig = LapEngineConfig()): ReplayDecodeResult` that (1) decodes via `FileSessionStore.canonicalJson.decodeFromString(TimingSessionPayloadV1.serializer(), exportedBytes.decodeToString())`, (2) maps `payload.samples` with the existing `LocationSampleDto.toModel()` (do not write a new parser), (3) rebuilds the course with the 3-arg `courseFromTrack(payload.session.startFinish, payload.session.sectors, payload.session.direction)`, (4) runs `ReplayRunner(course, config).run(samples)`. Return a sealed `ReplayDecodeResult` mirroring the WrongCoursePreflight/LoadResult "bad input is data, not exception" pattern: `Decoded(val result: ReplayResult)`, `Corrupt(val reason: String)` for serialization failure (catch the kotlinx.serialization exception and convert), and `NoCourse` when `courseFromTrack` returns null (payload lacks a confirmed start/finish). KDoc must cite D-25/D-28 and the clean-room rule. Reuse the SAME `FileSessionStore.canonicalJson` instance so a session encoded by `JsonExportService` decodes symmetrically. Create `SessionReplayDecoderTest.kt`: encode a known in-memory `TimingSessionPayloadV1` (or export an existing fixture-built session via JsonExportService), decode it, and assert the decoded `ReplayResult.finalState.lapCount` and `sectorEvents` match a direct `ReplayRunner` run of the same samples; assert garbage bytes return `Corrupt` (no thrown exception); assert a payload with null start/finish returns `NoCourse`. Then determine the exact host-test Gradle task: run the candidate commands and record which one executes commonTest (`:shared:testDebugUnitTest` is the working assumption from RESEARCH; the build uses `androidLibrary { withHostTest }`). Record the confirmed task name and its wall-clock runtime in the SUMMARY.</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest --tests "com.huanfuli.lapsight.shared.session.SessionReplayDecoderTest"</automated>
  </verify>
  <acceptance_criteria>
    - `:shared:testDebugUnitTest` (or the confirmed equivalent) exits 0 with SessionReplayDecoderTest passing.
    - Decoded replay output equals a direct ReplayRunner run of the same samples (lapCount + sectorEvents structural equality).
    - Corrupt bytes yield `ReplayDecodeResult.Corrupt`; a Grep of SessionReplayDecoder.kt finds zero `import androidx`, `import platform`, or `import okio` lines.
    - The exact Gradle host-test task name and runtime are written in the SUMMARY.
  </acceptance_criteria>
  <done>A pure decoder converts exported session bytes to a replay result via existing mappers/engine, handles corrupt/no-course input as data, and the confirmed test command is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add degradation-during-timing and closed-course perpendicular fixtures</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibraryTest.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt (ovalSession line 121, withOutlierLoop line 178, droppedLowFrequency line 219, const-id block 52-61, scenario(id) registry 455+, Lcg seed 94-106)
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibraryTest.kt
    - 05.1-PATTERNS.md section "fixtures/GpsFixtureLibrary.kt"
    - 05.1-VALIDATION.md Wave 0 Requirements
  </read_first>
  <action>Extend `GpsFixtureLibrary.kt` with two new scenarios following the existing full registration ritual (const id + builder fun + `scenario(id)` when-arm with name/description), all samples `LocationSource.Simulated` and seeded with fresh distinct `Lcg` seed constants for reproducibility. (1) `degradationDuringTiming()` (D-24): compose `ovalSession(...)` then mutate a mid-lap span exactly like `withOutlierLoop` does, pushing `horizontalAccuracyMeters` past the 25.0 m degraded line (use ~48.0 m) across roughly the second half of one loop so the Ready/suppression path deterministically degrades mid-lap. (2) `closedCoursePerpendicular()` (D-19/D-24): a multi-loop oval whose start/finish is crossed perpendicularly at low sample frequency (mirror `droppedLowFrequency` cadence, ~12 points/loop) so crossing time must be reconstructed by interpolation — the most likely real-world lap-count failure regime. Add a `const val` id for each and a matching `when` arm. Add two cases to `GpsFixtureLibraryTest.kt` asserting each scenario builds a non-empty deterministic sample list (same id produces identical samples across two calls), that the degradation scenario contains samples with accuracy > 25.0 m in its degraded span, and that the perpendicular scenario yields the expected loop count when replayed through `ReplayRunner` with a perpendicular start/finish course.</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest --tests "com.huanfuli.lapsight.shared.fixtures.GpsFixtureLibraryTest"</automated>
  </verify>
  <acceptance_criteria>
    - GpsFixtureLibraryTest passes; both new scenarios resolve through `scenario(id)`.
    - `scenario(id)` for each new id called twice returns structurally identical sample lists (deterministic).
    - degradationDuringTiming has a contiguous span with horizontalAccuracyMeters > 25.0; closedCoursePerpendicular replays to its expected lap count.
  </acceptance_criteria>
  <done>Two new deterministic fixtures cover the GPS-degradation and low-frequency-perpendicular regimes the determinism suite and Ready tests require.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exported file/bytes → decoder | Untrusted/possibly corrupt JSON crosses into core timing logic on replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-01 | Denial of Service | `SessionReplayDecoder.decodeForReplay` (V5 input validation) | mitigate | Catch kotlinx.serialization failure and return `ReplayDecodeResult.Corrupt`; null course returns `NoCourse`; never throw on malformed bytes ("bad input is data"). |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages added (RESEARCH Package Legitimacy Audit: zero new deps). |
</threat_model>

<verification>
- `./gradlew :shared:testDebugUnitTest` (or confirmed equivalent) green for SessionReplayDecoderTest + GpsFixtureLibraryTest.
- Grep confirms SessionReplayDecoder.kt and the new fixture builders import zero Compose/platform/Okio symbols.
</verification>

<success_criteria>
- Decoder round-trips an exported session to an identical replay result and handles corrupt/no-course bytes as typed data.
- Two new deterministic fixtures (degradation-during-timing, closed-course perpendicular) registered and tested.
- Exact Gradle host-test task name and runtime recorded for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md` when done. Record the confirmed Gradle host-test task name and runtime.
</output>
