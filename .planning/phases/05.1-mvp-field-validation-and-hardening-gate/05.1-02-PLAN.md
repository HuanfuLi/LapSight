---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/FullPipelineDeterminismTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/ReplayTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CompleteSectorReplayTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngineTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/DraftRecoveryTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CourseDirectionReplayTest.kt
autonomous: true
requirements: [LAP-02, LAP-03, LAP-04, LAP-06, LAP-07, GHOST-02, GHOST-03, GHOST-04, GPS-05]

must_haves:
  truths:
    - "Running the full SessionController pipeline N times over the same samples produces identical lap count, lap times, completedSectorResults, LiveDeltaSnapshot sequence, and CourseCompatibilityKey/direction."
    - "Decoding an exported JSON session and replaying it N times produces identical algorithmic output (formatting/rounding excepted)."
    - "Determinism is asserted across the GPS regimes: clean, noise/drift, dropped-low-frequency, degradation-during-timing, direction turnaround/reverse, and recovery."
    - "A resumed mid-draft session yields the same engine state as a fresh replay of the same samples."
    - "Decision coverage: D-25, D-26, D-27, D-28 (replay determinism is a hard gate; identical lap count, lap/sector times, ghost-delta state transitions, and course/profile/direction decisions; only formatting/rounding may differ; replay diagnoses anomalies) are enforced by this determinism suite."
  artifacts:
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/FullPipelineDeterminismTest.kt"
      provides: "N-run full-pipeline structural-equality determinism suite over SessionController"
      contains: "completedSectorResults"
  key_links:
    - from: "FullPipelineDeterminismTest.kt"
      to: "session/SessionReplayDecoder.kt"
      via: "decodeForReplay on an exported session, asserted N-run identical"
      pattern: "decodeForReplay"
    - from: "FullPipelineDeterminismTest.kt"
      to: "fixtures/GpsFixtureLibrary.kt"
      via: "degradationDuringTiming + closedCoursePerpendicular scenarios"
      pattern: "scenario\\("
---

<objective>
Widen determinism coverage from the current `finalState`-only assertion to the full D-26 surface: a standing N-run determinism suite over the full `SessionController`+`TimingSessionRecorder` pipeline asserting lap count, lap times, `completedSectorResults`, the ordered `LiveDeltaSnapshot` sequence, and the resolved `CourseCompatibilityKey`/direction are byte-identical across repeated runs and across decode-from-export. Promote the existing direction/turnaround and recovery cases into the standing suite.

Purpose: D-25..D-28 are a hard gate. The existing `ReplayTest.replayIsDeterministic` only checks `finalState`, missing sector-result and ghost-delta state-transition drift. This plan closes that gap and links field exports to deterministic validation via the Plan 01 decoder.
Output: New `FullPipelineDeterminismTest.kt` plus widened existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-RESEARCH.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-PATTERNS.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-VALIDATION.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md

<interfaces>
- lap/ReplayTest.kt:17 — existing `replayIsDeterministic` asserts only `a.finalState == b.finalState` (the gap to widen).
- lap/ReplayRunner.kt:28 — `sectorEvents` change-detection idiom (detect changes between consecutive steps, do NOT de-dup a sticky field — WR-05).
- lap/CompleteSectorReplayTest.kt:82 — `state.completedSectorResults.filter { it.lapNumber == 1 }`; order assertion via `.map { it.sectorOrder }`.
- session/SessionController.kt:51 — injectable `now: () -> Long`; drive with a fixed clock + InMemorySessionStore for determinism.
- session/SessionReplayDecoder.kt — `decodeForReplay(bytes)` from Plan 01 for the export-replay assertion.
- ghost/LiveDeltaEngine.kt → `LiveDeltaSnapshot` (assert the ordered sequence of states, not just the final delta).
- track/GhostCompatibility.kt → `CourseCompatibilityKey(profileId, geometryCompatibilityId, direction, isSimulated)`.
- lap/LapEngineConfig.kt — `lenientForTests()` for engine-tuning-independent assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full-pipeline N-run determinism suite over SessionController</name>
  <files>shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/FullPipelineDeterminismTest.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt (injectable now, startTiming, snapshot accessors)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt (onSample, completedSectorResults, courseFromTrack)
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CompleteSectorReplayTest.kt (sector-result assertion idiom)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionReplayDecoder.kt (Plan 01 decoder)
    - 05.1-PATTERNS.md section "FullPipelineDeterminismTest.kt"; 05.1-RESEARCH.md "Deterministic Replay Strategy" table
  </read_first>
  <behavior>
    - For each fixture regime (CLEAN_10_LOOP, NOISE_DRIFT, DROPPED_LOW_FREQUENCY, degradationDuringTiming, closedCoursePerpendicular, a Reverse-direction case, and a recovery case): running the full pipeline twice yields equal completed-laps list, equal `completedSectorResults`, equal ordered `LiveDeltaSnapshot` sequence, and equal `CourseCompatibilityKey`/direction.
    - Exporting one session to JSON, decoding via `decodeForReplay`, and replaying twice yields identical `ReplayResult` (lapCount + sectorEvents + finalState).
    - Lap/sector times compared as exact `Long` millis equality; only derived doubles may use a tolerance.
  </behavior>
  <action>Create `FullPipelineDeterminismTest.kt` in `...shared.session`. Add a `runOnce(scenarioId)` helper that constructs `SessionController(store = InMemorySessionStore(), now = fixedClock)`, builds the course from the fixture's start/finish + direction, feeds `GpsFixtureLibrary.scenario(id)` samples through the recorder, and returns a capture object holding: completed laps list, `completedSectorResults`, the ordered `LiveDeltaSnapshot` sequence (collect each step's snapshot using the `ReplayRunner.sectorEvents` change-detection idiom — capture the snapshot whenever it differs from the previous, do NOT de-dup a sticky field), and the resolved `CourseCompatibilityKey`. Write one `@Test` per regime named `<behavior>` asserting `runOnce(id) == runOnce(id)` via Kotlin data-class structural equality (no custom comparator). Add a dedicated `@Test` that exports a built session through `JsonExportService`, decodes via `SessionReplayDecoder.decodeForReplay`, and asserts two decode-replays are identical (D-25/D-28). Use `LapEngineConfig.lenientForTests()`. KDoc must cite D-25/D-26/D-27/D-28.</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest --tests "com.huanfuli.lapsight.shared.session.FullPipelineDeterminismTest"</automated>
  </verify>
  <acceptance_criteria>
    - FullPipelineDeterminismTest passes; one test per listed regime plus the export-decode-replay test.
    - Assertions cover completed laps, `completedSectorResults`, ordered `LiveDeltaSnapshot` sequence, and `CourseCompatibilityKey` (not just lapCount or finalState).
    - Times asserted as exact Long-millis equality.
  </acceptance_criteria>
  <done>A standing suite proves the full pipeline and the export-decode-replay loop are deterministic across all required GPS/direction/recovery regimes.</done>
</task>

<task type="auto">
  <name>Task 2: Widen existing replay/recovery/ghost/direction tests to full determinism</name>
  <files>shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/ReplayTest.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CompleteSectorReplayTest.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngineTest.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/DraftRecoveryTest.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CourseDirectionReplayTest.kt</files>
  <read_first>
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/ReplayTest.kt:17 (finalState-only assertion)
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngineTest.kt
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/DraftRecoveryTest.kt
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CourseDirectionReplayTest.kt
  </read_first>
  <action>Widen `ReplayTest.replayIsDeterministic` to assert the full step/result sequence, not only `finalState` (lap events + sectorEvents). Extend `CompleteSectorReplayTest` with an N-run (N>=3) assertion that `completedSectorResults` is identical across runs. Add a transition-sequence assertion to `LiveDeltaEngineTest` that the ordered sequence of `LiveDeltaSnapshot` availability/state transitions (matched, unmatched-to-`--`, in-session new-best) is identical across two runs of the same input (use the change-detection idiom, not de-dup). Add to `DraftRecoveryTest` an equivalence assertion: a session resumed from a mid-draft checkpoint reaches the same engine state as a fresh replay of the same samples. Promote the `CourseDirectionReplayTest` Recorded/Reverse and mid-lap-turnaround cases into determinism assertions (two runs identical), not just behavior assertions. Do not change engine/production code; tests only.</action>
  <verify>
    <automated>./gradlew :shared:testDebugUnitTest --tests "com.huanfuli.lapsight.shared.lap.ReplayTest" --tests "com.huanfuli.lapsight.shared.lap.CompleteSectorReplayTest" --tests "com.huanfuli.lapsight.shared.ghost.LiveDeltaEngineTest" --tests "com.huanfuli.lapsight.shared.session.DraftRecoveryTest" --tests "com.huanfuli.lapsight.shared.lap.CourseDirectionReplayTest"</automated>
  </verify>
  <acceptance_criteria>
    - All five widened tests pass.
    - `ReplayTest` no longer asserts only `finalState`; Grep of ReplayTest.kt shows an assertion over a lap/sector sequence.
    - LiveDeltaEngineTest asserts an ordered transition sequence; DraftRecoveryTest asserts resume == fresh replay.
  </acceptance_criteria>
  <done>The pre-existing determinism/recovery/ghost/direction tests assert full algorithmic output, closing the finalState-only gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exported bytes → decode → replay (test) | Determinism assertion consumes exported session bytes via the Plan 01 decoder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-02 | Tampering | determinism assertions on decoded export | mitigate | Assert exact structural equality across N runs; any algorithmic drift fails the suite (D-27). |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages (tests only, kotlin-test already present). |
</threat_model>

<verification>
- Full shared suite green: `./gradlew :shared:testDebugUnitTest`.
- No watch-mode flags in any command.
</verification>

<success_criteria>
- N-run full-pipeline determinism asserted over lap/sector/ghost/course-direction plus export-decode-replay.
- Existing replay/recovery/ghost/direction tests widened beyond finalState.
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-02-SUMMARY.md` when done.
</output>
