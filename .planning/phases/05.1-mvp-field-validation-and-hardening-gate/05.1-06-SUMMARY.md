---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 06
subsystem: validation-evidence
tags: [uat, field-validation, evidence-index, ready-gate, replay, android-only, documentation]

# Dependency graph
requires:
  - phase: 05.1-mvp-field-validation-and-hardening-gate
    provides: SessionReplayDecoder.decodeForReplay + confirmed host-test/build commands (Plan 01)
  - phase: 05.1-mvp-field-validation-and-hardening-gate
    provides: conservative Ready thresholds 25.0 m / 15000 ms / 1.0 Hz (Plan 03)
provides:
  - 5.1-UAT.md — device-independent Android UAT protocol (smoke + display + closed-course + Ready + Review/export + replay-diagnosis)
  - 5.1-FIELD-TEST-LOG.md — 3+2 per-session evidence-index skeleton on the D-48 template
affects: [05.1-07 field UAT execution, 05.1-08 go-nogo]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Evidence-plan artifacts authored ahead of field execution so Plan 07 carries only irreducible manual evidence (D-54)"
    - "Replay-diagnosis-first: every field anomaly decoded via SessionReplayDecoder before root-cause assignment (D-28)"

key-files:
  created:
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md
  modified: []

key-decisions:
  - "Ready thresholds transcribed verbatim from Plan 03: horizontal accuracy 25.0 m, fix freshness 15000 ms, sample rate 1.0 Hz"
  - "Build/install commands use the corrected Plan 01 host-test task :shared:testAndroidHostTest (NOT :shared:testDebugUnitTest) plus :androidApp:assembleDebug + adb install"
  - "Review + JSON/GPX export smoke given concrete pass/fail acceptance (non-empty re-parsing GPX; JSON re-replays to identical output)"

requirements-completed: [PLAT-01, PLAT-03, PLAT-04, PLAT-05, GPS-01, GPS-02, SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, GHOST-01]

# Metrics
duration: 12min
completed: 2026-06-29
---

# Phase 5.1 Plan 06: Field-UAT Protocol and Evidence-Index Authoring Summary

**Authored the two evidence-plan artifacts that complete everything automatable about Phase 5.1 field UAT — `5.1-UAT.md` (the executable Android-only on-device + mounted-display + closed-course + Review/export + replay-diagnosis protocol with the verbatim Plan 03 Ready thresholds and the 3+2 five-valid-session matrix) and the `5.1-FIELD-TEST-LOG.md` per-session evidence-index skeleton on the D-48 template — so Plan 07 carries only the irreducible manual field evidence (D-54).**

## Performance

- **Duration:** ~12 min
- **Tasks:** 2
- **Files:** 2 created, 0 modified
- **Build/tests:** N/A — documentation-only artifacts (no source touched, no package installs).

## Accomplishments

- **Task 1 — `5.1-UAT.md`:** A complete, device-independent UAT protocol covering: (1) outcome semantics mapping to Go/Hardening-Required/No-Go/Pending; (2) Android-only scope + two-real-phone matrix with recorded identity, emulators for layout smoke only (D-02/D-05..D-08); (3) build/install via the corrected `:shared:testAndroidHostTest` + `:androidApp:assembleDebug` + `adb install`; (4) on-device smoke paths S1–S6 (launch, permission/fix, live speed/accuracy/rate, portrait/landscape, keep-dash-visible, honest Phone-GPS↔Simulated switch); (5) mounted-display UAT graded portrait+landscape × daylight+low-light against the seven core glance elements (D-29..D-32); (6) the Ready-before-timing rule with the **verbatim Plan 03 thresholds (25.0 m / 15000 ms / 1.0 Hz)** plus raw-recording and wrong-course-override rules (D-13..D-18); (7) the five-valid-session 3+2 matrix and four-item per-session evidence (D-10/D-11/D-12/D-47); (8) acceptance metrics — lap-count 100% HARD blocker, ≤0.5 s video target, unreadable-crossing exclusion (D-19..D-23); (9) the replay-diagnosis-first protocol against `SessionReplayDecoder.decodeForReplay` with the six root-cause categories and the permanent-fixture rule (D-25..D-28); and (10) the Review + JSON/GPX export smoke (R1–R5) with concrete pass/fail acceptance (SESS-02/03/04/05, GHOST-01), including a non-empty re-parsing GPX and JSON that re-replays to identical output. The D-54 pending-not-No-Go clause is stated explicitly.
- **Task 2 — `5.1-FIELD-TEST-LOG.md`:** A per-session evidence index with five reserved rows (three Primary + two Secondary, D-11), each on the D-48 minimal template (device, session ID/export, lap-count pass/fail, replay result, video reference/result) plus JSON+GPX export references (SESS-05), a Ready-before-timing checklist row, a wrong-course-override (D-18) row, and an optional-extras note (D-49, fill only on failure/anomaly). The header states the five-valid-session minimum and the pending status; a roll-up table tracks the gate metrics.

## Task Commits

1. **Task 1: Author 5.1-UAT.md** — `077f8a9` (docs)
2. **Task 2: Add 5.1-FIELD-TEST-LOG.md skeleton** — `90cedf9` (docs)

## Files Created/Modified

- `5.1-UAT.md` — executable Android UAT protocol (live timing + Review/export smoke + replay-diagnosis), Ready thresholds transcribed.
- `5.1-FIELD-TEST-LOG.md` — 3+2 per-session evidence-index skeleton on the D-48 template.

## Decisions Made

- **Thresholds transcribed verbatim** from Plan 03 (25.0 m / 15000 ms / 1.0 Hz) rather than re-derived, to keep the UAT and the implemented `ReadyThresholds` in lock-step.
- **Corrected build commands** carried forward from Plan 01 (`:shared:testAndroidHostTest`, not the non-existent `:shared:testDebugUnitTest`) so the protocol is runnable as written.
- **Concrete export acceptance** wording chosen so the GPX/JSON smoke has unambiguous pass/fail (non-empty re-parsing `.gpx`; JSON re-replays to identical, lap-count-correct output via `SessionReplayDecoder`).

## Deviations from Plan

None — plan executed exactly as written. Both tasks are documentation artifacts; no source code, tests, or dependencies were touched (consistent with threat register T-5.1-SC: no new packages).

## Decision Coverage

D-02 (Android-only), D-03 (no extra devices), D-05/D-06/D-07 (two real phones, recorded identity), D-08 (emulators = layout smoke only), D-10/D-11 (five valid sessions, 3+2), D-12/D-47 (four-item per-session evidence), D-13 (Ready-before-timing), D-14 (conservative thresholds transcribed), D-16/D-17 (raw recording excluded), D-18 (wrong-course override as evidence), D-19/D-20 (lap-count 100% hard blocker / No-Go), D-21/D-22/D-23 (≤0.5 s video target, video+replay workaround, unreadable exclusion), D-25..D-28 (replay determinism + diagnosis-first + permanent fixtures), D-29/D-30/D-31/D-32 (portrait/landscape, daylight/low-light, one-second glance, core state list), D-33/D-34 (no safety mechanisms; readability-only grading), D-39 (diagnostics must not dominate), D-48 (minimal field-note template), D-49 (optional extras on anomaly), D-54 (pending-not-No-Go).

## Threat Coverage

- **T-5.1-09 (Repudiation, field evidence):** mitigated — every valid session row requires export + replay result + video + notes; missing any item disqualifies the session from the five.
- **T-5.1-SC (Tampering, dependency installs):** accept — documentation only; no packages added.

## Known Stubs

None. The artifacts are intentionally template skeletons to be filled with real field evidence during Plan 07 — this is the planned pending state (D-54), not an unwired stub.

## Self-Check: PASSED

- `5.1-UAT.md` present on disk; contains `ready`, `lap count`, and `gpx` (verify command returned PRESENT).
- `5.1-FIELD-TEST-LOG.md` present on disk; contains `lap count` (verify command returned PRESENT).
- Both task commits (`077f8a9`, `90cedf9`) verified in git history.

---
*Phase: 05.1-mvp-field-validation-and-hardening-gate*
*Completed: 2026-06-29*
