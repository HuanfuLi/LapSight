---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 04
type: execute
wave: 3
depends_on: ["05.1-02", "05.1-03"]
files_modified:
  - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md
  - docs/THIRD-PARTY-LICENSES.md
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, SAFE-01, SAFE-02, SAFE-03, SAFE-04, GPS-04]

must_haves:
  truths:
    - "Every Phase 1-5 core path is audited with severity-tagged findings (P0/P1/P2/P3) bounded to core paths (D-40..D-44)."
    - "The confirmed P1 source-provenance finding is recorded and verified resolved by Plan 03."
    - "The clean-room boundary is verified: the shared lap/ghost/session engine imports no Compose/platform/Okio symbols (ARCH-01/D-45)."
    - "ARCH-03 third-party license inventory and ARCH-04 GPL-not-copied attestation are documented; export-filename path-traversal and local-GPS-privacy are assessed."
    - "REQUIREMENTS.md traceability is reconciled against actual implementation (the Pending-but-shipped hygiene finding)."
    - "Decision coverage: D-41, D-42, D-43 (core-path audit scope spanning architecture/lap engine/replay/Ready/session/storage/export/track-profile/ghost/UI-state/licensing-privacy; P0/P1/P2 in core paths all fixed before Go; P2 bounded to Phase 5.1 core paths) govern this audit."
  artifacts:
    - path: ".planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md"
      provides: "Deep core-path audit with severity table and closure status"
      contains: "P0"
    - path: "docs/THIRD-PARTY-LICENSES.md"
      provides: "ARCH-03 third-party inventory + ARCH-04 clean-room attestation"
      contains: "clean-room"
  key_links:
    - from: "5.1-CODE-REVIEW.md"
      to: "shared/src + androidApp/src"
      via: "scoped audit excluding .claude/worktrees"
      pattern: "shared/src"
---

<objective>
Produce `5.1-CODE-REVIEW.md`: a deep, severity-tagged audit of every Phase 1-5 core path (D-40/D-41), with all P0/P1/P2 in core paths fixed or confirmed-fixed before Go (D-42), P3/info backlogged (D-44). Verify the clean-room architecture boundary (ARCH-01/D-45), confirm the Plan 03 source-provenance fix resolved the known P1, document ARCH-03/ARCH-04 license/privacy items, assess export-filename path traversal and local-GPS privacy, and reconcile the REQUIREMENTS.md traceability hygiene finding.

Purpose: Code audit is one of the five evidence layers composing the Go decision; unresolved P0/P1/P2 in core paths force at best Hardening Required, and a core architecture defect forces No-Go (D-45/D-53).
Output: `5.1-CODE-REVIEW.md` + `docs/THIRD-PARTY-LICENSES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-RESEARCH.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-CONTEXT.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-03-SUMMARY.md

<interfaces>
<!-- Audit scope = RESEARCH Component Responsibilities table. Scope the audit to shared/src + androidApp/src; EXCLUDE .claude/worktrees (stale duplicate copies). -->
- Core path file map: 05.1-RESEARCH.md "Component Responsibilities (real file paths)".
- Architectural Responsibility Map: 05.1-RESEARCH.md (tier each capability SHOULD own; flag leakage).
- Known P1: AppShell.kt:95 source provenance (fixed in Plan 03 - verify).
- Security Domain: 05.1-RESEARCH.md (V5 input validation; V10/V12 ExportFileNames path traversal; local GPS privacy; GPL contamination).
- Traceability hygiene: REQUIREMENTS.md marks PLAT/GPS-01..04/SAFE-01..03/ARCH-01/03/04 "Pending" though Phase 1 shipped.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep severity-tagged audit of all Phase 1-5 core paths</name>
  <files>.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md</files>
  <read_first>
    - 05.1-RESEARCH.md "Component Responsibilities (real file paths)" + "Architectural Responsibility Map" + "Common Pitfalls" + "Security Domain"
    - 05.1-03-SUMMARY.md (confirm the source-provenance fix landed)
    - the core-path source files listed in the RESEARCH component table (read each once; extract findings in one pass)
  </read_first>
  <action>Audit each core path in the RESEARCH component table: architecture boundaries, lap engine, sector/crossing logic, replay, GPS Ready/quality/provider state (including the new ReadyGate + raw-recording seam from Plan 03), session lifecycle/draft/recovery, storage/schema migration, export, track profile/revision/direction/wrong-course, ghost reference/delta confidence, live timing UI state, and license/privacy. Use a defined severity taxonomy (P0 = breaks core timing trustworthiness; P1 = wrong/unsafe behavior in core paths e.g. evidence mislabel; P2 = code-quality issue inside core paths only, NOT repo-wide cleanup; P3/info = backlog). Run scoped searches over `shared/src` and `androidApp/src` ONLY, excluding `.claude/worktrees`. Specifically: (1) verify the clean-room boundary by searching the shared lap/ghost/session/track/storage/export algorithm files for `import androidx`, `import platform`, or `import okio` (ARCH-01/D-45 - any hit in an algorithm file is a finding); (2) verify orientation stays manual (no `screenOrientation` sensor/fullSensor; layout branches on the user toggle - RESEARCH Pitfall 8); (3) assess `ExportFileNames` for track-name-derived path-traversal sanitization (V10/V12); (4) confirm `WrongCoursePreflight`, `LoadResult.Corrupt`, and the new decoder treat bad input as data (V5); (5) confirm the Plan 03 source-provenance fix resolved the known P1 (re-check AppShell SessionController wiring and that the live source flows into the session). Write `5.1-CODE-REVIEW.md` with a findings table (id, severity, core path, file:line, description, disposition: fixed / fix-required / backlog) and an overall P0/P1/P2 closure status. Re-check safety positioning copy (closed-course/private-track; SAFE-03) per D-35 and confirm it is unchanged.</action>
  <verify>
    <automated>test -f .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md && grep -v '^#' .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md | grep -Eqi 'p0|p1|p2' && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `5.1-CODE-REVIEW.md` exists and contains a severity-tagged findings table covering each core path with an overall P0/P1/P2 closure status.
    - A clean-room search result is recorded (algorithm files import no Compose/platform/Okio, or each violation is a finding).
    - The known source-provenance P1 is listed with disposition fixed (verified against Plan 03).
    - Orientation-manual, ExportFileNames sanitization, and bad-input-as-data are each assessed.
  </acceptance_criteria>
  <done>Every core path has a severity-tagged finding with disposition; clean-room boundary and the known P1 are verified; safety positioning confirmed unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document ARCH-03/ARCH-04 license + privacy and remediate in-scope findings</name>
  <files>docs/THIRD-PARTY-LICENSES.md, .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md</files>
  <read_first>
    - 05.1-RESEARCH.md "Security Domain" + "Runtime State Inventory" (verified clean: no GPL/doves in shared/src; no network/analytics in commonMain)
    - .planning/REQUIREMENTS.md (ARCH-03, ARCH-04, traceability table)
    - shared/build.gradle.kts + gradle/libs.versions.toml (the actual third-party dependency set)
  </read_first>
  <action>Create `docs/THIRD-PARTY-LICENSES.md`: (1) ARCH-03 - inventory the actual third-party dependencies from `gradle/libs.versions.toml` / `shared/build.gradle.kts` (Kotlin, Compose Multiplatform, kotlinx-serialization, Okio, AndroidX lifecycle) with their licenses; (2) ARCH-04 - a clean-room attestation that no DovesLapTimer/DovesDataViewer/GPL code was copied into `shared/src` (cite the RESEARCH verified-clean grep finding), and that GPL projects were studied only as references per AGENTS.md; (3) a privacy note that location traces are sensitive local-only personal data with no upload/analytics (the ARCH-03 privacy documentation owed). Record in `5.1-CODE-REVIEW.md` that the app's own license remains an open product decision (ROADMAP Open Decision 1 / STATE checklist) - noted as a risk, not a Phase 5.1 core-timing blocker since core is verified clean. Remediate any in-scope P0/P1/P2 the audit surfaced that has a bounded fix; if a finding's fix exceeds bounded scope, record it as fix-required with a precise scope note (do not silently defer). Append a reconciliation note mapping the REQUIREMENTS.md "Pending"-but-shipped Phase 1 requirements (PLAT-01..05, GPS-01..04, SAFE-01..03, ARCH-01/03/04) to their actual implementation status as a traceability-hygiene finding.</action>
  <verify>
    <automated>test -f docs/THIRD-PARTY-LICENSES.md && grep -qi 'clean-room' docs/THIRD-PARTY-LICENSES.md && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `docs/THIRD-PARTY-LICENSES.md` exists with a third-party inventory, a clean-room/GPL-not-copied attestation, and a local-GPS privacy note.
    - `5.1-CODE-REVIEW.md` records the app-license open decision as a risk and reconciles the REQUIREMENTS.md traceability hygiene finding.
    - Any bounded in-scope P0/P1/P2 fix is applied (shared suite stays green) or recorded as fix-required with a scope note.
  </acceptance_criteria>
  <done>ARCH-03/ARCH-04 documented, privacy noted, traceability reconciled, and in-scope findings remediated or precisely scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| track name → export filename | User-controlled track name flows into a filesystem path on export. |
| local GPS traces at rest | Sensitive personal location data persisted locally. |
| third-party / reference code → repo | License contamination risk if GPL code were copied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-03 | Tampering | `ExportFileNames` (V10/V12) | mitigate | Audit confirms track-name-derived filenames are sanitized against path traversal; record finding if not. |
| T-5.1-04 | Information Disclosure | local GPS traces (privacy) | mitigate | Document local-first, no-upload, no-analytics in THIRD-PARTY-LICENSES.md (ARCH-03 owed). |
| T-5.1-05 | Compliance (Tampering) | GPL contamination in core paths | mitigate | ARCH-04 clean-room attestation citing the verified-clean shared/src search. |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages; existing deps inventoried for ARCH-03. |
</threat_model>

<verification>
- `5.1-CODE-REVIEW.md` and `docs/THIRD-PARTY-LICENSES.md` exist and are complete.
- Shared suite remains green after any in-scope remediation: `./gradlew :shared:testDebugUnitTest`.
- Audit searches scoped to shared/src + androidApp/src, excluding .claude/worktrees.
</verification>

<success_criteria>
- Deep core-path audit with severity table and P0/P1/P2 closure status produced.
- Clean-room boundary and the known source-provenance P1 verified.
- ARCH-03/ARCH-04 + privacy documented; export-filename + bad-input handling assessed; traceability reconciled.
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-04-SUMMARY.md` when done. List any fix-required findings that remain open for gap closure.
</output>
