# 5.1 Field-Test Log — Closed-Course Session Evidence Index

**Phase:** 5.1 — MVP Field Validation and Hardening Gate
**Protocol:** see `5.1-UAT.md` (executable UAT). This file is the per-session evidence index it feeds.
**Status:** **PENDING** — no field evidence exists yet. Lack of devices / closed-course access leaves Phase 5.1 pending/blocked, NOT a product No-Go (D-54). Fill the rows during Plan 07 field execution.

**Minimum to clear the field gate:** **five complete valid closed-course sessions** (D-10), distributed **three on the primary Android phone + two on the secondary Android phone** (D-11). Android-only (D-02); emulator runs never count as field evidence (D-08).

**A session counts toward the five only if** the app showed **Ready before timing started** (D-13) AND all four evidence items below are preserved (D-12/D-47). Missing any evidence item disqualifies the session (T-5.1-09).

---

## Per-session evidence template (D-48 minimal field notes)

Each valid session records:

| Field | Meaning |
|-------|---------|
| **Device** | Device name/model + label (Primary / Secondary) + OS version (D-07). |
| **Session ID / export file** | Saved session identifier and the export file path (JSON; GPX where available) (SESS-04/SESS-05). |
| **Lap count pass/fail** | Engine lap count vs manual/video tally — must be 100% correct, the HARD blocker (D-19/D-20). |
| **Replay result** | `SessionReplayDecoder.decodeForReplay` → `Decoded`, deterministic across runs, lap count matches (D-25/D-28). |
| **Video reference / result** | Start/finish video file; lap-time vs readable crossing ≤ 0.5 s target; unreadable crossings excluded (D-21/D-23). |

**Optional extras (D-49)** — fill ONLY on failure/anomaly: weather, mount position, lighting, temperature, battery, GPS Ready wait time, abnormal events.

---

## Evidence index (5 rows reserved — fill during Plan 07)

### Session 1 — Primary phone

| Field | Value |
|-------|-------|
| Device (name/model · label · OS) | _pending_ — Primary |
| Session ID | _pending_ |
| JSON export path | _pending_ |
| GPX export path | _pending_ |
| Ready before timing? (≤25 m / ≤15 s / ≥1.0 Hz + course/start-finish/direction) | _pending_ |
| Wrong-course override used? (D-18 evidence visible in Review/export) | _pending_ |
| Lap count pass/fail (vs manual/video tally) | _pending_ |
| Replay result (Decoded · deterministic · lap-count match) | _pending_ |
| Video reference / result (≤0.5 s where readable) | _pending_ |
| Optional extras (only on failure/anomaly) | _n/a_ |

### Session 2 — Primary phone

| Field | Value |
|-------|-------|
| Device (name/model · label · OS) | _pending_ — Primary |
| Session ID | _pending_ |
| JSON export path | _pending_ |
| GPX export path | _pending_ |
| Ready before timing? (≤25 m / ≤15 s / ≥1.0 Hz + course/start-finish/direction) | _pending_ |
| Wrong-course override used? (D-18 evidence visible in Review/export) | _pending_ |
| Lap count pass/fail (vs manual/video tally) | _pending_ |
| Replay result (Decoded · deterministic · lap-count match) | _pending_ |
| Video reference / result (≤0.5 s where readable) | _pending_ |
| Optional extras (only on failure/anomaly) | _n/a_ |

### Session 3 — Primary phone

| Field | Value |
|-------|-------|
| Device (name/model · label · OS) | _pending_ — Primary |
| Session ID | _pending_ |
| JSON export path | _pending_ |
| GPX export path | _pending_ |
| Ready before timing? (≤25 m / ≤15 s / ≥1.0 Hz + course/start-finish/direction) | _pending_ |
| Wrong-course override used? (D-18 evidence visible in Review/export) | _pending_ |
| Lap count pass/fail (vs manual/video tally) | _pending_ |
| Replay result (Decoded · deterministic · lap-count match) | _pending_ |
| Video reference / result (≤0.5 s where readable) | _pending_ |
| Optional extras (only on failure/anomaly) | _n/a_ |

### Session 4 — Secondary phone

| Field | Value |
|-------|-------|
| Device (name/model · label · OS) | _pending_ — Secondary |
| Session ID | _pending_ |
| JSON export path | _pending_ |
| GPX export path | _pending_ |
| Ready before timing? (≤25 m / ≤15 s / ≥1.0 Hz + course/start-finish/direction) | _pending_ |
| Wrong-course override used? (D-18 evidence visible in Review/export) | _pending_ |
| Lap count pass/fail (vs manual/video tally) | _pending_ |
| Replay result (Decoded · deterministic · lap-count match) | _pending_ |
| Video reference / result (≤0.5 s where readable) | _pending_ |
| Optional extras (only on failure/anomaly) | _n/a_ |

### Session 5 — Secondary phone

| Field | Value |
|-------|-------|
| Device (name/model · label · OS) | _pending_ — Secondary |
| Session ID | _pending_ |
| JSON export path | _pending_ |
| GPX export path | _pending_ |
| Ready before timing? (≤25 m / ≤15 s / ≥1.0 Hz + course/start-finish/direction) | _pending_ |
| Wrong-course override used? (D-18 evidence visible in Review/export) | _pending_ |
| Lap count pass/fail (vs manual/video tally) | _pending_ |
| Replay result (Decoded · deterministic · lap-count match) | _pending_ |
| Video reference / result (≤0.5 s where readable) | _pending_ |
| Optional extras (only on failure/anomaly) | _n/a_ |

---

## Roll-up (fill after the five sessions exist)

| Metric | Result |
|--------|--------|
| Valid sessions (target ≥5: 3 primary + 2 secondary) | 0 / 5 — **pending** |
| Lap count 100% on every valid session (HARD blocker, D-19/D-20) | _pending_ |
| All exports re-replay deterministically via `SessionReplayDecoder` (D-25/D-28) | _pending_ |
| Lap-time ≤0.5 s vs readable video (target, D-21) | _pending_ |
| All five sessions carry export + replay + notes + video (D-12/D-47) | _pending_ |

**Each field export becomes a permanent replay fixture** that must reproduce identical output on later runs (D-25/D-28); reference the fixture path in the session row once added.

---

*Phase: 05.1-mvp-field-validation-and-hardening-gate · evidence-index skeleton authored by Plan 06 · filled during Plan 07.*
