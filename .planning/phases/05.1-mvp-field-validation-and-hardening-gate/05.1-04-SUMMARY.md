---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 04
subsystem: audit
tags: [code-audit, clean-room, licensing, privacy, traceability, go-nogo-evidence]

# Dependency graph
requires:
  - phase: 05.1-mvp-field-validation-and-hardening-gate
    plan: 03
    provides: aggregateReady gate + RawRecordingController + AppShell source-provenance fix (the P1 this audit verifies)
  - phase: 05.1-mvp-field-validation-and-hardening-gate
    plan: 02
    provides: FullPipelineDeterminismTest (replay-trustworthiness evidence cited by the audit)
provides:
  - 5.1-CODE-REVIEW.md — deep severity-tagged core-path audit with P0/P1/P2 closure status (core P0/P1/P2 CLEAR for Go, D-42)
  - docs/THIRD-PARTY-LICENSES.md — ARCH-03 dependency inventory + ARCH-04 clean-room attestation + local-GPS privacy note
  - REQUIREMENTS.md traceability reconciliation (PLAT-01/SAFE-03/ARCH-01/03/04 marked complete; PLAT-02 correctly pending)
affects: [05.1-08 go-nogo]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-CODE-REVIEW.md
    - docs/THIRD-PARTY-LICENSES.md
  modified: []

key-decisions:
  - "Okio imports in storage/export I/O files are NOT an ARCH-01 violation: Okio is a KMP I/O library and appears only in the persistence seam, never in the lap/ghost/session algorithm engine"
  - "Core-path P0/P1/P2 are CLEAR for the Go gate (D-42): zero open findings; the single confirmed P1 (source provenance) is verified fixed by Plan 03; remaining items are 4 P3/info backlog (D-44)"
  - "SAFE-03 met via persistent closed-course copy on dash/editor/settings rather than a one-time onboarding gate; marked complete with an optional first-run-acknowledgment backlog note"
  - "App's own license remains an open product decision (tracked risk, not a core-timing blocker since core is verified clean-room and dependency-clean)"

requirements-completed: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, SAFE-01, SAFE-02, SAFE-03, SAFE-04, GPS-04]

# Metrics
duration: 30min
completed: 2026-06-29
---

# Phase 5.1 Plan 04: Deep Core-Path Code Audit + License/Privacy Documentation Summary

**A deep, severity-tagged audit of every Phase 1-5 core path (`5.1-CODE-REVIEW.md`) concluding core-path P0/P1/P2 CLEAR for the Go gate (zero open findings, the one confirmed source-provenance P1 verified fixed by Plan 03, four P3/info backlog items), with the clean-room boundary verified (engine imports zero Compose/platform symbols; Okio only in the I/O seam), export-filename path traversal and bad-input-as-data confirmed mitigated, manual orientation confirmed; plus `docs/THIRD-PARTY-LICENSES.md` delivering the owed ARCH-03 dependency inventory + ARCH-04 GPL-not-copied clean-room attestation + local-GPS privacy note, and a REQUIREMENTS.md traceability reconciliation.**

## Performance

- **Duration:** ~30 min
- **Tasks:** 2
- **Files:** 2 created (audit + licenses doc); 0 production code changed
- **Suite:** `./gradlew :shared:testAndroidHostTest` -> BUILD SUCCESSFUL, 359 tests, 0 failures (re-confirmed at audit time; no code changed so it remains green)

## Accomplishments

- **Task 1 - `5.1-CODE-REVIEW.md`:** Audited all 12 core-path groups from the RESEARCH component table (architecture, lap engine, sector/crossing, replay, GPS Ready/quality/provider, raw-recording, session lifecycle/recovery, storage/migration, export, track profile/direction/wrong-course, ghost, live UI state, licensing/privacy). Defined a P0/P1/P2/P3 taxonomy bounded to core paths (D-42/D-43/D-44). Produced a findings table (F-01..F-04) with dispositions and an overall closure table. Verified via scoped greps (excluding `.claude/worktrees`): clean-room boundary (zero `import androidx/platform/android./java.` in algorithm files; Okio only in 3 I/O files), engine purity (no `Random`/clock/`System.` in `lap/`), no network/analytics in `commonMain`, no `doves`/`gpl` in `shared/src`, manual orientation (portrait-locked manifest + fixed `SCREEN_ORIENTATION_*`, never `SENSOR_*`), export-filename sanitization (whitelist + traversal strip, tested), and bad-input-as-data (SessionReplayDecoder `Corrupt`/`NoCourse`, WrongCoursePreflight `Unavailable`, ReadyGate `NotReady`). Confirmed the Plan 03 source-provenance P1 fix at `AppShell.kt:106-117`.
- **Task 2 - `docs/THIRD-PARTY-LICENSES.md`:** Inventoried the full dependency set from `libs.versions.toml`/`build.gradle.kts` with licenses (all Apache-2.0 except proprietary Play Services Location SDK and test-only JUnit EPL-1.0; none GPL). Wrote the ARCH-04 clean-room attestation citing the verified-clean grep, and the ARCH-03 local-GPS privacy note (local-first, no upload/analytics/accounts, user-controlled export only). Recorded the app's own undecided license as a tracked risk. The app-license risk and traceability reconciliation are recorded in `5.1-CODE-REVIEW.md` (front-loaded in Task 1).

## Task Commits

1. **Task 1: deep severity-tagged core-path code audit** - `7038d2c` (docs)
2. **Task 2: third-party license inventory + clean-room/privacy attestation** - `fc1b2cb` (docs)

## Decisions Made

- **Okio in I/O files is not an ARCH-01 violation.** The clean-room rule targets the *algorithm* engine; Okio (a KMP I/O library) appears only in `FileSessionStore`/`StoragePaths`/`LocalExportWriter` — the persistence seam — and never in `lap/ghost/session` decision logic. Recorded as P3/info (F-02), not a finding.
- **Core P0/P1/P2 CLEAR for Go (D-42).** No open P0/P1/P2; the confirmed P1 (F-01 source provenance) is verified fixed by Plan 03. No core architecture defect (no D-45/D-53 No-Go trigger from code audit). Four P3/info items backlogged (D-44).
- **SAFE-03 satisfied via persistent placement.** Closed-course/private-track copy is persistently visible (dash/editor/settings), met the safety-language non-negotiable; the literal "onboarding" wording is covered by persistent visibility, with an optional first-run-acknowledgment backlogged (F-04).
- **App license = tracked risk, not a blocker.** Core is clean-room and dependency-clean, so the undecided app license does not block the core-timing gate; it must be resolved before public distribution.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Stripped literal control characters from the audit markdown**
- **Found during:** Task 1 verification (the `<automated>` grep reported the file as binary).
- **Issue:** The draft embedded literal C0 control-character glyphs (illustrating the sanitized characters) which made git/grep treat `5.1-CODE-REVIEW.md` as a binary file, failing the automated check.
- **Fix:** Replaced the control-char illustration with the phrase "control characters" and stripped any residual C0/C1 bytes (preserving tab/newline). The file is now plain UTF-8 text and the automated check returns PRESENT.
- **Files modified:** `5.1-CODE-REVIEW.md`.
- **Committed in:** `7038d2c`.

**Total deviations:** 1 auto-fixed (blocking, doc-formatting only). No production code changed; the audit surfaced no open P0/P1/P2 requiring code remediation, so the suite stays green at 359/0.

## Fix-Required Findings Remaining Open

**None.** The audit found no open P0/P1/P2 in any core path. The four backlog items (F-02 Okio-in-I/O note, F-03 belt-and-suspenders `..` replacement, F-04 optional first-run safety gate, and the tracked app-license decision) are P3/info and do not block Go (D-44). Plan 05.1-08 (Go/No-Go) can cite this audit as the code-audit evidence layer with a PASS verdict.

## Known Stubs

None. Both artifacts are complete, evidence-backed documents.

## Threat Flags

None new. The plan's threat register is closed: T-5.1-03 (export path traversal) mitigated, T-5.1-04 (local-GPS privacy) mitigated, T-5.1-05 (GPL contamination) mitigated, T-5.1-SC (dependency installs) accepted (no new packages).

## Self-Check: PASSED

- Both created files verified present on disk (`5.1-CODE-REVIEW.md`, `docs/THIRD-PARTY-LICENSES.md`).
- Both task commits (`7038d2c`, `fc1b2cb`) verified in git history.
- Task 1 automated check returns PRESENT; Task 2 automated check returns PRESENT.
- Host suite green: 359 tests, 0 failures.

---
*Phase: 05.1-mvp-field-validation-and-hardening-gate*
*Completed: 2026-06-29*
