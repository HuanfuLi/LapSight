# 05.1-07 Summary — On-device + Mounted-display UAT

**Phase:** 05.1 — MVP Field Validation and Hardening Gate  
**Plan:** 07  
**Status:** Complete by owner acceptance  
**Date:** 2026-07-06

## Outcome

Android on-device and mounted-phone UAT is accepted for the Phase 5.1 gate.
The product owner reported no remaining blocking issues after the latest local
field-test build.

This summary records owner acceptance rather than fabricating the originally
planned five-row closed-course evidence package. Raw field artifacts, if kept,
remain local/debug evidence and are not committed in this repository.

## Evidence

- Automated host tests passed: `.\gradlew.bat :shared:testAndroidHostTest`
- Android debug build passed: `.\gradlew.bat :androidApp:assembleDebug`
- Latest workspace build installed on device `56031FDCH005PL` / Pixel 10 Pro.
- Installed package: `com.huanfuli.lapsight`
- Version: `versionCode=1`, `versionName=1.0`
- Install confirmation: `lastUpdateTime=2026-07-06 04:20:11`
- Launch smoke passed via `adb shell monkey -p com.huanfuli.lapsight 1`.
- Process remained alive after launch: `pid 32448`.
- Recent logcat smoke showed no `FATAL EXCEPTION`.

## UAT Scope Covered

- Android app installs and launches on the primary test device.
- Mounted-phone drive surface remained the active validation target.
- Recent hardening addressed field-test issues discovered during Phase 5.1:
  - Phone GPS remains real-source gated; simulated data does not silently stand
    in for Phone GPS.
  - Review actions are available from list rows without drilling into details.
  - Review action results use transient toast instead of layout-shifting status
    rows.
  - Review timestamps use the device local time zone.
  - Session-ended icon buttons have touch-safe separation.
  - Lap Focus landscape panels preserve visible lap time and delta.
  - Course map handedness was corrected.
  - Live map arrow is bounded to the map viewport.

## Residual Risks

- iOS real-device UAT remains explicitly unvalidated and does not block the
  Android phone MVP gate.
- The original five-session closed-course matrix is not archived in the repo as
  replay/video evidence. This is accepted by the product owner for this gate,
  but future timing-accuracy work should keep exported JSON/GPX/video evidence
  when validating external GNSS or higher-speed track behavior.

## Decision Input

Plan 07 feeds Plan 08 as owner-accepted Android on-device UAT with no known
P0/P1/P2 blocker remaining.
