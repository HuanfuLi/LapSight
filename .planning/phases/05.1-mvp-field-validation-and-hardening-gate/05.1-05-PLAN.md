---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 05
type: execute
wave: 2
depends_on: ["05.1-03"]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Theme.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Spacing.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt
  - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UI-REVIEW.md
autonomous: true
requirements: [SAFE-01, SAFE-02, PLAT-03, PLAT-04, PLAT-05]

must_haves:
  truths:
    - "Colors, typography, and spacing on the Drive dash and shell flow through MaterialTheme semantic tokens + a Spacing system, not hardcoded hex/sp/dp."
    - "Glance sizing no longer relies on a brittle if(compact)42.sp else 54.sp pair; it uses semantic typographic bounds that respect viewport scaling."
    - "The dash information hierarchy is unchanged (bounded hardening, not a redesign)."
    - "All six D-36 pillars score >= 3/4 against the UI-SPEC, with Visual Polish/Accessibility and Motion/Glance lifted from their failing baseline."
    - "Decision coverage: D-33, D-34, D-35, D-37, D-38, D-39 (no new safety-restriction mechanisms; UI audit grades readability/state-clarity not movement-restriction; closed-course positioning unchanged; each pillar >= 3/4 or it blocks Go; Domain Fit as a mounted lap-timing instrument; diagnostics must not visually dominate) govern this UI hardening + re-audit."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Theme.kt"
      provides: "Typography tokens (and color token wiring) mirroring App.kt color scheme"
      contains: "Typography"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Spacing.kt"
      provides: "Spacing scale object / CompositionLocal from the UI-SPEC (4dp multiples)"
      contains: "Spacing"
    - path: ".planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UI-REVIEW.md"
      provides: "Re-audit on the six D-36 pillars with all pillars >= 3/4"
      contains: "Domain Fit"
  key_links:
    - from: "ui/DriveScreen.kt"
      to: "ui/Theme.kt + ui/Spacing.kt"
      via: "MaterialTheme.colorScheme / MaterialTheme.typography / LocalSpacing"
      pattern: "MaterialTheme\\.(colorScheme|typography)"
---

<objective>
Lift the two failing D-36 pillars (Visual Polish/Accessibility 1/4, Motion/Glance Readability 2/4) to >= 3/4 with a bounded tokenization hardening, then re-audit all six pillars against the UI-SPEC and rewrite `5.1-UI-REVIEW.md`. Replace 30+ hardcoded hex colors and 100+ inline `.sp`/`.dp` on the Drive dash and shell with MaterialTheme semantic tokens + a Spacing system, and replace the brittle `if (compact) 42.sp else 54.sp` glance sizing with semantic typographic bounds.

Purpose: D-37 blocks Go on any pillar below 3/4. The current dash bypasses theming/accessibility systems (Visual Polish 1/4) and risks clipping on untested aspect ratios (Motion/Glance 2/4). This is a hardening fix, NOT a redesign - the dash information hierarchy must not change (D-38/D-39, Open Q4).
Output: `Theme.kt` + `Spacing.kt`, tokenized `App.kt`/`DriveScreen.kt`/`AppShell.kt`, and a re-scored `5.1-UI-REVIEW.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-UI-SPEC.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-RESEARCH.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-PATTERNS.md
@AGENTS.md

<interfaces>
- App.kt:57 — existing `MaterialTheme(colorScheme = if (useDarkTheme) lapsightDarkColors else lapsightLightColors) { AppShell(...) }` (extend with `typography =` and a Spacing provider).
- App.kt:77 — `lapsightDarkColors = darkColorScheme(background=Color(0xFF05070A), surface=Color(0xFF101722), primary=Color(0xFF62E3FF), ...)` (the ONLY token system today; mirror its shape for Typography + Spacing).
- UI-SPEC Spacing Scale (xs 4 / sm 8 / md 16 / lg 24 / xl 32 / 2xl 48 / 3xl 64), Typography (Body 16/Label 14/Heading 24/Display 56), Color roles (#101722 / #1E293B / #38BDF8 / #EF4444).
- DriveScreen.kt:314 `Color(0xFF101722)`, :767 metric font sizes, :998 DeltaTone, :1048-1049 GPS-state hex, :456 isCompactLandscape; AppShell.kt:324 `padding(20.dp)`, :330 `28.sp`.
- 5.1-UI-REVIEW.md baseline scores (Visual Polish 1/4, Motion/Glance 2/4) — the pillars to lift.
- Project memory: orientation-no-sensor-rotation (manual toggle only; never sensor).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Typography + Spacing token systems and wire them into MaterialTheme</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Theme.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Spacing.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt (existing lapsightDarkColors/lapsightLightColors + MaterialTheme block)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-UI-SPEC.md (Spacing Scale + Typography + Color tables - the source of truth for token values)
    - 05.1-PATTERNS.md section "UI tokenization + glance hardening"
  </read_first>
  <action>Create `ui/Theme.kt` defining a `Typography` instance whose semantic roles map to the UI-SPEC: `displayLarge` ~56.sp/600 (primary lap/elapsed readout), plus heading 24.sp/600, label 14.sp/600, body 16.sp/400 with the specified line heights; keep the existing `lapsightDarkColors`/`lapsightLightColors` as the colorScheme (move them here or keep in App.kt, but reference the UI-SPEC color roles). Create `ui/Spacing.kt` defining a `Spacing` data class (xs=4.dp, sm=8.dp, md=16.dp, lg=24.dp, xl=32.dp, xxl=48.dp, xxxl=64.dp) exposed via a `LocalSpacing` `staticCompositionLocalOf`. Wire both into `App.kt`'s `MaterialTheme` call: pass `typography = lapsightTypography` and wrap the content in `CompositionLocalProvider(LocalSpacing provides Spacing())`. Keep the dark racing theme as default. Do NOT change the dash layout or information hierarchy. Preserve the manual orientation toggle; introduce no sensor rotation.</action>
  <verify>
    <automated>./gradlew :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `:androidApp:assembleDebug` succeeds.
    - `Theme.kt` exports a Typography with displayLarge/heading/label/body roles matching UI-SPEC; `Spacing.kt` exports the 4dp-multiple scale via LocalSpacing.
    - App.kt MaterialTheme passes `typography =` and provides LocalSpacing.
  </acceptance_criteria>
  <done>A Typography + Spacing token system exists and is wired into MaterialTheme alongside the existing color scheme, with no layout change.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize DriveScreen + AppShell and harden glance sizing</name>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt (hardcoded hex at :314/:1048-1049, font sizes at :767, DeltaTone :998, isCompactLandscape :456, glance sizing if(compact)42.sp else 54.sp)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt (:324 padding(20.dp), :330 28.sp)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Theme.kt + Spacing.kt (the tokens from Task 1)
    - 5.1-UI-REVIEW.md Pillar 4 + Pillar 6 findings
  </read_first>
  <action>Replace hardcoded styling in `DriveScreen.kt` and `AppShell.kt` with tokens: route every `Color(0xFF...)` through `MaterialTheme.colorScheme` semantic roles (background/surface/primary/error and the GPS/Delta state colors), every inline `fontSize`/`.sp` through `MaterialTheme.typography` semantic roles (the primary lap/elapsed readout uses `displayLarge`), and every inline `.dp` padding/gap through `LocalSpacing.current` tokens. Replace the brittle `if (compact) 42.sp else 54.sp` glance sizing with a semantic typographic bound (use the display role + an `AutoSize`/constrained text approach so long lap times or extreme speeds do not clip on foldables/edge aspect ratios), keeping high contrast for sunlight visibility. Keep the `isCompactLandscape` branch behavior (do not redesign responsiveness in this plan) but source its spacing from tokens. Preserve the exact information hierarchy: Speed and Delta remain the dominant readouts; the Ready and raw-recording indicators added in Plan 03 keep their position. Do not change copy, layout order, or the manual orientation toggle.</action>
  <verify>
    <automated>./gradlew :androidApp:assembleDebug && ./gradlew :shared:testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `:androidApp:assembleDebug` succeeds and the shared suite stays green.
    - A search of DriveScreen.kt + AppShell.kt shows the count of inline `Color(0xFF` and bare `.sp`/`fontSize =` literals reduced to near-zero (remaining ones documented as intentional in the SUMMARY).
    - The `if (compact) 42.sp else 54.sp` pattern is removed in favor of a semantic/constrained sizing approach.
  </acceptance_criteria>
  <done>Dash and shell styling flow through tokens, glance sizing is clip-safe, and the information hierarchy is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Re-audit all six D-36 pillars and rewrite 5.1-UI-REVIEW.md</name>
  <files>.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UI-REVIEW.md</files>
  <read_first>
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UI-REVIEW.md (the baseline to replace - already on the D-36 pillar set)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-UI-SPEC.md (grade against this contract)
    - 05.1-CONTEXT.md D-36/D-37/D-38/D-39
    - the tokenized DriveScreen.kt / AppShell.kt / Theme.kt / Spacing.kt
  </read_first>
  <action>Re-score the six D-36 pillars (Domain Fit, Information Hierarchy, State Clarity, Motion/Glance Readability, Responsiveness/Device Fit, Visual Polish/Accessibility) against the UI-SPEC after tokenization, using a 1-4 rubric where each pillar must reach >= 3/4 (D-37). Cite the now-tokenized colors/typography/spacing as the basis for lifting Visual Polish/Accessibility from 1/4 and the semantic glance sizing for lifting Motion/Glance from 2/4. Rewrite `5.1-UI-REVIEW.md` with the new per-pillar scores, the evidence for each, and any residual issues. Note explicitly that mounted-display readability under daylight/low-light/motion (D-29/D-30/D-31) is confirmed separately by the Plan 07 manual display UAT; this code-based re-audit grades structure/contrast/tokenization. If any pillar still scores below 3/4 after the bounded fix, record it as a Hardening-Required blocker rather than forcing a passing score.</action>
  <verify>
    <automated>test -f .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UI-REVIEW.md && grep -qi 'Visual Polish' .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UI-REVIEW.md && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `5.1-UI-REVIEW.md` contains all six D-36 pillars with a 1-4 score and per-pillar evidence.
    - Visual Polish/Accessibility and Motion/Glance scores are justified by the tokenization + glance-sizing changes, or any still-failing pillar is flagged as a Hardening-Required blocker.
    - The review defers daylight/low-light/motion glance confirmation to the Plan 07 display UAT.
  </acceptance_criteria>
  <done>All six D-36 pillars are re-scored against the UI-SPEC with evidence; the two formerly-failing pillars are addressed or flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI tokenization is cosmetic; introduces no new untrusted input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-08 | Tampering (regression) | DriveScreen/AppShell tokenization | mitigate | Bounded token-only change; `:androidApp:assembleDebug` + shared suite must stay green; no information-hierarchy change. |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages; uses existing Compose Material3 typography/color APIs. |
</threat_model>

<verification>
- `./gradlew :androidApp:assembleDebug` succeeds and `./gradlew :shared:testDebugUnitTest` green after tokenization.
- `5.1-UI-REVIEW.md` shows all six pillars >= 3/4 or explicitly flags a remaining blocker.
- Manual orientation toggle preserved; no sensor rotation introduced.
</verification>

<success_criteria>
- Color/type/spacing tokenized on the dash + shell; glance sizing clip-safe.
- Information hierarchy unchanged (bounded hardening).
- Six D-36 pillars re-scored against the UI-SPEC with the two failing pillars lifted to >= 3/4 or flagged.
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-05-SUMMARY.md` when done. List any residual hardcoded values left intentionally and any pillar still below 3/4.
</output>
