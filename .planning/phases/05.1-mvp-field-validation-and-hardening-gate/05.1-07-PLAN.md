---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 07
type: execute
wave: 4
depends_on: ["05.1-02", "05.1-03", "05.1-04", "05.1-05", "05.1-06"]
files_modified:
  - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md
  - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md
autonomous: false
requirements: [PLAT-01, PLAT-03, PLAT-04, PLAT-05, GPS-01, GPS-02, LAP-02, LAP-05, SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, GHOST-01]

must_haves:
  truths:
    - "The Android UAT APK installs and the on-device smoke + mounted-display checks pass in portrait/landscape and daylight/low-light."
    - "Five valid closed-course sessions are completed (3 primary + 2 secondary phone), each started only after the app showed Ready."
    - "Every valid session has 100% correct lap count vs manual tally."
    - "The saved-session Review tab, track trace, reference-lap save, and JSON + GPX export are functionally exercised on device with concrete pass/fail."
    - "Each valid session's exported JSON replays through SessionReplayDecoder to identical, lap-count-correct output and is linked in the field-test log with its video reference."
    - "Decision coverage: D-09 (closed-course testing is a hard Go requirement; walking/bike loops do not replace it) is exercised by this field execution."
  artifacts:
    - path: ".planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md"
      provides: "Filled per-session evidence index with replay + video results"
      contains: "primary"
  key_links:
    - from: "5.1-FIELD-TEST-LOG.md"
      to: "session/SessionReplayDecoder.kt"
      via: "each field export replayed to confirm deterministic, lap-count-correct output"
      pattern: "replay"
---

<objective>
Execute the manual evidence the gate cannot automate: the on-device Android smoke + mounted-display UAT (including the Review-tab + JSON/GPX export smoke), and the five valid closed-course sessions (3 primary + 2 secondary phone), each gated behind the in-app Ready state, with full evidence (export + replay + notes + video) preserved and the field-test log filled. Replay every field export through `SessionReplayDecoder` to confirm deterministic, lap-count-correct output.

Purpose: Closed-course validation with 100% lap count is the hard field standard (D-09/D-19/D-20); mounted-display readability under real conditions confirms the UI pillars (D-29..D-32); the Review/export smoke functionally exercises the saved-session review, track trace, reference-lap save, and GPX/JSON export paths (SESS-02/SESS-03/SESS-05/GHOST-01) that are otherwise unexercised. Per D-54, if real devices or a closed course are unavailable, this plan pauses the phase as pending - it is not a product No-Go.
Output: filled `5.1-FIELD-TEST-LOG.md` and UAT results appended to `5.1-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-CONTEXT.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and install the Android UAT debug APK</name>
  <files>.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md</files>
  <read_first>
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md (build/install + smoke steps)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md (confirmed build command)
  </read_first>
  <action>Build the debug APK with `./gradlew :androidApp:assembleDebug`. If a device/emulator is reachable via adb, install it (`adb install -r` the produced APK) for the subsequent manual checks. If no device is reachable, note that in 5.1-UAT.md and proceed to the checkpoint, which will gate on device availability (D-54). This is the automatable preparation step before the manual checkpoints.</action>
  <verify>
    <automated>./gradlew :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `:androidApp:assembleDebug` exits 0 and the debug APK path is recorded.
    - Install attempted via adb (or device-absence noted for the D-54 gate).
  </acceptance_criteria>
  <done>A debug APK is built (and installed if a device is present) ready for on-device UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device smoke + mounted-display + Review/export UAT (portrait/landscape, daylight/low-light)</name>
  <read_first>
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md (on-device smoke, mounted-display, and Review + JSON/GPX export smoke steps + acceptance)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md (where on-device results/exports are referenced)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md (confirmed build command + SessionReplayDecoder entry point for the export re-replay check)
  </read_first>
  <action>Run the documented 5.1-UAT.md on-device smoke + mounted-display + Review/export checks on the installed debug APK; record pass/fail and device identity per check in 5.1-UAT.md. This is a manual verification of automated build output - if no device/closed course is available, record pending per D-54.</action>
  <what-built>The hardened Android app (Plan 03 Ready gate + raw recording + source fix; Plan 05 tokenized dash) built as a debug APK in Task 1.</what-built>
  <how-to-verify>
    1. Launch the app on the primary Android phone; grant precise location; confirm GPS fix + live speed/accuracy/update rate appear (GPS-01/GPS-02).
    2. Verify portrait and landscape are both usable with the same information hierarchy (PLAT-03/PLAT-04); confirm the dash stays visible during an active timing session (PLAT-05).
    3. Switch Settings between Phone GPS and Simulated; confirm the source is honestly labeled and simulated never stands in for a real selection.
    4. Mount the phone; in daylight AND low-light (D-30), confirm the core timing state is legible within a one-second glance (D-31/D-32): current lap/elapsed, delta or `--`, lap count/progress, last/best, GPS Ready/poor, raw-recording state, timing-active state.
    5. Confirm the app shows Ready only under good GPS/course/start-finish/direction conditions, and that when not Ready you can start raw recording without entering lap/ghost timing (D-13/D-16).
    6. Record a session (or use one of the closed-course sessions from Task 3), then open the Review tab: confirm the saved session lists with its lap list, best lap, total duration, and GPS quality summary (SESS-02), and that the simple track trace renders (SESS-03). Save a completed lap as the reference/best lap and confirm it is retained (GHOST-01).
    7. Export the session as JSON AND as GPX (SESS-05): confirm the GPX file is non-empty and re-imports/parses (well-formed track with points), and that the exported JSON re-replays through `SessionReplayDecoder` to identical, lap-count-correct output. Record pass/fail for each.
    8. Record pass/fail per check (and device identity) in 5.1-UAT.md. If no device/closed course is available, record the phase as pending (D-54) and stop here.
  </how-to-verify>
  <acceptance_criteria>
    - On-device smoke (launch, permission, GPS, source switch) passes on at least the primary phone.
    - Mounted-display one-second-glance legibility confirmed in portrait + landscape and daylight + low-light, or specific failures are recorded.
    - Ready/raw-recording behavior confirmed on device.
    - Review tab lists the saved session with lap list/best lap/duration/GPS quality (SESS-02) and renders its track trace (SESS-03); a reference/best lap can be saved (GHOST-01).
    - JSON + GPX export confirmed (SESS-05): GPX is non-empty and re-imports/parses; JSON re-replays to identical, lap-count-correct output.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded results, or describe issues (or "pending - no device/course" per D-54).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Five valid closed-course sessions with evidence + replay verification</name>
  <read_first>
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md (closed-course session matrix, Ready protocol, acceptance metrics, replay-diagnosis protocol)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md (the D-48 per-session rows to fill)
    - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md (SessionReplayDecoder entry point + confirmed Gradle test/build commands)
  </read_first>
  <action>Execute the five valid closed-course sessions per 5.1-UAT.md (3 primary + 2 secondary phone), each started only after the app shows Ready; preserve export + video + manual tally; replay each export through SessionReplayDecoder; fill 5.1-FIELD-TEST-LOG.md. Record pending per D-54 if closed-course access is unavailable.</action>
  <what-built>The Ready-gated timing pipeline + SessionReplayDecoder replay loop, validated end-to-end on a closed course.</what-built>
  <how-to-verify>
    1. On a closed course, run five valid sessions: three on the primary phone, two on the secondary phone (D-10/D-11). Start formal timing ONLY after the app shows Ready (D-13); raw-recording diagnostics do not count (D-17).
    2. For each session capture: the exported session JSON, a fixed start/finish video, and a manual lap tally as independent ground truth (D-12/D-22/D-47).
    3. Confirm 100% correct lap count per session vs the manual tally (D-19) - any wrong lap count is a No-Go for that run and must be fixed and rerun (D-20/D-53).
    4. Replay each exported JSON through `SessionReplayDecoder` (the Plan 01 decoder) and confirm the replay reproduces identical, lap-count-correct output (D-25/D-28); for any anomaly, diagnose via replay before assigning root cause (D-28).
    5. Where the video crossing is clearly readable, compare engine lap time to the video-derived crossing (target within 0.5s, D-21); exclude unreadable crossings (D-23).
    6. Fill all five rows of 5.1-FIELD-TEST-LOG.md using the D-48 template, including each session's JSON + GPX export reference (SESS-05); commit the exports as permanent replay fixtures. If closed-course access is unavailable, record the phase as pending per D-54.
  </how-to-verify>
  <acceptance_criteria>
    - Five valid sessions recorded (3 primary + 2 secondary), each started after Ready, each at 100% lap count vs manual tally.
    - Each session's export replays to identical, lap-count-correct output via SessionReplayDecoder and is linked in the field-test log with its video reference.
    - 5.1-FIELD-TEST-LOG.md is fully populated (including JSON/GPX export references), OR the phase is explicitly marked pending (D-54) with the reason.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the filled field-test log, or "pending - no closed course" (D-54), or describe lap-count/replay failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live GPS on closed course → timing/evidence | Real-world GPS crosses into the timing pipeline and becomes Go/No-Go evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-10 | Spoofing (evidence integrity) | valid-session counting (D-13/D-17) | mitigate | Only Ready-gated sessions count; raw recordings explicitly excluded; field exports verified to carry the live PhoneGps source. |
| T-5.1-11 | Repudiation | lap-count ground truth (D-19/D-22) | mitigate | Independent manual tally + video, not the app, are ground truth; replay reproduces the result deterministically. |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages. |
</threat_model>

<verification>
- `:androidApp:assembleDebug` green; APK installed on at least the primary phone (or device-absence recorded per D-54).
- Review/export smoke confirmed: Review tab + track trace + reference-lap save + non-empty GPX + JSON re-replay (or specific failures recorded).
- Five valid Ready-gated sessions at 100% lap count, each replay-verified and logged, OR phase explicitly pending (D-54).
</verification>

<success_criteria>
- On-device + mounted-display UAT confirmed in portrait/landscape and daylight/low-light.
- Review tab, track trace, reference-lap save, and JSON + GPX export functionally confirmed on device.
- Five valid closed-course sessions (3+2) at 100% lap count with complete evidence + deterministic replay, or a documented pending status (D-54).
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-07-SUMMARY.md` when done. Record session count, lap-count results, replay results, Review/export smoke results, and any pending status.
</output>
