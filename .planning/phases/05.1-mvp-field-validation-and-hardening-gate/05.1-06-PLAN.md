---
phase: 05.1-mvp-field-validation-and-hardening-gate
plan: 06
type: execute
wave: 2
depends_on: ["05.1-01", "05.1-03"]
files_modified:
  - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md
  - .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md
autonomous: true
requirements: [PLAT-01, PLAT-03, PLAT-04, PLAT-05, GPS-01, GPS-02, SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, GHOST-01]

must_haves:
  truths:
    - "5.1-UAT.md defines the Android-only on-device + mounted-display + closed-course protocol with the Ready-before-timing rule, the 3+2 session distribution, and per-session evidence requirements."
    - "5.1-UAT.md records the chosen conservative Ready thresholds (accuracy/age/rate) from Plan 03."
    - "5.1-UAT.md documents the replay-diagnosis protocol: every field anomaly is diagnosed via SessionReplayDecoder before assigning root cause."
    - "5.1-UAT.md defines a Review-tab smoke check (session list/best lap/duration/GPS quality + track trace + reference-lap save) and a JSON AND GPX export smoke check with concrete acceptance criteria."
    - "5.1-FIELD-TEST-LOG.md provides a per-session evidence index using the minimal field-note template (device, session id/export, lap-count pass/fail, replay result, video reference)."
    - "Decision coverage: D-02, D-03, D-05, D-06, D-07, D-08, D-10, D-11, D-12, D-19, D-20, D-21, D-22, D-23, D-29, D-30, D-31, D-32, D-47, D-48, D-49 (Android-only scope; no extra devices; two-phone matrix with recorded identity; emulators only for layout smoke; five valid sessions distributed 3+2; per-session evidence; lap-count 100% hard standard and incorrect-count No-Go; <=0.5s video target with unreadable-crossing exclusion; portrait/landscape and daylight/low-light display UAT; one-second-glance core state; field-note template and optional extras) are encoded in this UAT/field-log authoring."
  artifacts:
    - path: ".planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md"
      provides: "UAT plan: smoke paths, Ready protocol, thresholds, session matrix, Review/export smoke, evidence + replay-diagnosis rules"
      contains: "Ready"
    - path: ".planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md"
      provides: "Per-session evidence index skeleton (5 rows: 3 primary + 2 secondary)"
      contains: "lap count"
  key_links:
    - from: "5.1-UAT.md"
      to: "session/SessionReplayDecoder.kt"
      via: "replay-each-field-export-to-confirm-deterministic-and-lap-count protocol"
      pattern: "decode"
---

<objective>
Author the two evidence-plan artifacts (the autonomous portion of the field UAT): `5.1-UAT.md` (the executable UAT protocol) and the `5.1-FIELD-TEST-LOG.md` skeleton index. Encode the Android-only scope (D-02), the Ready-before-timing rule (D-13), the chosen conservative Ready thresholds from Plan 03 (D-14 discretion), the five-valid-session matrix (3 primary + 2 secondary, D-10/D-11), per-session evidence requirements (export + replay + notes + video, D-12/D-47), the minimal field-note template (D-48), the lap-count 100% hard blocker (D-19/D-20), the 0.5s video target (D-21/D-22/D-23), the replay-diagnosis-first protocol (D-28), and a Review-tab + JSON/GPX export smoke check (SESS-02/SESS-03/SESS-05/GHOST-01).

Purpose: The field execution plan (Plan 07) needs a precise, device-independent protocol authored before anyone is on a closed course. This plan completes everything automatable about UAT, so Plan 07 only carries the irreducible manual evidence (per D-54). The Review/export smoke ensures the saved-session review, track trace, reference-lap save, and GPX/JSON export paths are functionally exercised, not just the live timing path.
Output: `5.1-UAT.md` + `5.1-FIELD-TEST-LOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-CONTEXT.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-VALIDATION.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-RESEARCH.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-03-SUMMARY.md
@.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-01-SUMMARY.md

<interfaces>
- VALIDATION.md "Manual-Only Verifications" table (the irreducible manual evidence items).
- Plan 03 SUMMARY: the chosen Ready thresholds (accuracy/age/rate) to transcribe.
- Plan 01 SUMMARY: the confirmed Gradle build command for the UAT APK + the SessionReplayDecoder entry point for replaying field exports.
- D-48 minimal field-note template: device; session id/export file; lap count pass/fail; replay output/result; video reference/result.
- D-49 optional extras (weather/mount/lighting/temperature/battery/Ready-wait/abnormal) - add only on failure/anomaly.
- REQUIREMENTS.md: SESS-02 (review saved session: lap list/best lap/duration/GPS quality), SESS-03 (track trace view), SESS-05 (export GPX or common interchange), GHOST-01 (save completed lap as reference/best lap).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 5.1-UAT.md (on-device + mounted-display + closed-course + Review/export protocol)</name>
  <files>.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md</files>
  <read_first>
    - 05.1-CONTEXT.md D-02, D-05..D-32, D-46..D-49
    - 05.1-VALIDATION.md "Manual-Only Verifications" + "Sampling Rate"
    - 05.1-03-SUMMARY.md (Ready thresholds) and 05.1-01-SUMMARY.md (build + decoder commands)
    - .planning/REQUIREMENTS.md (SESS-02/SESS-03/SESS-05/GHOST-01 exact wording)
  </read_first>
  <action>Write `5.1-UAT.md` containing: (1) Scope - Android-only (D-02); two real phones with recorded device identity (D-05/D-06/D-07); emulators allowed for layout smoke only, never core evidence (D-08). (2) Build/install - the confirmed androidApp assembleDebug command and adb install. (3) On-device smoke paths - launch, permission + GPS fix (GPS-01), live speed/accuracy/rate (GPS-02), portrait + landscape (PLAT-03/04), keep-dash-visible during timing (PLAT-05), Settings Phone GPS vs Simulated source switch with honest labeling. (4) Mounted-display UAT - portrait + landscape with equal strictness (D-29), daylight + low-light (D-30), one-second-glance core state legibility (D-31/D-32 list: current lap/elapsed, delta or `--`, lap count/progress, last/best, GPS Ready/poor, raw-recording state, timing-active state). (5) Ready protocol - timing counts only if the app showed Ready before start (D-13); transcribe the chosen conservative thresholds (accuracy/age/rate) from Plan 03 (D-14); raw recording allowed when not Ready and excluded from valid sessions (D-16/D-17); wrong-course override recorded as reviewable evidence (D-18). (6) Closed-course session matrix - five valid sessions, three on primary + two on secondary phone (D-10/D-11); per session preserve export + replay output + field notes + start/finish video (D-12/D-47). (7) Acceptance metrics - lap count 100% correct is the hard blocker (D-19/D-20); lap time within 0.5s vs readable video crossing is a target, unreadable crossings excluded (D-21/D-22/D-23). (8) Replay-diagnosis protocol - every field failure/anomaly is decoded and replayed through `SessionReplayDecoder` before assigning root cause among data quality / line placement / interpolation / Ready classification / course selection / implementation logic (D-28); each field export becomes a permanent replay fixture that must reproduce identical output (D-25). (9) Review + export smoke - after recording a valid session, open the Review tab and confirm it lists the saved session with its lap list, best lap, total duration, and GPS quality summary (SESS-02); confirm the simple track trace renders for that session (SESS-03); save a completed lap as the reference/best lap and confirm it is retained (GHOST-01); export the session as JSON AND as GPX (SESS-05) and confirm: the GPX file is non-empty and re-imports/parses (well-formed track with points), and the exported JSON re-replays through `SessionReplayDecoder` to identical, lap-count-correct output. Give concrete pass/fail acceptance for each of these (e.g. "Review tab lists the saved session; GPX export is a non-empty `.gpx` that re-imports/parses; JSON export re-replays to identical output"). State that lack of devices/closed-course leaves the phase pending, not No-Go (D-54).</action>
  <verify>
    <automated>test -f .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md && grep -qi 'ready' .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md && grep -qi 'lap count' .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md && grep -qi 'gpx' .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-UAT.md && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `5.1-UAT.md` exists and covers scope, build/install, on-device smoke, mounted-display UAT, Ready protocol with transcribed thresholds, the 3+2 session matrix, acceptance metrics, the replay-diagnosis protocol, and the Review + JSON/GPX export smoke.
    - The Ready-before-timing rule and the lap-count-100% hard blocker are stated explicitly.
    - The Review-tab smoke (SESS-02/SESS-03), reference-lap save (GHOST-01), and JSON + GPX export smoke (SESS-05) have concrete pass/fail acceptance, including a non-empty GPX that re-imports/parses and JSON that re-replays to identical output.
    - The D-54 pending-not-No-Go clause is present.
  </acceptance_criteria>
  <done>5.1-UAT.md is a complete, device-independent UAT protocol (live timing + Review/export smoke) ready for Plan 07 execution.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5.1-FIELD-TEST-LOG.md evidence-index skeleton</name>
  <files>.planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md</files>
  <read_first>
    - 05.1-CONTEXT.md D-10/D-11/D-12/D-47/D-48/D-49
    - 05.1-VALIDATION.md (field exports become permanent replay fixtures)
  </read_first>
  <action>Create `5.1-FIELD-TEST-LOG.md` as a per-session evidence index with five rows reserved for the five valid sessions (three labeled primary phone, two labeled secondary phone, D-11). Each row uses the minimal field-note template (D-48): device name/model, session id / export file path, lap count pass/fail (vs manual tally, D-19), replay output/result (deterministic + lap-count match via SessionReplayDecoder), and video reference / result (vs <= 0.5s target where readable, D-21/D-23). Add an optional-extras column note (D-49: weather/mount/lighting/temperature/battery/Ready-wait/abnormal - filled only on failure/anomaly). Add a column or note for the per-session JSON + GPX export references (SESS-05). Leave the rows as a blank template to be filled during Plan 07 field execution; add a header stating the five-valid-session minimum and the D-54 pending status until evidence exists.</action>
  <verify>
    <automated>test -f .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md && grep -qi 'lap count' .planning/phases/05.1-mvp-field-validation-and-hardening-gate/5.1-FIELD-TEST-LOG.md && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `5.1-FIELD-TEST-LOG.md` exists with five reserved rows (3 primary + 2 secondary) using the D-48 template columns plus a JSON/GPX export reference.
    - The header states the five-valid-session minimum and the pending status until field evidence exists.
  </acceptance_criteria>
  <done>A minimal, ready-to-fill field-test evidence index exists matching the D-48 template and the 3+2 distribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| field export → permanent replay fixture | Field-captured GPS bytes become long-lived validation inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-09 | Repudiation | field evidence index (D-12/D-48) | mitigate | Each valid session must link export + replay result + video + notes; missing evidence disqualifies the session from the five. |
| T-5.1-SC | Tampering | dependency installs | accept | No new packages; documentation only. |
</threat_model>

<verification>
- Both artifacts exist and contain the required protocol and template fields.
- 5.1-UAT.md transcribes the Plan 03 Ready thresholds, references the SessionReplayDecoder replay-diagnosis loop, and includes the Review + JSON/GPX export smoke with concrete acceptance.
</verification>

<success_criteria>
- 5.1-UAT.md is a complete Android-only UAT protocol (smoke + display + closed-course + Ready + Review/export + replay-diagnosis).
- 5.1-FIELD-TEST-LOG.md provides the 3+2 evidence-index skeleton on the D-48 template.
</success_criteria>

<output>
Create `.planning/phases/05.1-mvp-field-validation-and-hardening-gate/05.1-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
