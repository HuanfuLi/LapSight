---
phase: 03-local-sessions-review-and-export
plan: 2
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [SESS-01, SESS-02, SESS-03, SESS-04, SESS-05]
must_haves:
  truths:
    - "No new Maven dependency or Gradle plugin is added until the user verifies the package coordinates."
    - "The dependency gate covers serialization, Okio, and Material Icons because all are new Maven artifacts for Phase 3."
  artifacts:
    - path: ".planning/phases/03-local-sessions-review-and-export/03-02-SUMMARY.md"
      provides: "Human package verification outcome for downstream dependency changes"
  key_links:
    - from: ".planning/phases/03-local-sessions-review-and-export/03-RESEARCH.md"
      to: "gradle/libs.versions.toml"
      via: "approved coordinates before dependency edits"
      pattern: "kotlinx-serialization|okio|material-icons-core"
---

<objective>
Block dependency installation until the user verifies the Maven and Gradle Plugin Portal coordinates flagged by research/slopcheck.

Purpose: Phase 3 storage/export/UI work requires new artifacts, but the research package legitimacy audit marked recommended Maven coordinates as SUS. This plan satisfies the dependency checkpoint requirement before any later plan edits Gradle files.
Output: A human-verified dependency approval record in the plan summary; no source files are modified by this plan.
</objective>

## Phase Goal

**As a** track user, **I want to** save and review sessions after driving/riding, **so that** I can inspect laps and reuse the data for debugging or future analysis.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-local-sessions-review-and-export/03-CONTEXT.md
@.planning/phases/03-local-sessions-review-and-export/03-RESEARCH.md
@.planning/phases/03-local-sessions-review-and-export/03-UI-SPEC.md
@gradle/libs.versions.toml
@build.gradle.kts
@shared/build.gradle.kts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify new Maven and Gradle package coordinates</name>
  <read_first>
    @.planning/phases/03-local-sessions-review-and-export/03-RESEARCH.md
    @.planning/phases/03-local-sessions-review-and-export/03-UI-SPEC.md
    @gradle/libs.versions.toml
    @build.gradle.kts
    @shared/build.gradle.kts
  </read_first>
  <files>
    None
  </files>
  <action>Before any Gradle file is edited, ask the user to verify these exact coordinates and versions in official registries: `org.jetbrains.kotlin.plugin.serialization:org.jetbrains.kotlin.plugin.serialization.gradle.plugin:2.4.0`, `org.jetbrains.kotlinx:kotlinx-serialization-json:1.11.0`, `com.squareup.okio:okio:3.17.0`, and `org.jetbrains.compose.material:material-icons-core:1.11.1`. Explain that the first three were recommended by 03-RESEARCH.md but slopcheck marked them `SUS`; the fourth is required by 03-UI-SPEC.md for `NavigationBarItem` icons and is also a new Maven artifact. Continue only after the user replies with approval for these coordinates or gives replacement coordinates.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$txt = Get-Content -Raw gradle/libs.versions.toml; if ($txt -match 'kotlinx-serialization|okio|material-icons-core|kotlinSerialization') { exit 1 } else { exit 0 }"</automated>
    <human-check>Verify the package pages or official docs for all four coordinates, then reply `approved` or provide corrected coordinates.</human-check>
  </verify>
  <acceptance_criteria>
    - Gradle files remain unchanged during this checkpoint.
    - User explicitly approves or replaces every coordinate before Plan 03-03 can add dependencies.
    - The summary records the final approved coordinates and any user-supplied replacements.
  </acceptance_criteria>
  <done>Dependency gate is satisfied and downstream plans may add only the approved coordinates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External registry -> Gradle dependency graph | New Maven/Gradle artifacts would execute in build/runtime contexts after approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Gradle dependency graph | mitigate | Require blocking human verification before adding any new Maven dependency or plugin. |
| T-03-SC | Tampering | Maven/Gradle Plugin Portal installs | mitigate | Human verifies all SUS/new coordinates before Plan 03-03 edits `gradle/libs.versions.toml`, `build.gradle.kts`, or `shared/build.gradle.kts`; `[SLOP]` packages remain forbidden. |
</threat_model>

<verification>
This is a blocking human package verification plan. The automated check must confirm Gradle files have not already been edited with the new artifacts before approval.
</verification>

<success_criteria>
- The user has approved or corrected all new dependency coordinates.
- No package is added silently.
- Later plans reference only approved coordinates.
</success_criteria>

<output>
Create `.planning/phases/03-local-sessions-review-and-export/03-02-SUMMARY.md` when done, including the exact approved coordinates.
</output>
