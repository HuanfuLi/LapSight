---
phase: 03-local-sessions-review-and-export
plan: 5
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
autonomous: true
requirements: [SESS-02, SESS-03]
must_haves:
  truths:
    - "User can mark a track from the normal Demo GPS feed, inspect Track Review, and explicitly Save, Re-record, or Discard."
    - "The app has Drive / Review / Settings bottom navigation, and Track Review never shows lap times for marking samples."
    - "Saved Demo tracks appear in Review with a visible DEMO badge and source metadata; discarded markings stay out of Review history."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt"
      provides: "Drive / Review / Settings bottom navigation shell"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      provides: "Mark New Track capture and Track Review save/re-record/discard surface"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt"
      provides: "Review list with saved Track/marking rows and DEMO badges"
  key_links:
    - from: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      to: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ReferenceLineExtractor.kt"
      via: "Mark New Track capture completion runs extraction"
      pattern: "ReferenceLineExtractor"
    - from: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      to: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt"
      via: "Save Track writes Track plus source TrackMarkingSession"
      pattern: "LocalSessionStore"
---

<objective>
Wire the user-facing track-marking slice: refactor the dash into a three-tab shell, let the user Mark New Track from the Demo feed, inspect the extracted Track in Track Review, and explicitly Save / Re-record / Discard so a saved Demo Track appears in Review.

Purpose: This delivers the first saved local artifact a user can see end-to-end, and a saved Track with a confirmed start/finish is the precondition for formal timing (D-19). It consumes the storage layer (Plan 03-03) and the extraction domain (Plan 03-04) without re-implementing either.
Output: `AppShell` three-tab navigation, `DriveScreen` Mark New Track + Track Review flow, `ReviewScreen` saved-Track list, and `App.kt` refactor.
</objective>

## Phase Goal

**As a** track user, **I want to** save and review sessions after driving/riding, **so that** I can inspect laps and reuse the data for debugging or future analysis.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-local-sessions-review-and-export/03-CONTEXT.md
@.planning/phases/03-local-sessions-review-and-export/03-RESEARCH.md
@.planning/phases/03-local-sessions-review-and-export/03-PATTERNS.md
@.planning/phases/03-local-sessions-review-and-export/03-UI-SPEC.md
@.planning/phases/03-local-sessions-review-and-export/03-03-SUMMARY.md
@.planning/phases/03-local-sessions-review-and-export/03-04-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/SimulatedGpsProvider.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackReviewState.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
</context>

<interfaces>
Consume `TrackReviewState` and `ReferenceLineExtractor` (Plan 03-04), `LocalSessionStore`/`ReviewIndex` (Plan 03-03), and `SimulatedGpsProvider` (Plan 03-01). Do not add Compose Material Icons Core to Gradle — the coordinate was added by Plan 03-03; just import `Icons.Filled.*`.
Track Review must separate marking from lap timing and never show lap times for marking samples. `Start Timing` stays blocked with `Mark a track first. Timing needs a saved start/finish line.` until a saved Track has a confirmed start/finish (the timing flow itself is Plan 03-06).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor App into three-tab shell and wire Mark New Track capture</name>
  <read_first>
    @.planning/phases/03-local-sessions-review-and-export/03-UI-SPEC.md
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/SimulatedGpsProvider.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackReviewState.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ReferenceLineExtractor.kt
  </read_first>
  <files>
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
  </files>
  <action>Refactor `App.kt` into `AppShell` with three Material3 `NavigationBarItem`s: Drive using `Icons.Filled.Speed`, Review using `Icons.Filled.History`, and Settings using `Icons.Filled.Settings`, preserving the existing dark theme per D-26 and 03-UI-SPEC.md. Build `DriveScreen` so `Mark New Track` captures continuous samples from `SimulatedGpsProvider`, stops into a `TrackReviewState`, and runs `ReferenceLineExtractor` per D-06 through D-11. Preserve the explicit orientation toggle and the existing fullscreen mounted-phone mode that hides bottom navigation per D-29 and the no-sensor-rotation rule. Keep `Start Timing` blocked with the exact 03-UI-SPEC copy until a saved Track exists (D-19); the timing run itself arrives in Plan 03-06.</action>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - The app has Drive, Review, and Settings tabs with icons and the existing dark theme.
    - `Mark New Track` uses the provider feed and does not require starting exactly at start/finish.
    - Capture completion produces a `TrackReviewState` via `ReferenceLineExtractor`.
    - `Start Timing` remains blocked until a saved Track has a confirmed start/finish line.
  </acceptance_criteria>
  <done>The three-tab shell exists and Mark New Track captures a continuous marking run into Track Review state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Track Review save flow and Review list rows</name>
  <read_first>
    @.planning/phases/03-local-sessions-review-and-export/03-UI-SPEC.md
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackReviewState.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
  </read_first>
  <files>
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
  </files>
  <action>In `DriveScreen`, render Track Review from `TrackReviewState`: reference readiness, GPS/capture quality summary, start/finish and sector editing state, and `Save Track`, `Re-record`, and `Discard` actions per D-06 through D-12 and D-31, using the exact 03-UI-SPEC ready/not-ready copy. `Save Track` writes `Track` plus the source `TrackMarkingSession` through `LocalSessionStore`; `Discard` keeps the marking out of Review history; `Re-record` returns to capture. Track Review must never show lap times. Build `ReviewScreen` list rows for saved Tracks/marking entries with visible `DEMO` badges and row-to-detail navigation per D-27, D-28, and D-42, plus the `No saved sessions yet` empty state from 03-UI-SPEC.md.</action>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Track Review separates marking from lap timing and offers Save/Re-record/Discard.
    - Saved Demo tracks appear in Review with a visible `DEMO` badge and source metadata.
    - Discarded markings do not appear in Review history.
    - Review list shows the empty state when no tracks are saved.
  </acceptance_criteria>
  <done>User can create a saved Track from a simulated marking run, see it in Review, and keep discarded markings out of formal history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> track marking controller | User starts/stops marking and chooses Save/Re-record/Discard. |
| Saved tracks -> Review rows | Demo/simulated provenance must remain visible in the saved list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Integrity | Demo review rows | mitigate | Persist and render `LocationSource.Simulated`/`DEMO` badges on saved tracks and marking payloads so simulated data never appears as live history. |
| T-03-25 | Repudiation | Save/Discard track actions | mitigate | Require explicit Save/Re-record/Discard; only Save writes a Review row, and Discard keeps markings out of history. |
| T-03-SC | Tampering | Maven/Gradle dependencies | mitigate | This plan adds no new dependencies; all approved coordinates were added by Plan 03-03. |
</threat_model>

<verification>
Run `.\gradlew.bat :shared:testAndroidHostTest` after each task. Run `.\gradlew.bat :shared:check` and `.\gradlew.bat :androidApp:assembleDebug` before completing the plan.
</verification>

<success_criteria>
- D-06 through D-12, D-19, D-26 through D-28, D-29, D-31, D-42, and D-44 are implemented for the track-marking UI slice.
- Review history has saved Track entries backed by canonical app-private JSON payloads and an index (from Plan 03-03).
- Track Review never displays marking lap times, and discarded markings stay out of Review history.
- No timing recorder, trace renderer, export, real GPS provider, map tiles, database schema push, cloud sync, external GNSS, or glasses code is added (those belong to other plans).
</success_criteria>

<output>
Create `.planning/phases/03-local-sessions-review-and-export/03-05-SUMMARY.md` when done.
</output>
