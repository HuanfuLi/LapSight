---
phase: 05-track-setup-and-course-profiles
plan: 6
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/TraceProjection.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/TrackEditorScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
autonomous: true
requirements: [SC-02]
must_haves:
  truths:
    - "SC-02 / D-05: A user edits confirmed start/finish and Sector layout on the offline trace and saves it."
    - "D-12/D-13/D-14: Saving a geometry edit appends an immutable revision and exposes version history; prior geometry is unchanged."
    - "D-10: Drag forwards only a candidate progress; rendering after rotation/aspect changes derives from canonical data."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/TrackEditorScreen.kt"
      provides: "Offline trace editing surface with constrained drag"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/TraceProjection.kt"
      provides: "Invertible screen<->local viewport transform"
  key_links:
    - from: "TrackEditorScreen.kt"
      to: "CourseProfileEditor.kt"
      via: "screen-to-local pointer conversion and progress-only drag events"
      pattern: "detectDragGestures"
    - from: "TrackProfileController.kt"
      to: "saveProfile/appendRevision"
      via: "validated immutable geometry save"
      pattern: "appendRevision"
---

<objective>
Provide the offline Track-editor surface and save validated edits as immutable revisions via the existing repository operations.

Purpose: Deliver the complete course-setup interaction without map tiles or platform geometry, preserving prior revisions.
Output: An invertible viewport transform, a constrained Compose editor, revision-save wiring, and immediate version history.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-05-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/TraceView.kt
</context>

<interfaces>
Use `TrackProfileController.appendRevision(...)`/`saveProfile` only after full editor validation. Compose converts pointer coordinates through the invertible viewport and forwards a candidate progress; the pure editor snaps and validates. Editor gestures never appear during active/fullscreen Timing.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the invertible viewport transform and constrained editor surface</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/TraceProjection.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/TraceView.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileEditor.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/TraceProjection.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/TrackEditorScreen.kt</files>
  <action>Refactor `TraceProjection` into a reusable viewport object exposing both geo/local-to-screen and screen-to-local conversion while preserving common bounding-box projection. Build `TrackEditorScreen` with `pointerInput`/`detectDragGestures` that converts pointer positions to local candidates and forwards candidate progress to the pure `CourseProfileEditor`; render start/finish, generated boundaries, and full interval coverage using the existing canvas style. Persist no endpoint or canvas coordinates. Keep the editor unavailable from the moving fullscreen dash and retain closed-course/private-track safety language.</action>
  <acceptance_criteria>
    - Drag forwards only candidate progress; no pixels/Offset are persisted.
    - Rendering after rotation/aspect change derives from canonical geographic/progress data.
    - Editor controls are absent during active/fullscreen Timing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProfileEditorTest*" --tests "*ClosedReferencePathTest*"</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>The offline editor renders the trace and forwards constrained drag candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Save edits as immutable revisions with version history</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/TrackEditorScreen.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt</files>
  <action>Expose editing from Track detail; keep start/finish confirmation mandatory. On valid save, load the profile aggregate and call `TrackProfileController.appendRevision`/`saveProfile` to append an immutable revision per D-12 through D-14, then show updated revision history immediately. Reject invalid/partial edits without writing. Never overwrite prior revision geometry.</action>
  <acceptance_criteria>
    - A user can edit start/finish and 2/3/6-Sector layouts and save a new revision.
    - The prior revision remains loadable and unchanged.
    - Invalid/partial edits do not append a revision.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProfileEditorTest*" --tests "*TrackRevisionStoreTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>The offline Track detail provides a usable edit-save-history flow that preserves prior geometry.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer input -> editor state | Arbitrary screen positions must not become persisted coordinates. |
| Editor save -> immutable history | Invalid or partial revisions must not replace history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Tampering | revision append | HIGH | mitigate | Validate complete setup in the pure editor, then atomically append; never overwrite prior revision. Proven by `TrackRevisionStoreTest`/`CourseProfileEditorTest` append cases. |
| T-05-SC | Tampering | package installs | LOW | accept | Existing Compose gesture APIs are reused; no install occurs. |
</threat_model>

<verification>
Run editor/revision tests, `:shared:check`, and Android debug build, then manually inspect 2/3/6-Sector drag behavior and start/finish confirmation on device.
</verification>

<success_criteria>
- D-05 and D-10 editing is observable; D-12 through D-14 protect prior geometry.
- No canvas coordinates are persisted; rendering derives from canonical data.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-06-SUMMARY.md` when done.
</output>
</content>
