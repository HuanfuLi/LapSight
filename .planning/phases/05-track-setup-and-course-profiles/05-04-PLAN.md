---
phase: 05-track-setup-and-course-profiles
plan: 4
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
  - shared/src/iosMain/kotlin/com/huanfuli/lapsight/shared/MainViewController.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileCreateTest.kt
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-01: A brand-new user with an EMPTY store can mark a track, name it, and save it as the first selectable V2 TrackProfile."
    - "SC-01 / D-02: Drive has a compact selector and Track detail has `Set as current track`."
    - "SC-03 / D-01: The newly created and selected profile is reused for Timing after relaunch-equivalent recreation."
    - "D-03/D-04: No selection blocks Timing with a direct selector action; no automatic newest/nearby recommendation exists."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt"
      provides: "Create-first-profile saveProfile path from completed Track marking"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileCreateTest.kt"
      provides: "Empty-store mark -> name -> save -> selectable integration gate"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      provides: "Compact Track selector and blocked-selection action"
  key_links:
    - from: "DriveMarkingController.kt"
      to: "TrackProfileController.saveProfile"
      via: "completed Track marking creates a named V2 TrackProfile"
      pattern: "saveProfile"
    - from: "MainViewController.kt"
      to: "StoragePaths.fileSessionStore()"
      via: "iOS app-private persistence injection"
      pattern: "fileSessionStore"
---

<objective>
Deliver the new-user end-to-end create flow: completed Track marking creates a named, selectable V2 TrackProfile, exposed on Drive and Track detail, with iOS file-store bootstrap.

Purpose: Close SC-01 for a brand-new user with an empty store — the prerequisite that every other Phase 5 plan assumes already exists.
Output: A Track-marking → `TrackProfileController.saveProfile` create path, an empty-store integration test, Drive/Track-detail selection surfaces, and iOS persistence injection.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-03-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
</context>

<interfaces>
Reuse `TrackProfileController.saveProfile` and the typed selection results from Plan 05-03. Marking produces a named profile with an immutable first revision; it never auto-selects newest. Compose renders state and forwards a name plus a profile ID; it never chooses a profile.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the first named profile from completed Track marking</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileCreateTest.kt</files>
  <behavior>
    - Starting from an EMPTY store, a completed Track marking plus a user-provided name creates one logical profile with a fresh `profileId`, an immutable first revision, and a fresh `geometryCompatibilityId`.
    - The created profile appears in `listActiveProfiles` and can be set as the current selection and resolved for Timing.
    - The same name twice creates two independent profiles (no overwrite, no collision).
    - Empty/blank names and unsafe characters are rejected without writing a profile; canonical paths never derive from the name.
    - Creating a profile does not auto-select it as current; selection remains explicit.
  </behavior>
  <action>Write `CourseProfileCreateTest` as an empty-store integration test driving marking → name → `saveProfile` → `listActiveProfiles` → `setCurrentSelection` → resolve, asserting the behaviors above for the in-memory store; then route the completed-marking save in `DriveMarkingController` through `TrackProfileController.saveProfile` to persist a named V2 profile with an immutable first revision and generated opaque IDs. Validate the name and reject unsafe input before any write. Implement until the test passes.</action>
  <acceptance_criteria>
    - An empty store becomes one selectable named V2 profile after marking + naming + save.
    - The profile is selectable and resolvable for Timing; selection stays explicit.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProfileCreateTest*" --tests "*CurrentTrackSelectionTest*"</automated>
  </verify>
  <done>A brand-new user can mark a track, name it, and save it as the first selectable V2 TrackProfile.</done>
</task>

<task type="auto">
  <name>Task 2: Expose selection on Drive and Track detail and bootstrap iOS persistence</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
    @shared/src/iosMain/kotlin/com/huanfuli/lapsight/shared/MainViewController.kt
    @shared/src/androidMain/kotlin/com/huanfuli/lapsight/shared/StoragePaths.android.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt, shared/src/iosMain/kotlin/com/huanfuli/lapsight/shared/MainViewController.kt</files>
  <action>Render the exact selected Track name and a compact pre-Timing selector on Drive per D-01/D-02; when none is ready, disable Timing and route the direct action to selection per D-03. Add `Set as current track` and a name-on-create affordance to Track detail. Keep these controls out of the moving fullscreen dash and retain closed-course/private-track and GPS-limit safety language. Inject `StoragePaths.fileSessionStore()` into iOS `MainViewController` (mirroring Android `MainActivity`) so created profiles and selection survive cold launch. Add no automatic nearby/recent recommendation.</action>
  <acceptance_criteria>
    - Drive always states the current Track or clearly states none, with a direct selector action when blocked.
    - Track detail can create/name and set current without starting a session.
    - iOS no longer defaults product runtime to `InMemorySessionStore`.
    - No nearby/recent automatic recommendation exists.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProfileCreateTest*" --tests "*DriveMarkingControllerTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>A new user can mark, name, save, select, and reuse a profile on Drive after relaunch; iOS persists it.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User profile name -> repository | Names are untrusted; they must never become canonical path components. |
| Platform bootstrap -> store | iOS must use the same app-private persistence contract as Android. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | create-profile naming/IDs | HIGH | mitigate | Generate opaque IDs; validate/reject unsafe or blank names before write; never derive paths from names. Proven by `CourseProfileCreateTest` unsafe-name cases (Task 1). |
| T-05-08 | Information disclosure | local persistence | MEDIUM | mitigate | Use platform app-private roots; never derive paths from profile names. |
| T-05-09 | Tampering | iOS bootstrap persistence | MEDIUM | mitigate | Inject `StoragePaths.fileSessionStore()` so iOS uses app-private files instead of in-memory; covered by macOS/iOS cold-launch UAT gate. |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run create/selection/controller tests, `:shared:check`, and Android debug build. On macOS, compile the iOS target and cold-launch twice to confirm a created+selected profile persists; record that manual gate in Phase 5 UAT.
</verification>

<success_criteria>
- SC-01 new-user create+save+select+reuse is observable and tested from an empty store.
- D-01 through D-04 selection behavior is visible on Drive/Track detail.
- Moving UI remains passive with explicit closed-course safety language.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-04-SUMMARY.md` when done.
</output>
</content>
