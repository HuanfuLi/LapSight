---
phase: 05-track-setup-and-course-profiles
plan: 7
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/TimingLines.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapEngine.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/ReviewModels.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CompleteSectorReplayTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/review/ReviewSummaryTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/export/JsonExportTest.kt
autonomous: true
requirements: [SC-02]
must_haves:
  truths:
    - "SC-02 / D-06: N configured Sectors are N complete intervals covering the entire closed lap."
    - "D-07: Disabled or 2 through 6 Sectors reaches the timing engine without line-count ambiguity."
    - "D-11: Sector order begins at start/finish; duration is adjacent-crossing difference and cumulative Split is separate."
    - "D-06/D-11: The accepted start/finish crossing closes the final Sector and lap at the same interpolated timestamp."
    - "D-20: A missed/out-of-order boundary never pauses lap timing or raw GPS capture."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapModels.kt"
      provides: "Complete `SectorResult` with duration and cumulative Split"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CompleteSectorReplayTest.kt"
      provides: "Independent green complete-Sector replay gate"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt"
      provides: "V2 complete-Sector snapshot while preserving V1 legacy split meaning"
  key_links:
    - from: "LapEngine.kt"
      to: "CourseDefinition"
      via: "expected intermediate boundary order and final start/finish closure"
      pattern: "SectorResult"
    - from: "TimingSessionRecorder.kt"
      to: "ReviewModels.kt"
      via: "persisted V2 sector duration and cumulative split"
      pattern: "durationMillis.*cumulativeSplitMillis"
---

<objective>
Replace line-centric Sector semantics with complete adjacent course intervals and preserve the result through recording, Review, and export.

Purpose: Give each lap full Sector coverage without corrupting legacy cumulative split history.
Output: Complete Sector domain/results, a green replay gate, V2 persistence, and Review/export regression coverage.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-06-SUMMARY.md
</context>

<interfaces>
Separate intermediate `TimingBoundary` definitions from derived `SectorDefinition` intervals. Add `SectorResult(lapNumber, sectorId, sectorOrder, startedAtMillis, endedAtMillis, durationMillis, cumulativeSplitMillis)`. Preserve V1 `SectorEventDto` as `LegacyCumulativeSplit` data.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement complete ordered Sector intervals behind a replay gate</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/TimingLines.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapEngine.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/CrossingDetector.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/LapEngineTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/TimingLines.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapEngine.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CompleteSectorReplayTest.kt</files>
  <behavior>
    - N=2..6 yields exactly N complete ordered Sector results per fully observed lap; disabled yields none.
    - The final interval closes on the accepted start/finish crossing at the same interpolated timestamp as the lap.
    - Duration differs from cumulative Split after Sector 1.
    - Duplicate/out-of-order/opposite crossings do not advance expected Sector state.
    - A missed intermediate boundary leaves affected Sector coverage explicitly unavailable but lap/raw timing continues.
    - Multiple crossings inside one low-frequency sample keep interpolation ordering stable.
  </behavior>
  <action>Write `CompleteSectorReplayTest` for D-06, D-07, D-11, and D-20 (these fail under current line-centric `SectorEvent.splitMillis`); then refactor the pure engine to consume intermediate boundaries plus N derived complete intervals, track the next expected boundary, reject duplicate/opposite/out-of-order advances, emit each adjacent duration with separate cumulative Split, and close the final Sector with the accepted start/finish lap event. Keep crossing geometry clean-room and platform/UI independent. Implement until the test passes.</action>
  <acceptance_criteria>
    - `CompleteSectorReplayTest` passes for N=2..6 and disabled sectors.
    - Final Sector and lap share one interpolated crossing timestamp.
    - Raw timing is not gated by Sector completeness.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CompleteSectorReplayTest*" --tests "*LapEngineTest*"</automated>
  </verify>
  <done>The lap engine emits complete, ordered Sector results behind a focused green gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and review V2 Sector results without rewriting V1 history</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/ReviewModels.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/review/ReviewSummaryTest.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/export/JsonExportTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/ReviewModels.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/review/ReviewSummaryTest.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/export/JsonExportTest.kt</files>
  <behavior>
    - Saved V2 session Review exposes N Sector durations and cumulative Splits as separate fields.
    - V1 Review/export preserves legacy cumulative semantics and geometry; no inferred final Sector or in-place rewrite.
    - Recorder sample/checkpoint fan-out remains unconditional when Sector coverage is incomplete.
  </behavior>
  <action>Map `SectorResult` into V2 draft/session snapshots and Review rows with both duration and cumulative Split, labeling V1 rows as `LegacyCumulativeSplit`. Extend `ReviewSummaryTest` and `JsonExportTest` to assert V2 separate fields and preserved V1 legacy semantics; verify the existing canonical `JsonExportService` serializes the immutable V2 snapshot without service changes. Keep recorder fan-out unconditional. Implement until the tests pass.</action>
  <acceptance_criteria>
    - Saved V2 session Review exposes N Sector durations and cumulative Splits.
    - V1 Review/export preserves legacy semantics and geometry.
    - The extended test classes are green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CompleteSectorReplayTest*" --tests "*ReviewSummaryTest*" --tests "*JsonExportTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>Complete Sector timing survives record, save, Review, and export without rewriting legacy history.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Course setup -> engine | Invalid counts/order can corrupt streaming state. |
| Crossing stream -> Sector state | Duplicate/out-of-order/opposite crossings are untrusted events. |
| V1/V2 session -> Review/export | Old cumulative meaning must not be spoofed as complete intervals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Tampering | course construction | MEDIUM | mitigate | Validate disabled or 2..6 and exactly N-1 intermediate boundaries before engine creation. Proven by `CompleteSectorReplayTest` count cases (Task 1). |
| T-05-14 | Tampering/DoS | crossing sequence | MEDIUM | mitigate | Expected-order state, duplicate rejection, bounded collections, continued lap timing on incomplete sectors. Proven by `CompleteSectorReplayTest` order/missed cases (Task 1). |
| T-05-15 | Repudiation | legacy Review/export | MEDIUM | mitigate | Explicit `LegacyCumulativeSplit` label; no inferred final Sector or in-place rewrite. Proven by `ReviewSummaryTest`/`JsonExportTest` (Task 2). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run focused Sector replay and Review/export regression tests, then `:shared:check`. Do not proceed to direction/Ghost work unless this gate is green.
</verification>

<success_criteria>
- D-06, D-07, D-11, and D-20 are deterministic and persisted correctly.
- The final interval is always represented when observed; V1 legacy meaning is preserved.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-07-SUMMARY.md` when done.
</output>
</content>
