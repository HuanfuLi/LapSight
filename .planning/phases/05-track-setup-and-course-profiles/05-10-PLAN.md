---
phase: 05-track-setup-and-course-profiles
plan: 10
type: execute
wave: 9
depends_on: [05-09]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibility.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/ReferenceLapSelector.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibilityTest.kt
autonomous: true
requirements: [SC-03, SC-04]
must_haves:
  truths:
    - "D-15: Sector-only revisions retain fastest-lap/Ghost compatibility; reference-line or start/finish revisions do not."
    - "D-16: A duplicated profile has independent fastest-lap/Ghost history even when geometry is identical."
    - "D-19: Fastest laps and Ghosts isolate by profile, geometry compatibility, Course Direction, and Demo/Real source."
    - "D-18: Recorded and Reverse references occupy separate compatibility slots."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibility.kt"
      provides: "CourseCompatibilityKey and structural validation"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibilityTest.kt"
      provides: "Collision, revision, direction, duplicate, and source isolation gate"
  key_links:
    - from: "ReferenceLapSelector.kt"
      to: "GhostModels.kt"
      via: "strict-faster comparison only for equal compatibility keys"
      pattern: "fasterOf"
    - from: "GhostCompatibility.kt"
      to: "SessionModels.kt"
      via: "compatibility key embedded in reference/session course snapshot"
      pattern: "CourseCompatibilityKey"
---

<objective>
Introduce the exact `CourseCompatibilityKey` contract on reference and session models, and gate fastest-lap selection by equal keys.

Purpose: Replace Track-only Ghost/reference identity with profile/geometry/direction/source compatibility before storage and matcher changes.
Output: A validated compatibility value object, reference payload fields, key-gated selection, and a deterministic compatibility gate.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-09-SUMMARY.md
@.planning/phases/04-ghost-lap-live-delta/04-CONTEXT.md
</context>

<interfaces>
Use `CourseCompatibilityKey(profileId, geometryCompatibilityId, direction, isSimulated)`. Keep exact `revisionId` in historical snapshots but never use it alone for reference selection. V1 references migrate to Recorded only.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the compatibility contract and key-gated selection</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/ReferenceLapSelector.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibility.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/ReferenceLapSelector.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibilityTest.kt</files>
  <behavior>
    - Sector-only revision reuses a reference; start/finish/reference-line revision does not.
    - Recorded, Reverse, duplicate profile, and Demo/Real each isolate reference history.
    - `ReferenceLapSelector.fasterOf` requires equal keys before duration comparison; equal-duration preserves the incumbent only within the same key.
    - No floating-point geometry hash is used; unsafe/mismatched profile/geometry/direction/source values are rejected.
    - V1 references map to the deterministic migrated key with Recorded direction and their original source.
  </behavior>
  <action>Write `GhostCompatibilityTest` covering D-15, D-16, D-18, D-19, and V1 Recorded-only migration (failing under existing Track ID + source-slot lookup); then add the immutable `CourseCompatibilityKey` to reference domain/payload models and session course snapshots, validate safe IDs/known direction/source agreement/profile-geometry relationships, and make `fasterOf` require equal keys. Implement until the test passes.</action>
  <acceptance_criteria>
    - Every key dimension has a mismatch test.
    - Sector-only revision shares the exact key; geometry edits and duplicates receive distinct keys.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*GhostCompatibilityTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>Reference data carries explicit collision-safe compatibility and selection is key-gated.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session course -> compatibility key | Cross-field mismatch can spoof compatible history. |
| Reference candidate -> selection | An incompatible candidate must not win fastest-lap comparison. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Spoofing | compatibility key | HIGH | mitigate | Full value-object equality and structural profile/geometry/direction/source validation before comparison. Proven by `GhostCompatibilityTest` mismatch cases (Task 1). |
| T-05-22 | Tampering | reference key migration | HIGH | mitigate | V1 references migrate Recorded-only with deterministic key; no geometry hash. Proven by `GhostCompatibilityTest` V1-migration cases (Task 1). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run Ghost compatibility tests and `:shared:check`; keep store persistence and matcher behavior unchanged until Plans 05-11 and 05-12.
</verification>

<success_criteria>
- D-15, D-16, D-18, and D-19 compatibility semantics are implemented in domain.
- Full compatibility collisions fail closed in selection.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-10-SUMMARY.md` when done.
</output>
</content>
