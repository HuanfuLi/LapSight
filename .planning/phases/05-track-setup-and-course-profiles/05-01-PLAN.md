---
phase: 05-track-setup-and-course-profiles
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt
  - shared/src/commonTest/resources/fixtures/v1/legacy-dataset.json
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-12: Existing named Tracks decode into stable logical profiles with immutable first revisions."
    - "SC-03 / D-14: V1 sessions/Ghosts map to deterministic historical geometry identities."
    - "D-13: Persisted geometry changes have a V2 revision representation instead of mutating V1 meaning."
    - "D-15: Migration assigns an explicit geometry compatibility identity without relabeling legacy cumulative splits as complete Sectors."
    - "D-01/D-04: Migration maps current selection to null; it never derives newest or only Track."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt"
      provides: "V2 profile, revision, course snapshot, and compatibility identities"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt"
      provides: "Frozen V1 to V2 dispatch and pure migration mapping"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt"
      provides: "Literal historical JSON, version-dispatch, and migration-correctness gates"
  key_links:
    - from: "SchemaMigrations.kt"
      to: "TrackProfileModels.kt"
      via: "JsonElement schemaVersion dispatch before typed decode and mapping"
      pattern: "schemaVersion"
    - from: "TimingSession V2 payload"
      to: "CourseSnapshot"
      via: "immutable historical course identity and geometry"
      pattern: "courseSnapshot"
---

<objective>
Define frozen V1 DTOs, validated V2 profile/session contracts, and a pure V1→V2 migration mapping with literal fixture coverage.

Purpose: Establish the migration contract and identities required before any store wiring, selection, Sector, or Ghost semantics change.
Output: Frozen V1 serializers, V2 profile/revision/snapshot contracts, deterministic migration mapping, and a literal fixture-backed test that passes green.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt
</context>

<interfaces>
Preserve `LoadResult.Loaded/NotFound/Corrupt` and `SaveResult`. Freeze every existing `*PayloadV1.schemaVersion` to literal `1`. Introduce `TrackProfile`, `TrackRevision`, `CourseSetup`, `CourseSnapshot`, `CourseCompatibilityKey`, and explicit V2 payloads with literal version `2`. Keep legacy `SectorEventDto` cumulative meaning intact as `LegacyCumulativeSplit`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze V1 and define validated V2 contracts against literal fixtures</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStoreTest.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt, shared/src/commonTest/resources/fixtures/v1/legacy-dataset.json</files>
  <behavior>
    - Literal current serializer output for Track, Review index, saved session, draft, and Ghost decodes through explicit `schemaVersion` dispatch without reconstructing V1 objects in test code.
    - Each V1 DTO is frozen to literal version `1`; each V2 DTO emits literal version `2`; neither can emit the other's number.
    - Unknown/newer `schemaVersion` returns `Corrupt("unsupported schemaVersion ...")`.
    - Malformed JSON, unsafe IDs containing `/` `\` `..` or control characters, non-finite/oversized geometry, and empty point lists are typed corruption, never thrown.
    - Legacy `SectorEventDto` retains cumulative split semantics as `LegacyCumulativeSplit`.
  </behavior>
  <action>Write the literal `legacy-dataset.json` resource with separate raw Track, Review index, saved session, draft, and Ghost payload members captured byte-for-byte from the current serializer; then add `SchemaMigrationTest` cases that extract and decode each member, pin field names and historical meaning, and assert the corruption/version cases above. Freeze each existing V1 DTO with literal `schemaVersion = 1` (not the mutable `CURRENT_*` constant) and create V2 `TrackProfile`, `TrackRevision`, `CourseSetup`, `CourseSnapshot`, `CourseCompatibilityKey`, and explicit V2 payloads with literal `2` and bounded-finite validation. Implement the `JsonElement` version-dispatch decode in `SchemaMigrations` returning typed `LoadResult`. Write the failing assertions before the contracts, then implement contracts until the test class passes.</action>
  <acceptance_criteria>
    - All five V1 artifact classes have literal committed fixtures decoded through dispatch.
    - V2 contracts validate finite bounded geometry, revision ordinal ordering, and safe opaque IDs.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*SchemaMigrationTest*"</automated>
  </verify>
  <done>Frozen V1 + validated V2 contracts decode the literal fixtures through explicit version dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure V1 to V2 migration mapping</name>
  <read_first>
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt</files>
  <action>Per D-12 through D-15, implement pure mapping functions: `TrackPayloadV1` → profile with `profileId = old track.id`, deterministic `revisionId = oldId + ":r1"`, immutable first revision, deterministic `geometryCompatibilityId`, old Sector lines as intermediate boundaries when count is 1–5, Recorded direction. `TimingSessionPayloadV1` → migrated profile/revision/geometry IDs with preserved start/finish and `LegacyCumulativeSplit` events (never relabeled as complete Sectors). `GhostReferencePayloadV1` → Recorded-only migrated geometry key. `ReviewIndex` V1 → rebuilt-from-payload cache rows. Current selection → `null` (never newest/only). Extend `SchemaMigrationTest` with migration-correctness assertions for each mapping and run them green. Do not hash floating-point geometry for compatibility.</action>
  <acceptance_criteria>
    - V2 profile/revision identity encodes D-12 through D-15 without geometry hashing.
    - V1 split events remain explicitly legacy; no inferred complete final Sector.
    - Migration never produces a non-null current selection.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*SchemaMigrationTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>A literal V1 dataset maps to V2 profiles/revisions deterministically with preserved legacy meaning, behind a green gate.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; web auth/session/transport controls N/A — applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local JSON -> serializers | Files may be malformed, oversized, non-finite, partial, or from a future version. |
| V1 -> V2 meaning | Mapping can silently corrupt historical Sector/Ghost semantics or derive an implicit selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering/DoS | schema dispatch + decode | HIGH | mitigate | Frozen literal serializers, strict `JsonElement` version dispatch, bounded reads/lists, finite-geometry validation, typed `Corrupt`. Proven by `SchemaMigrationTest` version/corruption cases (Task 1). |
| T-05-02 | Tampering | V1→V2 mapping semantics | HIGH | mitigate | Deterministic identities, explicit `geometryCompatibilityId`, `LegacyCumulativeSplit` preservation, null selection. Proven by `SchemaMigrationTest` migration-correctness cases (Task 2). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run `SchemaMigrationTest` and `:shared:check`; confirm version dispatch fails closed and migration mapping never derives a current selection.
</verification>

<success_criteria>
- Literal V1 Track/index/session/draft/Ghost data decodes and maps to V2 deterministically.
- V1 historical meanings (legacy splits, source) are preserved in the mapping.
- Unsupported versions and unsafe/malformed payloads fail closed.
- No new dependency, ORM, cloud, map, GNSS, glasses, or telemetry scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
