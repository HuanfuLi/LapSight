---
phase: 05-track-setup-and-course-profiles
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt
  - shared/src/commonTest/resources/fixtures/v1/legacy-dataset.json
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-12: Existing named Tracks migrate into stable logical profiles with immutable first revisions."
    - "SC-03 / D-14: Existing sessions remain bound to their historical geometry after migration."
    - "D-13: Persisted geometry changes have a V2 revision representation instead of mutating V1 meaning."
    - "D-15: Migration assigns an explicit geometry compatibility identity without relabeling legacy cumulative splits as complete Sectors."
    - "D-01/D-04: Migration leaves current selection empty; it never chooses the newest or only Track."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt"
      provides: "V2 profile, revision, course snapshot, and compatibility identities"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt"
      provides: "Frozen V1 to V2 dispatch and migration"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt"
      provides: "Literal historical JSON, idempotence, fault, and future-version gates"
  key_links:
    - from: "FileSessionStore.kt"
      to: "SchemaMigrations.kt"
      via: "JsonElement schemaVersion dispatch before typed decode"
      pattern: "schemaVersion.*(1|2)"
    - from: "TimingSession V2 payload"
      to: "CourseSnapshot"
      via: "immutable historical course identity and geometry"
      pattern: "courseSnapshot"
---

<objective>
Make persisted Phase 3/4 data safely readable as V2 Track profiles while retaining original V1 files and historical meaning.

Purpose: Establish the independently green migration gate required before selection, Sector, or Ghost semantics change.
Output: Frozen V1 DTOs, V2 profile/session contracts, idempotent side-by-side migration, and literal fixture coverage.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
</context>

<interfaces>
Preserve `LoadResult.Loaded/NotFound/Corrupt` and `SaveResult`. Freeze every existing `*PayloadV1.schemaVersion` to literal `1`. Introduce `TrackProfile`, `TrackRevision`, `CourseSetup`, `CourseSnapshot`, and explicit V2 payloads. Keep legacy `SectorEventDto` cumulative meaning intact.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze literal V1 migration evidence</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStoreTest.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
  </read_first>
  <files>shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt, shared/src/commonTest/resources/fixtures/v1/legacy-dataset.json</files>
  <behavior>
    - Literal current serializer output for Track, index, saved session, draft, and Ghost loads without reconstructing V1 objects in test code.
    - Malformed JSON, unsafe IDs, non-finite/oversized geometry, and unknown future schema versions fail as typed corruption.
    - Legacy Sector events retain cumulative split semantics.
  </behavior>
  <action>Commit one literal `legacy-dataset.json` resource containing separate raw Track, Review index, saved session, draft, and Ghost payload members, then add a RED `SchemaMigrationTest` that extracts and decodes each payload independently while pinning byte-level field names and historical meanings. Include IDs containing `/`, `\`, `..`, and control characters; future schemaVersion values; oversized point lists; NaN/infinite geometry represented through invalid payload cases; and interrupted-write recovery. Do not mutate production serializers merely to make fixtures decode.</action>
  <acceptance_criteria>
    - All five V1 artifact classes have literal fixtures.
    - Tests distinguish legacy cumulative splits from V2 complete Sector results.
    - Corruption cases are typed and never escape as realtime exceptions.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*SchemaMigrationTest*"</automated>
  </verify>
  <done>The migration contract is executable and fails only on missing V2 dispatch/migration behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define frozen V1 and validated V2 profile contracts</name>
  <read_first>
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaVersions.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt</files>
  <action>Per D-12 through D-15, create separate V2 serializers and domain mappings for stable `profileId`, immutable `revisionId`/ordinal, `geometryCompatibilityId`, reference line, confirmed start/finish, intermediate boundary placements, and immutable `CourseSnapshot`. Freeze V1 defaults to literal 1. Implement `JsonElement` version dispatch and deterministic V1 identities. Validate bounded finite geometry, revision ordering, safe opaque IDs, and internal identity agreement before mapping. A V1 migration gets Recorded direction, an unchanged compatibility ID, and no current selection; preserve V1 split events as explicitly legacy.</action>
  <acceptance_criteria>
    - V1 and V2 serializers cannot emit each other's schema number.
    - V2 profile/revision identity encodes D-12 through D-15 without geometry hashing.
    - Unknown newer schemas return `Corrupt("unsupported schemaVersion ...")`.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*SchemaMigrationTest*"</automated>
  </verify>
  <done>Validated V2 contracts and pure V1-to-V2 mappings satisfy the literal fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire idempotent side-by-side store migration</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrationTest.kt</files>
  <action>Add typed V2 load/save operations and migration entry points to `LocalSessionStore`, mirroring them in file and memory stores. Decode and validate all source payloads before writes, write profile/session/reference payloads before rebuilding the index, retain V1 originals, and make retries idempotent after any injected Okio failure. Never derive a current Track during migration per D-01/D-04. Reject unsafe path IDs before constructing paths and cap file/list sizes before geometry work.</action>
  <acceptance_criteria>
    - Repeated migration produces one logical profile and one first revision.
    - Interrupted migration is recoverable and V1 originals remain readable.
    - Migrated sessions and Ghost history retain their original source and geometry association.
    - File and in-memory stores expose equivalent results.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*SchemaMigrationTest*" --tests "*FileSessionStoreTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>A V1 dataset upgrades to V2 profiles without data loss, semantic relabeling, or implicit selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local JSON -> serializers | Files may be malformed, oversized, non-finite, partial, or from a future version. |
| Persisted IDs -> file paths | IDs are untrusted path components. |
| V1 -> V2 meaning | Migration can silently corrupt historical Sector/Ghost semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/DoS | schema dispatch | mitigate | Frozen serializers, strict version dispatch, bounded reads/lists, finite geometry validation, and typed corruption. |
| T-05-02 | Tampering | path construction | mitigate | Reject separators, `..`, controls, and mismatched IDs before any Okio path is built. |
| T-05-03 | Tampering/DoS | migration writes | mitigate | Validate first, payload-first/index-last atomic writes, retained originals, idempotence, and fault-injected tests. |
| T-05-SC | Tampering | package installs | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run `SchemaMigrationTest`, `FileSessionStoreTest`, `:shared:check`, and confirm migration never writes current selection.
</verification>

<success_criteria>
- Literal V1 Track/index/session/draft/Ghost data migrates idempotently.
- V1 originals and historical meanings remain available.
- Unsupported versions and unsafe/malformed payloads fail closed.
- No new dependency, ORM, cloud, map, GNSS, glasses, or telemetry scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-01-SUMMARY.md` when done.
</output>
