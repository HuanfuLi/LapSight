---
phase: 05-track-setup-and-course-profiles
plan: 9
type: execute
wave: 8
depends_on: [05-07, 05-08]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseGeometryBuilder.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/TimingLines.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapEngine.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CourseDirectionReplayTest.kt
autonomous: true
requirements: [SC-03, SC-04]
must_haves:
  truths:
    - "SC-03 / D-18: Recorded and Reverse are selectable configurations over the same physical revision."
    - "D-11/D-18: Reverse transforms progress, Sector order, and every boundary orientation."
    - "D-19: Last Course Direction persists and is reused by the next Timing run."
    - "SC-04 / D-21: Opposite-direction crossings never complete a lap/Sector; Reverse flips accepted crossing direction."
    - "D-17/D-20: No global wrong-direction state, pause, warning, or data-capture stop is introduced."
  artifacts:
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CourseDirectionReplayTest.kt"
      provides: "Recorded/reverse and turnaround integrity gate"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      provides: "Remembered pre-Timing Recorded/Reverse selector"
  key_links:
    - from: "CourseGeometryBuilder.kt"
      to: "LapEngine.kt"
      via: "direction-specific progress/order/oriented boundaries"
      pattern: "CourseDirection"
    - from: "DriveMarkingController.kt"
      to: "SessionController.kt"
      via: "persisted selected CourseDirection at Timing start"
      pattern: "CourseDirection"
---

<objective>
Deliver Recorded/Reverse course behavior through the normal persisted selection and Timing start flow.

Purpose: Make direction a real course configuration without a global reverse-driving state machine.
Output: Direction transforms, explicit boundary orientation, persisted direction selection, UI selection, and a replay gate.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-07-SUMMARY.md
@.planning/phases/05-track-setup-and-course-profiles/05-08-SUMMARY.md
</context>

<interfaces>
Introduce `CourseDirection.Recorded/Reverse`. Store it with `CurrentTrackSelection` and snapshot it into the TimingSession course contract. Ghost compatibility is deliberately deferred to Plan 05-10. Reverse reuses physical anchors but inverts progress, Sector order, and boundary endpoints.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build direction-specific course definitions and turnaround integrity</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseGeometryBuilder.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/TimingLines.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapEngine.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/LapEngineTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseGeometryBuilder.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/TimingLines.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapEngine.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/CourseDirectionReplayTest.kt</files>
  <behavior>
    - Recorded and Reverse use identical physical anchors but inverse progress/order and swapped boundary endpoints.
    - Opposite crossings, including the first start/finish, are rejected by explicit accepted orientation (no learned-first-crossing direction).
    - A turnaround across start/finish or an intermediate boundary creates no false short lap/Sector and does not pause clocks.
    - Recorded and Reverse produce distinct ordered course definitions.
  </behavior>
  <action>Write `CourseDirectionReplayTest` for D-17, D-18, D-20, and D-21 (these reject learned-first-crossing semantics); then implement Recorded progress `wrap(s-s0,L)` and Reverse `wrap(s0-s,L)`, reverse Sector order/labels, and swap endpoints for start/finish plus every intermediate boundary in `CourseGeometryBuilder`. Replace learned first-crossing direction in `LapEngine` with explicit accepted orientation from course construction; reversing during a lap continues timing/Sector state and rejects only opposite boundary completion. Add `CourseDirection` to `TrackProfileModels`. Implement until the test passes.</action>
  <acceptance_criteria>
    - Recorded/reverse replay tests pass with identical physical geometry.
    - Turnaround cannot create false completion and never pauses state.
    - The lap engine has no global wrong-direction mode.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseDirectionReplayTest*" --tests "*CompleteSectorReplayTest*"</automated>
  </verify>
  <done>CourseDirection changes progress/order/crossing acceptance, not physical geometry or timing continuity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and use the selected Course Direction at Timing start</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt</files>
  <action>Persist the last selected direction inside the existing atomic current-selection payload and expose a compact pre-Timing Recorded/Reverse selector on Drive. Snapshot direction into drafts/sessions and build the direction-specific `CourseDefinition` in `SessionController.startTiming`. Keep Ghost loading suppressed unless the existing reference is provably Recorded-compatible; Plan 05-10 replaces reference identity before Reverse Ghost use. Add no moving-fullscreen interaction. Extend `CurrentTrackSelectionTest`/`SessionControllerTest` assertions for direction persistence and reuse.</action>
  <acceptance_criteria>
    - Direction survives relaunch-equivalent recreation and affects the next normal Timing run.
    - Reverse Timing uses reversed progress/order and rejects opposite crossings.
    - Reverse cannot consume an unqualified legacy Track-only Ghost.
    - The affected test classes are green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseDirectionReplayTest*" --tests "*CurrentTrackSelectionTest*" --tests "*SessionControllerTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>A user can select and reuse Recorded/Reverse timing without cross-binding a legacy Ghost.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Direction selection -> course builder | Persisted enum/geometry must agree. |
| Persisted direction -> Timing session | A stale/unknown direction must not produce an invalid course. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Tampering | directional boundaries | HIGH | mitigate | Explicit endpoint orientation and replay tests from the first crossing onward. Proven by `CourseDirectionReplayTest` opposite-crossing cases (Task 1). |
| T-05-20 | Tampering | direction selection | MEDIUM | mitigate | Typed enum/default migration, exact persisted selection, no unqualified Reverse Ghost. Proven by `CurrentTrackSelectionTest` direction cases (Task 2). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run direction, complete-Sector, selection, and session-controller tests; then shared checks and Android build.
</verification>

<success_criteria>
- D-17 through D-21 are implemented for course timing.
- SC-03 reuses the exact selected profile/direction; SC-04 rejects wrong-direction completion without pausing timing.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-09-SUMMARY.md` when done.
</output>
</content>
