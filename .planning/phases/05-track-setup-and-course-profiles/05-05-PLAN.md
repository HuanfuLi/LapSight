---
phase: 05-track-setup-and-course-profiles
plan: 5
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseGeometryBuilder.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileEditor.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePathTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileEditorTest.kt
autonomous: true
requirements: [SC-02]
must_haves:
  truths:
    - "SC-02 / D-05: Start/finish is an editable confirmed boundary placement on the closed reference path."
    - "D-07: Sector timing supports disabled or 2 through 6 Sectors; enabling defaults to 3."
    - "D-08: N Sectors generate N-1 boundaries at equal closed-path arc-length intervals."
    - "D-09: Every generated boundary is perpendicular to the smoothed local tangent; endpoints are not user-editable."
    - "D-10: Dragging changes only reference-line progress and continuously regenerates tangent/perpendicular geometry."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt"
      provides: "Closed-loop arc length, wrapping, tangent, projection, and ambiguity data"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileEditor.kt"
      provides: "Pure validated start/finish and constrained Sector-boundary edit state"
  key_links:
    - from: "CourseProfileEditor.kt"
      to: "ClosedReferencePath.kt"
      via: "progress-only placement, projection, and perpendicular generation"
      pattern: "project|pointAt"
    - from: "CourseGeometryBuilder.kt"
      to: "ClosedReferencePath.kt"
      via: "equal arc-length boundary generation"
      pattern: "arcLength"
---

<objective>
Build the single pure `ClosedReferencePath` primitive and the constrained `CourseProfileEditor` state, with deterministic geometry tests.

Purpose: One closed-loop arc-length/projection model drives editor placement and (later) direction, preflight, and Ghost matching, preventing four divergent implementations.
Output: Pure closed-path/editor/geometry-builder algorithms and their deterministic green test gates.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-04-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ReferenceLineExtractor.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LocalProjection.kt
</context>

<interfaces>
Create one pure `ClosedReferencePath` (perimeter, wrapped `pointAt`, smoothed tangent, nearest-segment projection with ambiguity). Persist normalized recorded-orientation progress, not canvas coordinates or editable endpoints. `CourseProfileEditor` is plain immutable Kotlin state with validation. Centralize all thresholds per 05-RESEARCH Pattern 3 (see Plan 05-RESEARCH for resolved initial values).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the closed reference path primitive</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ReferenceLineExtractor.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LocalProjection.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/ReferenceLineExtractorTest.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePathTest.kt</files>
  <behavior>
    - The last-to-first segment participates in perimeter, `pointAt`, projection, and wrapping; zero-length segments are ignored.
    - `pointAt(s)` wraps `s` modulo total length and binary-searches the segment.
    - `project(point)` returns absolute recorded progress, lateral distance, segment identity, and ambiguity data.
    - Smoothed tangent uses the centralized half-window and falls back to the nearest non-degenerate segment.
    - Empty, oversized, non-finite, or degenerate paths return typed rejection, never throw.
  </behavior>
  <action>Write `ClosedReferencePathTest` using the closed oval and hairpin/parallel fixtures from 05-VALIDATION asserting the behaviors above; then implement the cumulative-length table with explicit closing segment, wrapped `pointAt`, smoothed tangent, O(N) nearest-segment projection with ambiguity, and bounded/non-finite rejection. Centralize tangent half-window, boundary length, and snap thresholds. Implement until the test passes.</action>
  <acceptance_criteria>
    - All path tests pass deterministically across repeated runs.
    - Projection carries absolute progress, lateral distance, segment ID, and ambiguity.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*ClosedReferencePathTest*"</automated>
  </verify>
  <done>One reusable closed-path primitive supports wrapping, projection, tangent, and ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement constrained editor state and equal Sector generation</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePathTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackReviewState.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseGeometryBuilder.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileEditor.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/track/CourseProfileEditorTest.kt</files>
  <behavior>
    - Disabled or 2..6 Sectors is valid; enabling defaults to 3; N produces N-1 stable boundary IDs.
    - Equal arc placement, perpendicular normal generation, 1 m progress snap, and minimum cyclic spacing are deterministic.
    - Hairpin/self-intersecting placement and impossible-spacing counts are rejected with typed validation.
    - Drag candidates project onto the trace and persist progress only — never endpoint or canvas coordinates.
    - Start/finish is a confirmed placement; saving requires a valid confirmed boundary (D-05).
  </behavior>
  <action>Write `CourseProfileEditorTest` covering 2/3/6 Sectors and disabled timing, equal generation, constrained drag, spacing, validation, and confirmed start/finish; then implement `CourseGeometryBuilder` equal `N-1` generation over `ClosedReferencePath` and `CourseProfileEditor` immutable state with snapping, cyclic spacing clamp, tangent/normal regeneration, and typed validation. Add the `CourseSetup`/editor fields to `TrackProfileModels` as needed. Keep all code Compose/platform/storage free. Implement until the test passes.</action>
  <acceptance_criteria>
    - Tests cover 2, 3, and 6 Sector configurations plus disabled timing.
    - Boundary endpoints are always derived from progress and tangent; no UI type appears in domain models.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProfileEditorTest*" --tests "*ClosedReferencePathTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>Pure shared geometry supports valid offline course editing with constrained drag and equal generation.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted reference geometry -> path math | Malformed or huge geometry can crash or exhaust processing. |
| Candidate progress -> editor state | Arbitrary positions must not become persisted endpoint/canvas coordinates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | DoS | `ClosedReferencePath` | MEDIUM | mitigate | Finite/bounded/non-degenerate validation before projection or tangent work. Proven by `ClosedReferencePathTest` invalid-geometry cases (Task 1). |
| T-05-11 | Tampering | editor placement | MEDIUM | mitigate | Project candidates to nearest path; persist progress only and regenerate endpoints. Proven by `CourseProfileEditorTest` drag/validation cases (Task 2). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run focused path/editor tests and `:shared:check`; confirm algorithms are pure shared Kotlin and replay-testable.
</verification>

<success_criteria>
- D-05 and D-07 through D-10 algorithms are implemented and deterministic.
- No UI/platform/storage types leak into domain geometry.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-05-SUMMARY.md` when done.
</output>
</content>
