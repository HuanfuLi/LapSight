---
phase: 05-track-setup-and-course-profiles
plan: 2
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
  - shared/src/iosMain/kotlin/com/huanfuli/lapsight/shared/MainViewController.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingControllerTest.kt
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-01: A named profile can be explicitly selected and its exact name is shown on Drive."
    - "SC-03 / D-01: The selected profile survives store/controller recreation and is reused for Timing."
    - "D-02: Drive has a compact selector and Track detail has `Set as current track`."
    - "D-03: No selection, stale selection, archived selection, or non-timing-ready selection blocks Timing with a direct selector action."
    - "D-04: Newer profiles and location do not replace the user's explicit selection."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt"
      provides: "Typed current-profile resolution with no fallback"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt"
      provides: "Compact Track selector and blocked selection action"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt"
      provides: "Persistence, stale/archive, and no-newest regression gate"
  key_links:
    - from: "DriveMarkingController.kt"
      to: "TrackProfileController.kt"
      via: "immutable current-selection snapshot"
      pattern: "current.*profile"
    - from: "MainViewController.kt"
      to: "StoragePaths.fileSessionStore()"
      via: "iOS app-private persistence injection"
      pattern: "fileSessionStore"
---

<objective>
Deliver explicit current-Track selection end to end: persistence, Drive selector, Track-detail action, timing block, and iOS file-store bootstrap.

Purpose: Remove the silent newest-Track behavior and make prior course setup reusable on the next launch.
Output: Persisted selection operations, shared controller state, Drive/Review interactions, and deterministic selection tests.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-01-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
</context>

<interfaces>
Use typed selection results such as `Selected`, `None`, `Archived`, `Corrupt`, and `NotTimingReady`. Preserve plain-Kotlin snapshot/controller boundaries. Compose renders state and forwards profile IDs; it never chooses a profile.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin explicit selection and no-fallback behavior</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingControllerTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingControllerTest.kt</files>
  <behavior>
    - Empty/migrated storage has no current Track and Timing is blocked.
    - Selecting profile A persists across new store/controller instances even after profile B is created.
    - Stale, corrupt, archived, or not-ready selection never resolves to another profile.
    - Track detail selection and Drive selection produce the same persisted result.
  </behavior>
  <action>Replace the existing newest-ready regression expectation with RED tests for D-01 through D-04. Assert the blocked snapshot includes a direct selection action and that visible profile rows include only active latest revisions.</action>
  <acceptance_criteria>
    - Tests explicitly reject `maxByOrNull(createdAtEpochMillis)` fallback behavior.
    - Selection persistence is proven for both file-style and in-memory contracts.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CurrentTrackSelectionTest*" --tests "*DriveMarkingControllerTest*"</automated>
  </verify>
  <done>The selection contract is executable before production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and resolve the exact current profile</name>
  <read_first>
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt</files>
  <action>Add atomic `loadCurrentSelection`, `setCurrentSelection`, and `clearCurrentSelection` operations plus active-profile listing. Resolve only the exact persisted profile and its latest active timing-ready revision. Return typed unavailable states for stale/archive/corruption and keep Timing blocked; never repair by selecting another profile. Validate IDs before path use. Adapt `DriveMarkingController.snapshot()` to expose current name, rows, and selection action while removing newest-Track derivation.</action>
  <acceptance_criteria>
    - Exact selection survives relaunch-equivalent object recreation.
    - Creating/saving another profile does not change selection.
    - Invalid selection cannot cause path traversal or an implicit replacement.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CurrentTrackSelectionTest*" --tests "*DriveMarkingControllerTest*"</automated>
  </verify>
  <done>Shared storage and controller resolve one explicit current profile without fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Expose selection on Drive and Track detail</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
    @shared/src/iosMain/kotlin/com/huanfuli/lapsight/shared/MainViewController.kt
    @shared/src/androidMain/kotlin/com/huanfuli/lapsight/shared/StoragePaths.android.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt, shared/src/iosMain/kotlin/com/huanfuli/lapsight/shared/MainViewController.kt</files>
  <action>Render the exact selected Track name and a compact pre-Timing selector on Drive per D-01/D-02. When none is ready, disable Timing and route the direct action to selection per D-03. Add `Set as current track` to Track detail. Keep these controls out of the moving fullscreen dash and retain closed-course/private-track and GPS-limit language. Inject `StoragePaths.fileSessionStore()` into iOS `MainViewController` so selection/profile state can survive cold launch.</action>
  <acceptance_criteria>
    - Drive always states which Track is current or clearly states none.
    - Track detail can set current without creating a new session.
    - No nearby/recent automatic recommendation exists.
    - iOS no longer defaults product runtime to `InMemorySessionStore`.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CurrentTrackSelectionTest*" --tests "*DriveMarkingControllerTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>A repeat user can select a saved named profile and see/reuse it on Drive after relaunch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI profile ID -> repository | A stale or unsafe ID can reach local storage. |
| Selection file -> controller | Archived/corrupt state must not silently select another Track. |
| Platform bootstrap -> store | iOS must use the same app-private persistence contract as Android. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing/Tampering | profile selection | mitigate | Validate exact opaque ID, resolve active latest revision, and never fall back. |
| T-05-05 | DoS | stale archived selection | mitigate | Return typed unavailable state, block Timing, and expose explicit recovery action. |
| T-05-06 | Information disclosure | local persistence | mitigate | Use platform app-private roots; never derive paths from profile names. |
| T-05-SC | Tampering | package installs | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run focused selection/controller tests, `:shared:check`, and Android debug build. On macOS, compile the iOS target and cold-launch twice to confirm exact selection persistence; record that manual gate in Phase 5 UAT.
</verification>

<success_criteria>
- D-01 through D-04 are observable in Drive/Review and deterministic tests.
- Exact selection survives relaunch and blocks safely when unavailable.
- Moving UI remains passive with explicit closed-course safety language.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-02-SUMMARY.md` when done.
</output>
