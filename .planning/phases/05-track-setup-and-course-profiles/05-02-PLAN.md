---
phase: 05-track-setup-and-course-profiles
plan: 2
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/StoreMigrationTest.kt
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-12: A V1 dataset upgrades to side-by-side V2 profiles while V1 originals remain readable."
    - "SC-03 / D-14: Migrated sessions and Ghosts retain their original source and geometry association."
    - "D-13: Persisted geometry changes have a V2 revision representation; V1 files are not mutated."
    - "D-01/D-04: The store never writes a current selection during migration."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt"
      provides: "Typed V2 load/save operations and a migration entry point"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/StoreMigrationTest.kt"
      provides: "Idempotence, fault-injection, and retained-original gate"
  key_links:
    - from: "FileSessionStore.kt"
      to: "SchemaMigrations.kt"
      via: "version dispatch before typed decode and payload-first/index-last writes"
      pattern: "migrate"
    - from: "FileSessionStore.kt"
      to: "InMemorySessionStore.kt"
      via: "mirrored externally observable migration behavior"
      pattern: "migrate"
---

<objective>
Wire idempotent, fault-recoverable side-by-side V1→V2 migration into the file and in-memory stores using the mapping from Plan 05-01.

Purpose: Make persisted Phase 3/4 data readable as V2 profiles without losing V1 originals or deriving an implicit selection.
Output: Typed V2 store operations, a migration entry point mirrored across stores, and a fault-injected store migration test.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-01-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
</context>

<interfaces>
Reuse `SchemaMigrations` dispatch/mapping from Plan 05-01. Add typed `migrate`, `saveProfile`, `loadProfile`, and `listActiveProfiles` operations on `LocalSessionStore`; the in-memory store must mirror file-store behavior. Reject unsafe path IDs before constructing any Okio path.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed V2 store operations and migration entry point</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/SchemaMigrations.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStoreTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/StoreMigrationTest.kt</files>
  <behavior>
    - Migrating a literal V1 dataset writes V2 profile/session/reference payloads side-by-side and retains every V1 original file.
    - Migration is idempotent: re-running produces one logical profile and one first revision per source Track.
    - A write fault injected between payload and index writes leaves V1 originals readable and is recoverable on retry.
    - Migration never writes a current selection.
    - File and in-memory stores expose equivalent externally observable results.
    - Unsafe opaque IDs are rejected before any path is built.
  </behavior>
  <action>Write `StoreMigrationTest` using the injected `InMemoryFileSystem`/`FailingWritesFileSystem` pattern and the literal V1 fixtures, asserting the behaviors above; then add a `migrate` entry point plus `saveProfile`/`loadProfile`/`listActiveProfiles` to `LocalSessionStore`, implement them in `FileSessionStore` with decode-and-validate-before-write, payload-first/index-last atomic writes, retained V1 originals, and idempotent retries, and mirror the same observable behavior in `InMemorySessionStore`. Reject separators/`..`/control characters in IDs and cap file/list sizes before geometry work. Implement until `StoreMigrationTest` passes.</action>
  <acceptance_criteria>
    - Repeated migration is idempotent and lossless.
    - Interrupted migration is recoverable; V1 originals remain.
    - Both stores match for every migration result.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*StoreMigrationTest*" --tests "*FileSessionStoreTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>A V1 dataset upgrades to side-by-side V2 profiles without data loss, semantic relabeling, or implicit selection.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted IDs -> file paths | IDs are untrusted path components. |
| Migration writes -> stored history | Partial/duplicated writes can lose or fork history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | path construction | HIGH | mitigate | Reject separators, `..`, controls, and mismatched IDs before any Okio path is built. Proven by `StoreMigrationTest` unsafe-ID cases (Task 1). |
| T-05-04 | Tampering/DoS | migration writes | HIGH | mitigate | Validate-first, payload-first/index-last atomic writes, retained originals, idempotence, fault-injected retry. Proven by `StoreMigrationTest` fault-injection cases (Task 1). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run `StoreMigrationTest`, `FileSessionStoreTest`, and `:shared:check`; confirm migration retains V1 originals and writes no current selection.
</verification>

<success_criteria>
- Side-by-side migration is idempotent, fault-recoverable, and lossless.
- Both stores expose equivalent migration behavior.
- No new dependency, ORM, cloud, map, GNSS, glasses, or telemetry scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-02-SUMMARY.md` when done.
</output>
</content>
