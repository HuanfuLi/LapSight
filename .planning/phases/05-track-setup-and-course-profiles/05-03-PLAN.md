---
phase: 05-track-setup-and-course-profiles
plan: 3
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingControllerTest.kt
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-01: A named profile can be explicitly selected and resolved by exact identity."
    - "SC-03 / D-01: The selected profile survives store/controller recreation and is reused for Timing."
    - "D-03: No selection, stale, archived, corrupt, or non-timing-ready selection blocks Timing with a typed unavailable state."
    - "D-04: Newer profiles and location never replace the user's explicit selection."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt"
      provides: "Typed current-profile resolution with no fallback"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt"
      provides: "Persistence, stale/archive, and no-newest regression gate"
  key_links:
    - from: "DriveMarkingController.kt"
      to: "TrackProfileController.kt"
      via: "immutable current-selection snapshot"
      pattern: "current.*profile"
    - from: "TrackProfileController.kt"
      to: "LocalSessionStore current-selection operations"
      via: "exact persisted profile resolution"
      pattern: "loadCurrentSelection"
---

<objective>
Persist and resolve an exact current Track selection with no newest-Track fallback, and adapt the Drive controller snapshot to expose it.

Purpose: Remove the silent newest-Track behavior so prior course setup is reusable on the next launch.
Output: Persisted selection operations across both stores, a typed no-fallback controller resolution, and deterministic selection tests.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-02-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
</context>

<interfaces>
Use typed selection results such as `Selected`, `None`, `Archived`, `Corrupt`, and `NotTimingReady`. Add `loadCurrentSelection`/`setCurrentSelection`/`clearCurrentSelection` to `LocalSessionStore`. The controller resolves only the exact persisted profile and its latest active timing-ready revision; it never derives a Track.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and resolve the exact current profile with no fallback</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt</files>
  <behavior>
    - Empty/migrated storage resolves to `None`; Timing is blocked.
    - Selecting profile A persists across new store/controller instances even after profile B is created.
    - Stale, corrupt, archived, or not-timing-ready selection resolves to its typed unavailable state, never to another profile.
    - Selection survives relaunch-equivalent object recreation for both file and in-memory stores.
    - Invalid selection IDs cannot cause path traversal.
  </behavior>
  <action>Write `CurrentTrackSelectionTest` asserting the behaviors above against both stores; then add atomic `loadCurrentSelection`, `setCurrentSelection`, and `clearCurrentSelection` plus active-profile listing to `LocalSessionStore`, implement them in both stores with payload-first atomic writes and ID validation, and implement `TrackProfileController` resolution that returns only the exact persisted profile's latest active timing-ready revision or a typed unavailable state. Never repair by selecting another profile. Implement until the test passes.</action>
  <acceptance_criteria>
    - Exact selection survives relaunch-equivalent recreation.
    - Creating/saving another profile does not change selection.
    - Tests explicitly reject `maxByOrNull(createdAtEpochMillis)` fallback behavior.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CurrentTrackSelectionTest*"</automated>
  </verify>
  <done>Shared storage and controller resolve one explicit current profile without fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace newest-Track derivation in the Drive controller snapshot</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingControllerTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingController.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/DriveMarkingControllerTest.kt</files>
  <behavior>
    - The blocked snapshot exposes a direct selection action and the exact current name or a clear none state.
    - Visible profile rows include only active latest revisions.
    - No `maxByOrNull(createdAtEpochMillis)` newest-ready derivation remains.
  </behavior>
  <action>Replace the existing newest-ready regression test with assertions for D-01 through D-04, then adapt `DriveMarkingController.snapshot()` to consume `TrackProfileController` resolution: expose current name, active rows, and a selection action while removing the newest-Track heuristic. Implement until the controller test passes.</action>
  <acceptance_criteria>
    - The newest-ready heuristic is removed and asserted absent.
    - Blocked Timing routes to an explicit selector action.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*DriveMarkingControllerTest*" --tests "*CurrentTrackSelectionTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>The Drive controller resolves the exact persisted selection with no newest-Track fallback.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI profile ID -> repository | A stale or unsafe ID can reach local storage. |
| Selection file -> controller | Archived/corrupt state must not silently select another Track. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Spoofing/Tampering | profile selection | HIGH | mitigate | Validate exact opaque ID, resolve active latest revision, never fall back. Proven by `CurrentTrackSelectionTest` no-fallback/path cases (Task 1). |
| T-05-06 | DoS | stale/archived selection | MEDIUM | mitigate | Return typed unavailable state, block Timing, expose explicit recovery action. Proven by `CurrentTrackSelectionTest` stale/archived cases (Task 1). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run focused selection/controller tests and `:shared:check`; confirm no newest-Track derivation remains.
</verification>

<success_criteria>
- D-01 through D-04 are deterministic in tests.
- Exact selection survives relaunch-equivalent recreation and blocks safely when unavailable.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-03-SUMMARY.md` when done.
</output>
</content>
