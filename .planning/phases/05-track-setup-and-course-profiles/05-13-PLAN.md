---
phase: 05-track-setup-and-course-profiles
plan: 13
type: execute
wave: 12
depends_on: [05-12]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflight.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflightTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/CourseProfileIntegrationTest.kt
autonomous: true
requirements: [SC-01, SC-02, SC-03, SC-04]
must_haves:
  truths:
    - "SC-04 / D-22: Preflight compares latest valid GPS with the complete closed reference line and blocks only clearly far courses."
    - "D-22: Pit/paddock-near-course starts remain allowed; stale/poor/non-finite fixes do not create a false wrong-course claim."
    - "D-23: A blocked preflight offers `Still use this track` and persists the override into TimingSession."
    - "D-17/D-24: Preflight runs only before Timing; after start there is no global off-course stop/invalidation."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflight.kt"
      provides: "Typed whole-course Ready/Blocked/Unavailable decision"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/CourseProfileIntegrationTest.kt"
      provides: "Selection-to-preflight-to-save vertical replay"
  key_links:
    - from: "SessionController.startTiming"
      to: "WrongCoursePreflight"
      via: "latest usable GPS before recorder construction"
      pattern: "Still use this track"
    - from: "CoursePreflightSnapshot"
      to: "TimingSession draft/session"
      via: "persisted explicit override"
      pattern: "override"
---

<objective>
Add conservative whole-course preflight, a blocked-with-override Timing path, and the selection→preflight→save vertical integration.

Purpose: Detect obvious wrong-course setup before Timing without letting location confidence stop an active lap.
Output: A pure preflight decision, a Drive override flow, persisted override metadata, and an integration replay gate.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-12-SUMMARY.md
</context>

<interfaces>
Use `CoursePreflightResult.Ready`, `Blocked(distanceMeters, thresholdMeters)`, and `Unavailable(reason)`. Persist `CoursePreflightSnapshot` including explicit override; never feed preflight or matcher confidence into active lap validity. Use the resolved 05-RESEARCH thresholds (250 m block, 100 m accuracy limit, 15 s freshness).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement conservative whole-course preflight and snapshot contract</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/LocationSampleProvider.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflight.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflightTest.kt</files>
  <behavior>
    - Minimum distance checks every segment including last-to-first.
    - Pit/paddock-near-course position is Ready; clearly far course is Blocked.
    - Accuracy uncertainty is subtracted before the conservative 250 m block threshold.
    - Stale over 15 s, accuracy worse than 100 m, non-finite, malformed, or oversized geometry returns Unavailable/corrupt.
    - The snapshot distinguishes normal readiness from explicit override.
  </behavior>
  <action>Create deterministic pit, far-course, malformed, and stale fixtures in `GpsFixtureLibrary`, then implement pure bounded preflight over `ClosedReferencePath` per D-22 with centralized thresholds, typed results, and a serializable `CoursePreflightSnapshot`. Do not claim wrong course when fix quality is insufficient. Write `WrongCoursePreflightTest` for the behaviors above and implement until it passes. Keep preflight free of Compose, storage, and LapEngine dependencies.</action>
  <acceptance_criteria>
    - D-22 whole-course and pit allowance behavior is deterministic.
    - Oversized/non-finite geometry is rejected before distance scanning.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*WrongCoursePreflightTest*"</automated>
  </verify>
  <done>Preflight reliably distinguishes clearly far from near/unknown with a persistable evidence contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the block and override through normal Timing</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/WrongCoursePreflight.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/DriveScreen.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/CourseProfileIntegrationTest.kt</files>
  <behavior>
    - Clearly far blocks once with an exact `Still use this track` action; explicit override starts the same normal Timing flow.
    - On override the snapshot persists in draft/session; after start, preflight never reruns and timing is never invalidated by course distance.
    - The integration replay covers selection, direction, override, samples/laps/Sectors, Ghost suppression/rematch, save, and relaunch.
  </behavior>
  <action>Per D-23, resolve the exact current profile/direction and latest usable GPS, run preflight before recorder construction, and return a typed blocked state with the override action. On override, persist the snapshot and start normal Timing; per D-24 never rerun preflight or invalidate timing after start. Write `CourseProfileIntegrationTest` for the full vertical and implement until it passes. Keep closed-course/private-track copy visible and controls pre-Timing.</action>
  <acceptance_criteria>
    - Clearly far blocks once; explicit override starts normal Timing.
    - No active-lap off-course pause/invalidity state exists; non-delta metrics continue through matcher failure.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*WrongCoursePreflightTest*" --tests "*CourseProfileIntegrationTest*" --tests "*CourseProgressMatcherTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>The current-profile-to-saved-session flow handles wrong-course override without compromising active timing.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Latest GPS/geometry -> preflight | Bad input can cause false block or realtime crash. |
| Preflight/matcher -> active timing | Confidence failure must not deny timing/raw capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-28 | DoS/Safety | preflight | HIGH | mitigate | Whole-loop bounded distance, uncertainty margin, freshness/accuracy checks, typed Unavailable. Proven by `WrongCoursePreflightTest` (Task 1). |
| T-05-29 | DoS/Safety | active timing | HIGH | mitigate | Preflight only before start; matcher suppresses delta only and cannot stop engine/recorder. Proven by `CourseProfileIntegrationTest` continuity cases (Task 2). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run preflight/integration/matcher tests, `:shared:check`, and Android debug build.
</verification>

<success_criteria>
- D-22 and D-23 are user-visible before Timing; D-17/D-24 keep active timing continuous.
- The selection→preflight→save vertical is deterministic.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-13-SUMMARY.md` when done.
</output>
</content>
