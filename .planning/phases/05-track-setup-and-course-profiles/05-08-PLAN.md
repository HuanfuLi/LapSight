---
phase: 05-track-setup-and-course-profiles
plan: 8
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/TrackRevisionStoreTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/TrackProfileReviewTest.kt
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "SC-01 / D-12: Name and non-geometric metadata update without mutating immutable revisions."
    - "D-13/D-14: Geometry edits append revisions; active selectors show only latest active revision while history remains available."
    - "D-15: Sector-only revisions retain geometry compatibility; reference-line/start-finish revisions create new compatibility."
    - "D-16: Archive removes a profile from active selectors without deleting revisions, sessions, or Ghosts; duplicate creates an independent profile."
    - "D-01/D-03: Archiving current clears selection and never selects a replacement."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt"
      provides: "Rename, append revision, archive, duplicate, and history operations"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/TrackRevisionStoreTest.kt"
      provides: "Revision/lifecycle compatibility and preservation gate"
  key_links:
    - from: "archiveProfile"
      to: "clearCurrentSelection"
      via: "atomic matching-selection invalidation"
      pattern: "archive.*clear"
    - from: "duplicateProfile"
      to: "geometryCompatibilityId"
      via: "new independent identity even for copied geometry"
      pattern: "duplicate"
---

<objective>
Complete profile lifecycle behavior: rename, immutable history, archive, and independent duplicate, exposed in Track detail.

Purpose: Make saved profiles maintainable without deleting or rebinding historical sessions/Ghosts.
Output: Repository/controller lifecycle operations, Review interactions, and deterministic preservation tests.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-06-SUMMARY.md
</context>

<interfaces>
Operate on one atomic `TrackProfile` aggregate. `appendRevision` classifies sector-only versus lap-geometry change and assigns compatibility accordingly. Archive is reversible data state, never file deletion. Duplicate generates fresh profile/revision/compatibility IDs.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lifecycle repository and controller operations</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/CurrentTrackSelectionTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/TrackRevisionStoreTest.kt</files>
  <behavior>
    - Rename changes metadata only; revisions are immutable.
    - Sector-only append retains `geometryCompatibilityId`; reference/start-finish append changes it.
    - Archive preserves payloads/history and clears matching current selection only; it never deletes files.
    - Duplicate creates fresh profile/revision/compatibility IDs and leaves the source unchanged.
    - Active list excludes archived; history/exact load includes it.
    - File and in-memory stores match for every lifecycle result, including interrupted writes and unsafe IDs/names.
  </behavior>
  <action>Write `TrackRevisionStoreTest` (file and in-memory) covering D-12 through D-16, interrupted writes, unsafe IDs/names, non-increasing revisions, and stale current selection; then implement typed rename, append revision, list history, archive, and duplicate. Determine D-15 compatibility by explicit edit kind. Archive sets metadata and atomically clears only a matching selection. Duplicate deep-copies geometry into a fresh logical profile with independent IDs. Validate safe internal IDs, bounded history, monotonic ordinals, and cross-field identity with payload-first atomic writes. Implement until the test passes.</action>
  <acceptance_criteria>
    - Old sessions/references remain loadable after archive.
    - Duplicate cannot collide with source compatibility.
    - No destructive delete path is added.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*TrackRevisionStoreTest*" --tests "*CurrentTrackSelectionTest*"</automated>
  </verify>
  <done>Lifecycle operations preserve history and compatibility correctly across both stores.</done>
</task>

<task type="auto">
  <name>Task 2: Expose version history, archive, duplicate, and rename in Track detail</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/TrackProfileController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/review/ReviewModels.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/storage/TrackRevisionStoreTest.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/ReviewScreen.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/TrackProfileReviewTest.kt</files>
  <action>Add Track-detail rename, version-history, archive confirmation, and duplicate actions. Show archived history without placing it in active selectors. After archiving current, route Drive to the no-selection state rather than choosing another Track. Keep session Review bound to its stored course snapshot/exact revision, not latest geometry. Add `TrackProfileReviewTest` asserting these behaviors.</action>
  <acceptance_criteria>
    - A user can inspect old revisions and duplicate/archive without losing history.
    - An archived profile disappears from normal selection immediately.
    - Old session Review remains visually tied to old geometry.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*TrackRevisionStoreTest*" --tests "*TrackProfileReviewTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
    <automated>.\gradlew.bat :androidApp:assembleDebug</automated>
  </verify>
  <done>Track detail provides safe profile maintenance with preserved history.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI lifecycle action -> profile aggregate | Stale IDs or duplicate actions can target wrong records. |
| Revision append -> compatibility | Incorrect classification can collide or fork Ghost history. |
| Archive -> current selection/history | Archive must not substitute or delete data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16 | Spoofing | lifecycle IDs | HIGH | mitigate | Validate exact profile/revision relationship and safe opaque IDs. Proven by `TrackRevisionStoreTest` unsafe-ID cases (Task 1). |
| T-05-17 | Tampering | compatibility classification | HIGH | mitigate | Explicit edit-kind policy and tests for sector-only versus lap geometry. Proven by `TrackRevisionStoreTest` compatibility cases (Task 1). |
| T-05-18 | DoS/Repudiation | archive/duplicate | MEDIUM | mitigate | Atomic aggregate writes, no deletion, independent IDs, retained exact-history loads. Proven by `TrackRevisionStoreTest` archive/duplicate cases (Task 1). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run lifecycle/selection/Review tests, `:shared:check`, and Android build. Manually verify archive confirmation and history navigation.
</verification>

<success_criteria>
- D-12 through D-16 are implemented without destructive deletion.
- SC-01 named profiles remain reusable and maintainable.
- Same-wave Plan 05-07 has no overlapping write set.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-08-SUMMARY.md` when done.
</output>
</content>
