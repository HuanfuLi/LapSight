---
phase: 05-track-setup-and-course-profiles
plan: 8
type: execute
wave: 7
depends_on: [05-07]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcher.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngine.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcherTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
autonomous: true
requirements: [SC-04]
must_haves:
  truths:
    - "D-20: Stopping, reversing, or turning around does not pause Timing, Sector processing, Ghost processing, or raw capture."
    - "D-24: Off-course/ambiguous samples suppress live delta to `--` without stopping or invalidating the lap."
    - "D-24: A later confident match resumes live delta automatically without restarting Timing."
    - "D-18/D-19: Course progress is direction-relative and uses the exact compatible reference."
    - "D-17: Matcher confidence never creates a continuous wrong-direction state."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcher.kt"
      provides: "Typed direction-relative course position and confidence"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcherTest.kt"
      provides: "Independent green Ghost matcher replacement gate"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt"
      provides: "Available-to-unavailable-to-available continuity through production flow"
  key_links:
    - from: "CourseProgressMatcher.kt"
      to: "ClosedReferencePath.project"
      via: "lateral distance, ambiguity, continuity, and direction transform"
      pattern: "Matched|Unmatched"
    - from: "TimingSessionRecorder.kt"
      to: "LapEngine/raw samples"
      via: "unconditional fan-out before matcher suppression"
      pattern: "onSample"
---

<objective>
Replace traveled-distance Ghost progress with reliable direction-relative course matching while keeping lap timing and raw capture independent.

Purpose: Ensure matcher failures degrade only live delta and recover automatically.
Output: Pure matcher, ambiguity/excursion fixtures, LiveDelta integration, and deterministic continuity tests.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
@.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-07-SUMMARY.md
@.planning/phases/04-ghost-lap-live-delta/04-04-SUMMARY.md
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngine.kt
@shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
</context>

<interfaces>
Use `CourseMatchResult.Matched(directionProgressMeters, normalizedProgress, lateralDistanceMeters, confidence)` and `Unmatched(reason)`. Matching owns no lap clock. Every sample overwrites `LiveDeltaSnapshot`; unmatched maps to existing `UnmatchedProgress`/`--`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the matcher failure-and-recovery replay gate</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngine.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngineTest.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-VALIDATION.md
  </read_first>
  <files>shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcherTest.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt</files>
  <behavior>
    - Hairpin/parallel candidates become unmatched when ambiguity cannot be resolved.
    - Temporary excursion yields `Available -> -- -> Available`.
    - Backward movement can decrease progress without pausing lap/raw state.
    - Non-finite, poor-accuracy, implausible-jump, and incompatible direction/source inputs never throw.
    - Session sample count, elapsed timing, checkpoints, and lap completion continue through unmatched intervals.
  </behavior>
  <action>Add deterministic RED unit and production-path replay coverage using centralized research thresholds. Extend fixtures only with stable simulated scenarios; do not add a demo-only Ghost workflow.</action>
  <acceptance_criteria>
    - Tests fail against current accumulated-traveled-distance behavior.
    - Tests independently observe timing/raw continuity during matcher failure.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProgressMatcherTest*" --tests "*TimingGhostIntegrationTest*"</automated>
  </verify>
  <done>The matcher replacement has an independent deterministic green gate target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded direction-relative course matching</name>
  <read_first>
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcherTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/track/ClosedReferencePath.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngine.kt
    @.planning/phases/05-track-setup-and-course-profiles/05-RESEARCH.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcher.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostModels.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/LiveDeltaEngine.kt</files>
  <action>Implement pure matching over `ClosedReferencePath.project()` with finite/accuracy, lateral-distance, non-adjacent ambiguity, and speed/time continuity gates. Transform recorded path position into selected direction. Permit backward progress; return typed `Unmatched` for unreliable input and never throw. Replace `LiveDeltaEngine` traveled-distance accumulation with matched course progress and clear stale delta on every unmatched sample.</action>
  <acceptance_criteria>
    - Matcher is O(reference point count), bounded, and platform/storage free.
    - Unmatched output is `--`; next matched sample emits a value without reset.
    - Existing sign/interpolation semantics remain intact.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProgressMatcherTest*" --tests "*LiveDeltaEngineTest*"</automated>
  </verify>
  <done>Ghost delta uses reliable course position rather than traveled distance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve timing through realtime matcher failure</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/CourseProgressMatcher.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/fixtures/GpsFixtureLibrary.kt
    @.planning/phases/04-ghost-lap-live-delta/04-CONTEXT.md
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/TimingSessionRecorder.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt</files>
  <action>Wire matcher output only into Ghost delta availability after raw sample append and `LapEngine.onSample`. Preserve checkpointing, elapsed time, Sector state, and same-session new-best behavior through excursion/ambiguity. On rematch, resume against the same exact compatible reference. Keep other TimingRunSnapshot metrics visible while delta is `--`.</action>
  <acceptance_criteria>
    - Production-path replay proves `value -> -- -> value`.
    - Raw sample count and lap/Sector timing are continuous.
    - No matcher confidence import enters `LapEngine`.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*CourseProgressMatcherTest*" --tests "*TimingGhostIntegrationTest*" --tests "*CourseDirectionReplayTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>Realtime matcher failure suppresses only Ghost delta and recovers automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS sample -> matcher | Coordinates may be non-finite, noisy, ambiguous, or implausibly discontinuous. |
| Matcher -> live delta | Unreliable progress must not produce stale/misleading values. |
| Matcher -> timing pipeline | Failure must not deny lap timing or raw capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | DoS | matcher input | mitigate | Finite/bounded validation and typed unmatched results; no realtime exceptions. |
| T-05-20 | Spoofing | nearest segment | mitigate | Ambiguity and continuity gates before accepting course progress. |
| T-05-21 | DoS/Safety | recorder fan-out | mitigate | Append/engine/checkpoint independently; matcher controls delta only. |
| T-05-22 | Tampering | source/direction reference | mitigate | Consume exact CourseCompatibilityKey established in Plan 05-06. |
| T-05-SC | Tampering | package installs | accept | No package-manager install occurs. |
</threat_model>

<verification>
Run matcher, LiveDelta, timing integration, direction tests, and `:shared:check`. Do not merge this plan unless timing continues through all unmatched fixtures.
</verification>

<success_criteria>
- D-20 and D-24 are proven through normal production-path replay.
- Matcher replacement is independently green from migration and Sector replacement.
- Existing compact `--` UI behavior remains passive and truthful.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-08-SUMMARY.md` when done.
</output>
