---
phase: 05-track-setup-and-course-profiles
plan: 11
type: execute
wave: 10
depends_on: [05-10]
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
autonomous: true
requirements: [SC-03, SC-04]
must_haves:
  truths:
    - "D-19: References persist and resolve by exact CourseCompatibilityKey across both stores."
    - "D-20: A real timing run on a Demo-created Track remains real because active provider provenance owns session source."
    - "D-15: Sector-only revision and same-session new-best behavior remain green under exact-key persistence."
    - "D-18: Recorded/Reverse, duplicate, and geometry changes cannot cross-bind references."
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt"
      provides: "Exact compatibility-key reference persistence"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt"
      provides: "Provider-provenance and exact-key resolution gate"
  key_links:
    - from: "SessionController.kt"
      to: "LocalSessionStore reference operations"
      via: "CourseCompatibilityKey built from selected course and active provider"
      pattern: "CourseCompatibilityKey"
    - from: "FileSessionStore.kt"
      to: "InMemorySessionStore.kt"
      via: "mirrored exact-key mismatch results"
      pattern: "source"
---

<objective>
Persist and resolve references by exact `CourseCompatibilityKey` in both stores and derive session source from the active provider, not Track marking.

Purpose: Prevent revision/direction/source collisions and Demo/Real contamination at the storage boundary.
Output: Exact-key reference operations in both stores, a provider-provenance fix in the controller, and a deterministic integration gate.
</objective>

## Phase Goal

**As a** repeat track user, **I want to** save start/finish and course configuration, **so that** I do not have to reconfigure every session.

<execution_context>
@C:/Users/16079/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/16079/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-track-setup-and-course-profiles/05-CONTEXT.md
@.planning/phases/05-track-setup-and-course-profiles/05-PATTERNS.md
@.planning/phases/05-track-setup-and-course-profiles/05-10-SUMMARY.md
@.planning/phases/04-ghost-lap-live-delta/04-CONTEXT.md
</context>

<interfaces>
Replace Track/source reference operations and filenames with exact `CourseCompatibilityKey` operations. Build the key in `SessionController` from selected profile compatibility/direction plus active provider/run provenance. Validate payload/request equality before return; reject unsafe path IDs.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and resolve references by exact compatibility and active provenance</name>
  <read_first>
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt
    @shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/GhostCompatibility.kt
    @shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt
  </read_first>
  <files>shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionController.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/LocalSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/FileSessionStore.kt, shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/storage/InMemorySessionStore.kt, shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/session/TimingGhostIntegrationTest.kt</files>
  <behavior>
    - File and in-memory stores return identical mismatch results for any differing key dimension.
    - Demo/Real, Recorded/Reverse, duplicate, and geometry changes cannot cross-bind references.
    - A real provider run on a Demo-created profile saves as real (active provider owns source), not simulated.
    - Sector-only revision and same-session new-best promotion remain green within the same key.
    - Unsafe path IDs are rejected before path construction; payload/request key equality is validated before return.
  </behavior>
  <action>Write `TimingGhostIntegrationTest` covering the behaviors above (failing under existing Track ID + source-slot lookup and `sourceForTrack`); then replace reference operations and filenames in both stores with exact `CourseCompatibilityKey` operations, validate payload/request equality and reject unsafe IDs, and build the key in `SessionController` from selected profile compatibility/direction plus active provider/run provenance (not Track marking source). Preserve same-session and save-only reference promotion only inside that key. Implement until the test passes.</action>
  <acceptance_criteria>
    - Demo/Real, Recorded/Reverse, duplicate, and geometry changes cannot cross-bind.
    - A real run on a Demo-created profile remains real.
    - The test class is green; no assertion is left intentionally failing.
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat :shared:testAndroidHostTest --tests "*TimingGhostIntegrationTest*" --tests "*GhostCompatibilityTest*" --tests "*StoreMigrationTest*"</automated>
    <automated>.\gradlew.bat :shared:check</automated>
  </verify>
  <done>Fastest-lap/Ghost persistence resolves only exact compatible course history and correct run source.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1 (offline mobile app; controls applied by analogy to local data integrity and input validation).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Key -> reference path | Unsafe IDs can escape app-private reference paths. |
| Track marking/provider -> source | Marking provenance can contaminate real/simulated timing history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-23 | Tampering | reference paths | HIGH | mitigate | Safe opaque ID validation before path construction; payload/request key equality. Proven by `TimingGhostIntegrationTest` mismatch/path cases (Task 1). |
| T-05-24 | Spoofing/Tampering | Demo/Real provenance | HIGH | mitigate | Active provider owns run source; source participates in path and payload validation. Proven by `TimingGhostIntegrationTest` provenance cases (Task 1). |
| T-05-SC | Tampering | package installs | LOW | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
Run Ghost compatibility, timing integration, and migration tests, then `:shared:check`. Keep matcher behavior unchanged until Plan 05-12.
</verification>

<success_criteria>
- D-15, D-16, D-18, D-19, and source-related D-20 persistence behavior is implemented.
- Full compatibility collisions fail closed; Demo/Real data remains isolated.
- No new dependency or deferred scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-track-setup-and-course-profiles/05-11-SUMMARY.md` when done.
</output>
</content>
