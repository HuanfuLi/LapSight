---
phase: 06-external-gnss-and-sensor-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/external/ExternalGnssModels.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/external/Nmea0183Parser.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/external/ExternalGnssReplayProvider.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/external/Nmea0183ParserTest.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/external/ExternalGnssReplayProviderTest.kt
autonomous: true
requirements: [EXT-01, EXT-02]

must_haves:
  truths:
    - "D-01: completion is protocol compatibility preview, not hardware validation"
    - "D-04: external GNSS feeds the existing LocationSampleProvider seam"
    - "D-05: decoded external samples use LocationSource.ExternalGnss and preserve protocol provenance"
    - "D-06: replay fixtures are the acceptance backbone"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/external/Nmea0183Parser.kt"
      provides: "pure NMEA byte/sentence parser -> decoded fix state"
      contains: "RMC|GGA|VTG"
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/external/ExternalGnssReplayProvider.kt"
      provides: "replay-backed LocationSampleProvider for decoded external GNSS samples"
      contains: "LocationSampleProvider"
  key_links:
    - from: "ExternalGnssReplayProvider"
      to: "LocationSampleProvider"
      via: "drainPending emits queued LocationSample(source = ExternalGnss)"
      pattern: "LocationSource.ExternalGnss"
---

<objective>
Build the shared, hardware-free foundation for external GNSS: protocol metadata,
NMEA 0183 parsing, and a replay-backed provider that emits normalized
`LocationSample` values through the existing `LocationSampleProvider` boundary.
This plan proves the core source and parser path without Android BLE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-external-gnss-and-sensor-ingestion/06-CONTEXT.md
@.planning/phases/06-external-gnss-and-sensor-ingestion/06-RESEARCH.md

<interfaces>
Existing provider seam:
  shared/.../LocationSampleProvider.kt
  shared/.../GpsProbeModels.kt (`LocationSample`, `LocationSource.ExternalGnss`)

Parser scope:
  NMEA RMC/GGA/GNS/VTG first; GSA/GST/ZDA diagnostic fields where cheap.
  Checksum failure, malformed fields, no-fix, fragmented input, and burst input
  are typed parser outcomes, not thrown exceptions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: External GNSS shared models</name>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/GpsProbeModels.kt
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/LocationSampleProvider.kt
    - .planning/phases/06-external-gnss-and-sensor-ingestion/06-CONTEXT.md
  </read_first>
  <action>
    Add shared external GNSS models for protocol, receiver identity, connection
    state, decoded fix quality, and source metadata. Keep them platform-free.
    Ensure every path that produces samples can identify `ExternalGnssProtocol`
    and report hardware validation as unknown/unverified.
  </action>
  <acceptance_criteria>
    - Models compile in commonMain.
    - No Android/iOS/BLE imports in shared external models.
    - `LocationSource.ExternalGnss` remains the normalized source for samples.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: NMEA 0183 parser with deterministic fixtures</name>
  <read_first>
    - .planning/phases/06-external-gnss-and-sensor-ingestion/06-RESEARCH.md
  </read_first>
  <action>
    Implement a pure NMEA parser that accepts text bytes/sentences, validates
    checksums when present, handles fragmented input and multiple sentences per
    buffer, and emits decoded fix snapshots. Support RMC + GGA/GNS + VTG at a
    minimum, with satellite count, altitude, speed, heading, fix validity, and
    horizontal accuracy/HDOP where available. Add fixture tests for valid fix,
    no-fix, checksum failure, malformed coordinate, sentence fragmentation, and
    high-rate bursts.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest --tests "*Nmea0183ParserTest*"</automated>
  </verify>
  <acceptance_criteria>
    - Valid NMEA RMC/GGA/VTG combinations decode to expected latitude,
      longitude, speed, heading, altitude, satellites, and validity.
    - Bad checksums and malformed fields are rejected as data, not crashes.
    - Fragmented and burst inputs produce the same decoded sequence as whole
      sentence input.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replay-backed external provider</name>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/LocationSampleProvider.kt
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/SimulatedGpsProvider.kt
  </read_first>
  <action>
    Add an `ExternalGnssReplayProvider` that converts decoded external fixes into
    `LocationSample(source = LocationSource.ExternalGnss)`, preserves elapsed
    timing, and drains queued high-rate samples through `drainPending()`. Add
    tests proving it emits stable samples and can feed more than one decoded fix
    per UI tick without dropping the backlog.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest --tests "*ExternalGnssReplayProviderTest*"</automated>
  </verify>
  <acceptance_criteria>
    - External replay samples carry `LocationSource.ExternalGnss`.
    - High-rate decoded fixes are drained in order.
    - Reset clears stale queued fixes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :shared:testAndroidHostTest --tests "*Nmea0183ParserTest*"`
- `./gradlew :shared:testAndroidHostTest --tests "*ExternalGnssReplayProviderTest*"`
- Full `./gradlew :shared:testAndroidHostTest`
</verification>

<success_criteria>
- LapSight has a pure, test-backed NMEA parser.
- External GNSS replay emits normalized samples through the same provider seam as
  phone GPS and simulated replay.
- No hardware validation is claimed.
</success_criteria>

<output>
Create `.planning/phases/06-external-gnss-and-sensor-ingestion/06-01-SUMMARY.md`
when done.
</output>
