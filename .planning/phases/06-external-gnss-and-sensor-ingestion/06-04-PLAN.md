---
phase: 06-external-gnss-and-sensor-ingestion
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/external/ExternalGnssTimingPipelineTest.kt
  - docs/EXTERNAL-GNSS.md
  - .planning/phases/06-external-gnss-and-sensor-ingestion/06-HARDWARE-RISK.md
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03]

must_haves:
  truths:
    - "D-01: phase closeout says protocol-complete, hardware-unvalidated"
    - "D-02: do not claim hardware validation"
    - "D-04: lap engine behavior is unchanged for external samples"
    - "D-05: session/review/export source metadata distinguishes ExternalGnss from PhoneGps and Simulated"
    - "D-06: full replay is the acceptance backbone"
    - "D-09: basic telemetry ingestion is validated only as optional provenance/capture, not timing input"
---

<objective>
Close the protocol-first Phase 6 preview with end-to-end replay verification,
documentation, and a clear hardware-risk register. This plan decides whether the
phase can be marked protocol-complete while leaving hardware validation open.
</objective>

<context>
@.planning/phases/06-external-gnss-and-sensor-ingestion/06-CONTEXT.md
@.planning/phases/06-external-gnss-and-sensor-ingestion/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full timing pipeline replay with external samples</name>
  <action>
    Add tests that feed decoded NMEA and synthetic RaceBox external samples
    through the same timing/session pipeline used by phone GPS. Assert lap count,
    lap timestamps, ghost delta behavior where applicable, source metadata, and
    review/export provenance. Do not add external-GNSS branches inside the lap
    engine. Where the parser emits basic telemetry metadata, assert it is
    preserved in the appropriate session/review/export path and is not consumed
    by lap timing.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest --tests "*ExternalGnssTimingPipelineTest*"</automated>
  </verify>
  <acceptance_criteria>
    - External samples complete the same fixture laps as phone/simulated samples.
    - Session source metadata records ExternalGnss and protocol where available.
    - Basic telemetry metadata, when available, is preserved as provenance/capture
      data and absent telemetry is handled cleanly.
    - No lap-engine hardware-specific code path is introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: User/developer documentation</name>
  <action>
    Add `docs/EXTERNAL-GNSS.md` explaining supported protocols, expected receiver
    classes, how protocol support was tested, what remains unvalidated without
    hardware, and what feedback/logs users should provide if a real receiver
    fails. Document the EXT-03 boundary: basic telemetry may be captured when a
    supported protocol exposes it, but Phase 6 does not provide IMU fusion or
    vehicle telemetry timing features. Keep claims conservative.
  </action>
  <acceptance_criteria>
    - Docs separate NMEA protocol support, RaceBox protocol preview, and hardware
      validation status.
    - Docs separate optional telemetry capture from lap timing.
    - Docs ask for receiver model, firmware, phone model, Android version,
      connection logs, and exported session/replay logs for user feedback.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Hardware-risk register and phase closeout</name>
  <action>
    Create `06-HARDWARE-RISK.md` with unvalidated hardware risks, mitigation
    plan, and future validation checklist for RaceBox Mini/Mini S/Micro and NMEA
    receivers, including IMU/telemetry frame variation. If all automated gates
    pass, write `06-04-SUMMARY.md` and mark the phase as protocol-complete /
    hardware-unvalidated, not fully hardware-tested.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Risk doc exists and does not hide the lack of receiver hardware.
    - Final build/test gates pass.
    - Summary uses "protocol-complete, hardware-unvalidated" wording.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :shared:testAndroidHostTest :androidApp:assembleDebug`
- Manual hardware UAT: explicitly not performed in this phase.
</verification>

<success_criteria>
- Phase 6 can be accepted as protocol compatibility preview.
- Hardware validation remains tracked and cannot be mistaken for done.
</success_criteria>

<output>
Create `.planning/phases/06-external-gnss-and-sensor-ingestion/06-04-SUMMARY.md`
and update Phase 6 closeout docs when done.
</output>
