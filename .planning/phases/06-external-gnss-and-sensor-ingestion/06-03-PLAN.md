---
phase: 06-external-gnss-and-sensor-ingestion
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/ExternalGnssLocationProvider.kt
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/ExternalGnssBleClient.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/SettingsScreen.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/Localization.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/DisplaySettings.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/ui/ExternalGnssSettingsTest.kt
autonomous: true
requirements: [EXT-01, EXT-02]

must_haves:
  truths:
    - "D-01: Android external GNSS remains protocol preview until hardware validation exists"
    - "D-02: Android BLE behavior is hardware-unvalidated in this phase"
    - "D-04: Android BLE support is implemented behind the same provider seam"
    - "D-05: source/protocol/hardware-validation provenance must be visible in state and persistence"
    - "D-08: Drive remains low-interaction; detailed diagnostics do not return to the main Drive surface"
    - "Phone GPS and Simulated source behavior must not regress"
  artifacts:
    - path: "androidApp/src/main/kotlin/com/huanfuli/lapsight/ExternalGnssLocationProvider.kt"
      provides: "Android provider wrapper for external GNSS byte streams"
      contains: "LocationSampleProvider"
---

<objective>
Wire the shared parsers into Android as an optional external GNSS source. Add
settings/source UX and a provider shell that can be tested with injected byte
streams now and real BLE hardware later.
</objective>

<context>
@.planning/phases/06-external-gnss-and-sensor-ingestion/06-CONTEXT.md
@.planning/phases/06-external-gnss-and-sensor-ingestion/06-RESEARCH.md

<interfaces>
Existing platform patterns:
  androidApp/.../AndroidPhoneLocationProvider.kt
  androidApp/.../AndroidFusedLocationSampleProvider.kt
  shared/.../ui/SettingsScreen.kt

Permissions:
  Android 12+ BLE requires BLUETOOTH_SCAN/CONNECT as applicable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android external provider shell</name>
  <action>
    Add `ExternalGnssLocationProvider` that composes a byte-stream client with
    NMEA/RaceBox parsers and exposes `LocationSampleProvider`. The provider must
    be injectable so tests can supply fake byte chunks without BLE hardware.
    Handle start/stop/reset, connection state, reconnect gaps, and high-rate
    queue draining. Carry optional telemetry records alongside source state when
    the parser emits them, but do not feed telemetry into lap timing.
  </action>
  <acceptance_criteria>
    - Provider starts/stops without touching phone GPS state.
    - Fake byte-stream tests can produce external samples without hardware.
    - Optional telemetry records can be surfaced for review/export plumbing
      without altering `LocationSampleProvider` timing semantics.
    - Reset clears parser and sample queues.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Settings source UX and localization</name>
  <action>
    Extend settings/source selection to include External GNSS when Android
    support is available. Show protocol and hardware-unvalidated state clearly
    in Settings. Keep Drive simple: show selected source/quality/status, not a
    diagnostic recording tool. Add localized strings for the supported languages.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest --tests "*ExternalGnssSettingsTest*"</automated>
  </verify>
  <acceptance_criteria>
    - External GNSS can be selected separately from Phone GPS and Simulated.
    - Timing source is locked while timing, matching existing source behavior.
    - UI copy says protocol support is unvalidated without hardware.
    - Localization compiles for all current languages.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build and regression verification</name>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Shared host tests pass.
    - Android debug build passes.
    - Existing Phone GPS and Simulated source tests still pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `./gradlew :shared:testAndroidHostTest :androidApp:assembleDebug`
- No connected Android test against the user's real device unless explicitly
  requested; do not run destructive install/test flows.
</verification>

<success_criteria>
- Android can select an External GNSS source and feed injected external samples
  through the production provider seam.
- Real BLE hardware remains an explicit follow-up risk.
</success_criteria>

<output>
Create `.planning/phases/06-external-gnss-and-sensor-ingestion/06-03-SUMMARY.md`
when done.
</output>
