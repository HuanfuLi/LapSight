---
phase: 07-phone-to-glasses-dat-display-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudPage.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudModel.kt
  - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/GlassesGpsState.kt
  - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/glasses/HudModelTest.kt
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/MainActivity.kt
autonomous: true
requirements: [MR-01, MR-02]

must_haves:
  truths:
    - "The same SessionController instance that drives the phone dash is reachable from MainActivity"
    - "A pure mapper turns a timing snapshot into per-page HUD content for all three pages"
    - "The mapper renders the neutral --, GPS-idle, and stale-fix states without any DAT SDK type"
    - "The mapper reuses the existing lap/delta formatting — no re-derived lap math"
  artifacts:
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudModel.kt"
      provides: "TimingRunSnapshot + GPS state + page + now + flashUntil -> HudModel projection"
      contains: "fun from"
      min_lines: 60
    - path: "shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudPage.kt"
      provides: "enum HudPage { DELTA_ONLY, FOCUSED, TELEMETRY }"
      contains: "enum class HudPage"
    - path: "shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/glasses/HudModelTest.kt"
      provides: "host tests for all five readouts, delta tone/sign, D-13/D-14/D-15"
      contains: "HudModel"
  key_links:
    - from: "shared/.../ui/AppShell.kt"
      to: "MainActivity onSessionControllerReady"
      via: "callback invoked once after remember { SessionController(...) }"
      pattern: "onSessionControllerReady"
    - from: "shared/.../glasses/HudModel.kt"
      to: "DeltaDisplayState.text / Long?.formatLapTime()"
      via: "reused formatting"
      pattern: "deltaDisplay|formatLapTime"
---

<objective>
Deliver the read-side foundation of the bridge: (1) hoist the single
`SessionController` so `androidApp` can poll the SAME instance the UI drives (the
keystone MR-01 seam), and (2) a PURE, host-testable `HudModel` mapper that projects
`TimingRunSnapshot` (+ GPS/ready state) into per-page HUD content covering every
MR-02 readout and the D-13/D-14/D-15 non-timing states.

Purpose: This is the vertical slice on the shared/pure side — after it, host tests
prove the exact strings and states the glasses will show, with zero DAT SDK
dependency, so the clean-room boundary (ARCH-01) holds and the mapper runs under the
fast `:shared:testAndroidHostTest` path.
Output: Hoisted controller reachable in MainActivity; `HudModel`/`HudPage`/
`GlassesGpsState` in shared; green `HudModelTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-RESEARCH.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-PATTERNS.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-UI-SPEC.md

<interfaces>
<!-- Extracted from the repo. The mapper consumes ONLY these platform-free types. -->

TimingRunSnapshot (shared/.../session/SessionModels.kt:412-461) fields:
  isActive:Boolean, lapCount:Int, currentLapNumber:Int?, currentLapMillis:Long?,
  lastLapMillis:Long?, bestLapMillis:Long?, sessionElapsedMillis:Long,
  speedMetersPerSecond:Double?, accuracyMeters:Double?, sampleRateHz:Double?,
  currentSectorNumber:Int?, sectorCount:Int, latestSectorName:String?,
  latestSectorSplitMillis:Long?, deltaDisplay:DeltaDisplayState, referenceLapMillis:Long?
  companion.inactive(): neutral -- instance (use for pre-run states).

DeltaDisplayState (shared/.../ghost/DeltaDisplayState.kt): val text:String (sign baked:
  "-0.218s"/"+0.421s"/"--"), val tone: DeltaTone { Faster, Slower, Neutral }.

Long?.formatLapTime() (shared/.../lap/LapDashState.kt:97-107): "M:SS.mmm"/"--:--.---".
  D-05 rule: HUD shows TENTHS ("1:23.4") — write a tenths variant in the mapper; never a second clock.

ReadyThresholds.Default (shared/.../session/ReadyGate.kt:64-80):
  maxHorizontalAccuracyMeters (degraded default), freshness, minSampleRateHz = 0.9.
ReadyState (ReadyGate.kt:90-95): sealed Ready / NotReady(reasons: List<ReadyBlocker>).
GpsFixStatus (shared/.../GpsProbeModels.kt:14): Idle/Acquiring/.../Simulated.

SessionController seam (shared/.../session/SessionController.kt):
  fun timingRunSnapshot(): TimingRunSnapshot     (L364)
  fun snapshot(): SessionControllerSnapshot      (L69) — draft state only
  fun startTiming(...) / fun stop()              (D-08 delegation target, later plans)
Construction site (AppShell.kt:98-115): remember { SessionController(store, sourceForTrack) }.
App() -> AppShell() param plumbing (App.kt:36-73).
DriveScreen GPS/ready path: dashReadyState(snapshot: DriveMarkingSnapshot) in
  ui/drive/DriveTelemetry.kt:221 — UI-layer type; see Task 1 fallback for the bridge accessor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist the SessionController and expose a platform-free GPS/ready accessor</name>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/App.kt (param plumbing App->AppShell, L36-73)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/AppShell.kt (controller construction L98-115)
    - androidApp/src/main/kotlin/com/huanfuli/lapsight/MainActivity.kt (onCreate provider wiring L87-126, onDestroy L133-136)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/drive/DriveTelemetry.kt (dashReadyState L221-235, DriveMarkingSnapshot inputs)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-RESEARCH.md (KMP Integration Seam — Option 1 hoist+poll, L413-449; Open Q2)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-PATTERNS.md (KMP seam analog, L168-178)
  </read_first>
  <action>
    Add an `onSessionControllerReady: (SessionController) -> Unit = {}` parameter to `App()` and thread
    it into `AppShell()` (RESEARCH Option 1 — hoist+poll, zero engine changes, no Flow added). In
    AppShell, after the existing `remember { SessionController(...) }`, invoke the callback exactly once
    with the hoisted instance via `LaunchedEffect(sessionController) { onSessionControllerReady(sessionController) }`.
    In MainActivity, capture the controller into a nullable field (e.g. `private var sessionController: SessionController?`)
    by passing `onSessionControllerReady = { sessionController = it }` into `App(...)`. Do NOT construct a
    second controller. For the D-13 idle screen the bridge needs GPS fix status pre-timing (Open Q2):
    add a small platform-free `GlassesGpsState` (fixStatus: GpsFixStatus, accuracyMeters: Double?,
    sampleRateHz: Double?, ready: Boolean) in shared, and resolve its source by reading how DriveScreen
    derives `dashReadyState(snapshot)` — reuse the same GPS/marking snapshot accessor. If that snapshot is
    only reachable inside Compose, expose a minimal platform-free `glassesGpsState(): GlassesGpsState`
    from the hoisted holder that wraps the current GPS probe + `ReadyThresholds.Default`; document the
    chosen accessor in the SUMMARY. Keep all changes free of `com.meta.wearable.*` and platform types.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest</automated>
  </verify>
  <acceptance_criteria>
    - `App()` and `AppShell()` accept `onSessionControllerReady` and invoke it once per hoisted controller (LaunchedEffect keyed on the instance).
    - MainActivity holds a reference to the SAME controller instance (no second `SessionController(...)` constructed anywhere in androidApp).
    - `GlassesGpsState` exists in shared commonMain and imports zero Compose/DAT types.
    - Existing shared host suite stays green: `./gradlew :shared:testAndroidHostTest` passes.
  </acceptance_criteria>
  <done>The phone dash and MainActivity share one SessionController, and a platform-free GPS/ready accessor exists for the idle screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure HudModel mapper + HudPage enum (all pages, all non-timing states)</name>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/lap/LapDashState.kt (from() factory + format-on-struct idiom, L40-107; formatLapTime L97-107)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/session/SessionModels.kt (TimingRunSnapshot L412-461, inactive())
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ghost/DeltaDisplayState.kt (text/tone, DeltaTone L13-77)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-CONTEXT.md (D-01..D-05, D-13, D-14, D-15)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-UI-SPEC.md (Typography roles, page content)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-PATTERNS.md (HudModel/HudPage analog sections, L43-78)
  </read_first>
  <behavior>
    - DELTA_ONLY page (D-01): HudModel exposes delta pill fields + current-lap clock only.
    - FOCUSED page (D-01/D-02): delta + current-lap hero co-equal, footer = last/best/speed.
    - TELEMETRY page (D-01): FOCUSED plus lapCount.
    - Delta tone → caret intent: Faster→CaretDown, Slower→CaretUp, Neutral→no caret (D-03/D-14); delta text passed through from deltaDisplay.text (sign already baked), never reformatted.
    - Current-lap clock rendered to TENTHS ("1:23.4") from currentLapMillis (D-05), not a re-derived clock.
    - D-14 neutral pill: referenceLapMillis == null (or DeltaTone.Neutral) → pill shows "--", no caret, identical geometry fields.
    - D-13 idle: GlassesGpsState not ready / no active run → idle content ("Waiting for GPS" / "Ready — start timing" per UI-SPEC copy) using ReadyThresholds.Default semantics.
    - D-15 stale fix: clock keeps advancing (wall-clock base), speed and delta collapse to "--"/dim with a stale flag set; delta never shown as live when the fix is stale.
    - D-04 sector flash: when nowEpochMs < flashUntilEpochMs and a latestSectorSplitMillis exists, the clock slot yields the sector split label (e.g. "S2 +0.12") instead of the running clock; otherwise the clock.
  </behavior>
  <action>
    Create `HudPage` (enum DELTA_ONLY, FOCUSED, TELEMETRY — cycle order = ordinal, D-01) and `HudModel`
    with a `from(run: TimingRunSnapshot, gps: GlassesGpsState, page: HudPage, nowEpochMs: Long, flashUntilEpochMs: Long?): HudModel`
    factory mirroring the `LapDashState.from(...)` + format-on-the-struct idiom. Expose pre-formatted,
    SDK-free fields the renderer will emit: page, the per-page visible field set, delta pill fields
    (text passthrough + a `DeltaCaret { Up, Down, None }` derived from `DeltaDisplayState.tone`), the
    tenths clock string (or the sector-flash label inside the flash window), last/best labels, speed
    label (mirror DisplaySettings unit handling — accept the unit as a param or expose raw + a
    unit-applied label), lapCount, and boolean flags `isIdle` (D-13), `isStaleFix` (D-15),
    `isNeutralDelta` (D-14). Reuse `Long?.formatLapTime()` for the ms reference and write only a tenths
    truncation helper — do NOT recompute lap/delta math (RESEARCH "Don't Hand-Roll"). Import zero
    `com.meta.wearable.*` and zero Compose types (keep it in shared commonMain so it host-tests fast).
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest --tests "*HudModelTest*"</automated>
  </verify>
  <acceptance_criteria>
    - `HudModel.from(...)` returns the correct visible field set for each of DELTA_ONLY / FOCUSED / TELEMETRY.
    - Delta caret maps Faster→Down, Slower→Up, Neutral→None; delta text is byte-identical to `deltaDisplay.text`.
    - Clock output is tenths precision; inside a flash window it yields the sector split label instead.
    - `isNeutralDelta` (D-14), `isIdle` (D-13), and `isStaleFix` (D-15, speed+delta = "--") flags are set exactly per the behavior block.
    - HudModel/HudPage import no DAT or Compose types (grep confirms).
  </acceptance_criteria>
  <done>A pure, SDK-free mapper produces every HUD readout and non-timing state, ready for the renderer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Host tests for the mapper (MR-01/MR-02 projection + D-13/14/15)</name>
  <read_first>
    - shared/src/commonTest/kotlin/com/huanfuli/lapsight/shared/lap/LapDashStateTest.kt (kotlin.test factory-assertion analog, L1-52)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudModel.kt (the mapper under test — from Task 2)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-VALIDATION.md (Phase Requirements -> Test Map, Wave 0)
  </read_first>
  <action>
    Add `HudModelTest` in shared commonTest mirroring `LapDashStateTest` (plain `kotlin.test`). Cover:
    all five MR-02 readouts present per page; delta tone/sign → caret + passthrough text; the D-14
    neutral `--` pill keeps identical field geometry with no caret; D-13 idle content appears when the
    GPS state is not ready; D-15 stale fix keeps the clock advancing while speed and delta read `--`;
    the tenths clock formatting; and the D-04 sector-flash window swapping the clock slot for the split
    label and reverting after `flashUntilEpochMs`. Build inputs from `TimingRunSnapshot.inactive()` and
    explicit active instances — no SDK types.
  </action>
  <verify>
    <automated>./gradlew :shared:testAndroidHostTest --tests "*HudModelTest*"</automated>
  </verify>
  <acceptance_criteria>
    - Tests exist for each page's visible readouts, delta caret/sign, D-13, D-14, D-15, tenths, and the sector-flash window.
    - `./gradlew :shared:testAndroidHostTest` is green including HudModelTest.
  </acceptance_criteria>
  <done>The mapper's MR-01/MR-02 projection and non-timing states are locked by fast host tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared engine → androidApp bridge | only platform-free snapshot types cross (clean-room ARCH-01) |
| GPS fix freshness → HUD delta | stale fix must not be presented as live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering (data integrity) | HudModel delta/speed on stale GPS (D-15) | mitigate | `isStaleFix` collapses speed+delta to "--" while the clock advances on wall-clock; delta is never emitted as live when the fix is stale; asserted by HudModelTest |
| T-07-04 | Information Disclosure / boundary breach | HudModel/HudPage placement | mitigate | Mapper lives in shared commonMain and imports zero `com.meta.wearable.*`/Compose types; acceptance_criteria grep enforces the clean-room boundary |
| T-07-05 | Repudiation | second/duplicate SessionController | mitigate | Hoist reuses the single instance; acceptance_criteria forbids constructing another controller in androidApp |
</threat_model>

<verification>
- `./gradlew :shared:testAndroidHostTest` green (existing + HudModelTest).
- `./gradlew :androidApp:assembleDebug` still compiles with the new App/AppShell signature and MainActivity capture.
- grep confirms HudModel/HudPage/GlassesGpsState import no DAT/Compose types.
</verification>

<success_criteria>
- MainActivity can reach the live SessionController (MR-01 seam ready).
- A pure mapper produces all MR-02 readouts + D-13/D-14/D-15 states, host-tested.
</success_criteria>

<output>
Create `.planning/phases/07-phone-to-glasses-dat-display-bridge/07-02-SUMMARY.md` when done
(record the exact GPS/ready accessor chosen for D-13, resolving RESEARCH Open Q2).
</output>
