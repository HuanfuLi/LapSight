---
phase: 07-phone-to-glasses-dat-display-bridge
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/GlassesInput.kt
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/GlassesBridge.kt
  - androidApp/src/androidTest/kotlin/com/huanfuli/lapsight/glasses/CaptouchInputTest.kt
autonomous: false
requirements: [MR-03]

must_haves:
  truths:
    - "IF the real-device captouch receive API is confirmed, a tap cycles HUD pages and a tap-and-hold delegates start/stop to the phone controller"
    - "The phone remains the owner and executor of timing regardless of glasses input"
    - "Unexpected/unmapped glasses events are ignored, not acted on"
    - "D-09: no neural-band path; glasses input is limited to captouch tap/tapAndHold + display callbacks"
  artifacts:
    - path: "androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/GlassesInput.kt"
      provides: "captouch/clickable -> page cycle (local) / start-stop (delegate) router"
      contains: "sessionController"
  key_links:
    - from: "GlassesInput tapAndHold"
      to: "SessionController.startTiming/stop"
      via: "delegate to the single controller public API (D-08)"
      pattern: "startTiming|stop"
    - from: "GlassesInput tap"
      to: "GlassesBridge.page"
      via: "ordinal advance page cycle (D-07)"
      pattern: "HudPage"
---

<objective>
OPTIONAL / experimental stretch: map glasses captouch to HUD control — tap cycles
pages (D-07), tap-and-hold delegates start/stop to the phone-owned `SessionController`
(D-08). The phase does NOT depend on this: the phone-side page selector (07-05) and
phone-owned start/stop are the guaranteed MR-03-safe controls. The DAT real-device
captouch RECEIVE API is undocumented in 0.8 (RESEARCH Open Q1 / Pitfall 2), so this
plan is hardware-gated: confirm the API exists before implementing the mapping.

Purpose: Add a stationary convenience without weakening the guaranteed control path.
If the receive API cannot be confirmed, this plan stops at the checkpoint and the
phone-side controls remain the answer.
Output: A hardware/API confirmation gate, then (only if confirmed) `GlassesInput` +
MockCaptouchKit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-RESEARCH.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-PATTERNS.md

<interfaces>
<!-- Canonical: C:\Users\16079\Code\meta-wearables-dat-android\plugins\mwdat-android\skills\mockdevice-testing\SKILL.md
     MockCaptouchKit simulates tap()/tapAndHold(); the REAL-device receive API is the open question. -->

D-07: captouch TAP cycles DELTA_ONLY → FOCUSED → TELEMETRY (ordinal advance).
D-08: captouch TAP-AND-HOLD → start/end session; the PHONE owns and executes start/stop
      via SessionController.startTiming(...)/stop() (the exact public API the phone UI uses).
D-09 (constraint, MUST NOT design around): neural-band (sEMG) input is NOT available to DAT
      apps — do not add any neural-band code path. Glasses input = captouch + clickable callbacks only.
Delegation analog (repo): ui/drive/DriveScreen.kt:350-433 — route intent → single controller
      public API → match sealed StartTimingResult; keep callbacks fast (don't block captouch).
Bridge (07-03): GlassesBridge holds `page: HudPage` and the hoisted SessionController.
MockCaptouchKit (07-RESEARCH.md L486-495): device.services.captouch.tap()/.tapAndHold().
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the real-device captouch receive API before implementing (Open Q1)</name>
  <what-built>Nothing yet — this gate decides whether the D-07/D-08 gesture mapping is buildable. RESEARCH found MockCaptouchKit simulates tap/tapAndHold but no real-device receive API is exposed on Display/DeviceSession/Wearables in DAT 0.8; the only documented glasses→phone input is clickable/button onClick.</what-built>
  <read_first>
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-RESEARCH.md (Open Questions Q1; Pitfall 2; Assumptions A1)
    - C:\Users\16079\Code\meta-wearables-dat-android\plugins\mwdat-android\skills\mockdevice-testing\SKILL.md
    - C:\Users\16079\Code\meta-wearables-dat-android\CHANGELOG.md (MockCaptouchKit 0.8.0 entry)
  </read_first>
  <how-to-verify>
    1. On real Meta Ray-Ban Display glasses (or via the Meta Wearables MCP), determine whether a Display app can subscribe to raw temple captouch tap / tap-and-hold events (a gesture Flow on Display/DeviceSession/Wearables).
    2. If YES: record the exact API surface (type + subscription entry point) for Task 2.
    3. If NO (only clickable/button onClick is available): decide whether to implement an on-screen clickable page-cycle element instead, or to SKIP this plan entirely — the phone-side selector (07-05) already covers page control and the phone owns start/stop. Record the decision.
  </how-to-verify>
  <acceptance_criteria>
    - A written determination of whether the captouch receive API exists, with the exact API cited if it does.
    - If it does not exist: an explicit decision recorded (skip vs on-screen clickable fallback); no speculative captouch subscription is implemented.
  </acceptance_criteria>
  <resume-signal>Type "captouch-confirmed: <api>" to proceed to Task 2, or "captouch-unavailable: skip" / "captouch-unavailable: clickable-fallback" to close or redirect this plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: GlassesInput router + MockCaptouchKit tests (gated on Task 1 = confirmed)</name>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/drive/DriveScreen.kt (intent→controller→sealed-result delegation L350-433)
    - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/GlassesBridge.kt (page state + controller — 07-03)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudPage.kt (07-02)
    - C:\Users\16079\Code\meta-wearables-dat-android\plugins\mwdat-android\skills\mockdevice-testing\SKILL.md
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-CONTEXT.md (D-07, D-08, D-09)
  </read_first>
  <action>
    ONLY if Task 1 confirmed the receive API (or approved the on-screen clickable fallback). Create
    `GlassesInput` that subscribes to the confirmed captouch/clickable source and routes: TAP → advance
    `GlassesBridge.page` by ordinal (DELTA_ONLY → FOCUSED → TELEMETRY, wrapping) — pure local state, no
    controller call (D-07); TAP-AND-HOLD → delegate to the hoisted `SessionController.startTiming(...)` /
    `stop()` and pattern-match the sealed `StartTimingResult` exactly like DriveScreen (D-08) — the phone
    owns and executes timing; the glasses only emit the trigger. Validate input (ASVS V5): ignore any
    event that is not a mapped gesture; never act on unexpected/malformed events. Keep the callback fast
    (RESEARCH anti-pattern: don't block captouch). Do NOT add any neural-band path (D-09). Wire
    `GlassesInput` into the bridge. Add `CaptouchInputTest` (androidTest, MockCaptouchKit): tap advances
    the page across all three; tapAndHold delegates start then stop to the controller; an unmapped event
    is ignored.
  </action>
  <verify>
    <automated>./gradlew :androidApp:connectedAndroidTest --tests "com.huanfuli.lapsight.glasses.CaptouchInputTest"</automated>
  </verify>
  <acceptance_criteria>
    - Tap advances `GlassesBridge.page` by ordinal with wraparound and makes no controller call.
    - Tap-and-hold calls `SessionController.startTiming`/`stop` (phone executes) and matches the sealed result; glasses never run timing locally.
    - Unmapped events are ignored (asserted); no neural-band code path exists.
    - `CaptouchInputTest` passes via MockCaptouchKit on an API 29+ emulator.
  </acceptance_criteria>
  <done>Captouch tap cycles pages and tap-and-hold delegates start/stop to the phone — or the plan is closed per Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glasses input → phone action | untrusted gesture events cross into controller delegation |
| glasses → timing control | glasses may only emit triggers; phone owns/executes timing (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Spoofing / Tampering | routed glasses input events | mitigate | Validate the routed action (ASVS V5): only mapped tap/tapAndHold act; unexpected events ignored; asserted in CaptouchInputTest |
| T-07-15 | Elevation of Privilege | glasses triggering timing | mitigate | Glasses only emit the trigger; SessionController on the phone owns/executes start/stop (D-08); no local timing on glasses |
| T-07-16 | (Scope integrity) | neural-band input (D-09) | mitigate | No neural-band code path; glasses input limited to captouch/clickable; acceptance_criteria forbids it |
</threat_model>

<verification>
- Task 1 determination recorded (API confirmed → Task 2; unavailable → skip/fallback).
- If implemented: `./gradlew :androidApp:connectedAndroidTest --tests "*CaptouchInputTest*"` green; `:androidApp:assembleDebug` green.
- Phone-side controls (07-05) remain the guaranteed path regardless of outcome.
</verification>

<success_criteria>
- Captouch mapping works IF the receive API is confirmed; otherwise the plan closes cleanly with the phone-side controls intact.
- The phone owns timing in all cases (D-08); no neural-band design (D-09).
</success_criteria>

<output>
Create `.planning/phases/07-phone-to-glasses-dat-display-bridge/07-06-SUMMARY.md` when done
(record the Task 1 captouch-API determination — this resolves RESEARCH Open Q1 for the roadmap).
</output>
