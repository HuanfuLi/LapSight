---
phase: 07-phone-to-glasses-dat-display-bridge
plan: 04
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/hud/HudRenderer.kt
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/hud/DeltaPill.kt
  - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/GlassesBridge.kt
  - androidApp/src/androidTest/kotlin/com/huanfuli/lapsight/glasses/HudRenderSmokeTest.kt
autonomous: true
requirements: [MR-02]

must_haves:
  truths:
    - "Each of the three HUD pages renders its required readouts on the 600x600 display"
    - "The delta pill is an iconic CARD with caret + sign + value and no color"
    - "D-02: hero = delta pill + current-lap clock co-equal; the pill is an iconic CARD (no hue color)"
    - "The GPS-idle, neutral-delta, and stale-fix screens render distinctly"
    - "A sector crossing briefly shows the split in the clock slot then reverts"
    - "D-04: the sector flash renders transiently (~1.5 s) in the clock slot, not as an always-on sector row"
  artifacts:
    - path: "androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/hud/HudRenderer.kt"
      provides: "HudModel -> sendContent { flexBox } for all pages + non-timing states"
      contains: "flexBox"
      min_lines: 80
    - path: "androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/hud/DeltaPill.kt"
      provides: "CARD + caret icon + signed value pill (D-03), no color"
      contains: "FlexBoxBackground.CARD"
  key_links:
    - from: "GlassesBridge poll loop"
      to: "HudRenderer.render(model)"
      via: "sendContent { HudRenderer.render(model) }"
      pattern: "HudRenderer"
    - from: "HudRenderer delta"
      to: "DeltaPill"
      via: "caret + deltaDisplay.text"
      pattern: "CARET_(UP|DOWN)"
---

<objective>
Render the full HUD: map `HudModel` to `display.sendContent { flexBox { … } }` for all
three pages (DELTA_ONLY / FOCUSED / TELEMETRY, D-01) with the delta pill + current-lap
hero co-equal (D-02), the iconic CARD delta pill (D-03, no color), and the non-timing
screens — GPS idle (D-13), neutral `--` pill (D-14), stale-fix glyph (D-15), and the
transient sector flash in the clock slot (D-04). Replace 07-03's placeholder render.

Purpose: This completes MR-02 — the five readouts plus delta are actually drawn on the
600×600 glasses, using only the DAT DSL palette that research verified exists
(`FlexBoxBackground{NONE,CARD}`, `TextColor{PRIMARY,SECONDARY}`, `CornerRadius{NONE,
SMALL,MEDIUM}`, `IconName` incl. `CARET_UP/DOWN`). No arbitrary color.
Output: `HudRenderer` + `DeltaPill`, wired into the bridge loop, with instrumented render
smoke tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-RESEARCH.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-PATTERNS.md
@.planning/phases/07-phone-to-glasses-dat-display-bridge/07-UI-SPEC.md

<interfaces>
<!-- DAT DSL has NO in-repo analog. Mirror the external sample + RESEARCH Pattern 3.
     C:\Users\16079\Code\meta-wearables-dat-android\plugins\mwdat-android\skills\display-access\SKILL.md
     C:\Users\16079\Code\meta-wearables-dat-android\samples\DisplayAccess\...\display\DisplayViewModel.kt -->

DAT display DSL palette (VERIFIED, hard constraint — D-03 / Pitfall 1):
  FlexBoxBackground = { NONE, CARD }            (no hue; CARD is the "pill")
  TextColor         = { PRIMARY, SECONDARY }    (emphasis only, NOT color)
  CornerRadius      = { NONE, SMALL, MEDIUM }
  TextStyle         = { HEADING, BODY, META }   (UI-SPEC Typography roles)
  IconName          = enum incl. CARET_UP, CARET_DOWN (D-03 ahead/behind)
  flexBox(padding = 12, gap = 6, direction = ROW/COLUMN, crossAlignment = CENTER)
  Each display.sendContent { flexBox { text/icon(...) } } REPLACES the whole screen.

HudModel fields (from 07-02): page, per-page visible set, delta text (passthrough) +
  DeltaCaret { Up, Down, None }, tenths clock (or sector-flash label), last/best labels,
  speed label (unit-applied), lapCount, isIdle (D-13), isStaleFix (D-15), isNeutralDelta (D-14).
UI-SPEC copy: "Waiting for GPS" / "Ready — start timing"; GPS warning glyph for D-15.
Structure-only analog (pick fields per page, hero + footer): ui/drive/TimingDashboard.kt.
Delta-caret mapping (RESEARCH Pattern 3 / A4): Faster→CARET_DOWN(ahead), Slower→CARET_UP(behind), Neutral→none.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeltaPill — iconic CARD pill (D-03/D-14, no color)</name>
  <read_first>
    - C:\Users\16079\Code\meta-wearables-dat-android\plugins\mwdat-android\skills\display-access\SKILL.md
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/components/StatusChip.kt (tone->presentation switch idiom, L17-60)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudModel.kt (delta fields — 07-02)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-CONTEXT.md (D-03 amended, D-14)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-RESEARCH.md (Pattern 3 L306-325; Pitfall 1)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-UI-SPEC.md (Color table; flexBox padding=12 gap=6)
  </read_first>
  <action>
    Create `DeltaPill` as a render helper that emits a `flexBox(background = FlexBoxBackground.CARD,
    cornerRadius = CornerRadius.MEDIUM, direction = ROW, gap = 6, crossAlignment = CENTER)` containing:
    for `DeltaCaret.Down` an `icon(IconName.CARET_DOWN)`, for `DeltaCaret.Up` an `icon(IconName.CARET_UP)`,
    for `DeltaCaret.None` no icon (D-14 neutral `--`), followed by `text(model.deltaText, style =
    TextStyle.HEADING)` (the sign is already baked in — do NOT reformat). Optionally apply
    `TextColor.PRIMARY` (ahead) / `TextColor.SECONDARY` (behind) as a weak emphasis cue only. Keep the
    pill geometry identical whether or not a reference exists (D-14): CARD + `--` with no caret. Do NOT
    reference any background/text color enum value other than NONE/CARD/PRIMARY/SECONDARY (Pitfall 1
    warning sign).
  </action>
  <verify>
    <automated>./gradlew :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - DeltaPill emits a CARD flexBox with CornerRadius.MEDIUM.
    - Caret is CARET_DOWN for ahead, CARET_UP for behind, absent for neutral; value text is the passthrough `deltaText`.
    - No color enum outside {NONE, CARD, PRIMARY, SECONDARY} appears; pill geometry is identical in the neutral `--` case.
  </acceptance_criteria>
  <done>The delta pill renders ahead/behind via frame+caret+sign (no color), with a stable neutral state.</done>
</task>

<task type="auto">
  <name>Task 2: HudRenderer — three pages + idle/stale/sector-flash, wired into the bridge loop</name>
  <read_first>
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/ui/drive/TimingDashboard.kt (hero+footer decomposition, structure only)
    - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/hud/DeltaPill.kt (Task 1)
    - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/GlassesBridge.kt (poll loop placeholder to replace — 07-03)
    - shared/src/commonMain/kotlin/com/huanfuli/lapsight/shared/glasses/HudModel.kt (07-02)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-CONTEXT.md (D-01, D-02, D-04, D-13, D-14, D-15)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-UI-SPEC.md (Typography, Copywriting)
  </read_first>
  <action>
    Create `HudRenderer.render(model: HudModel)` returning the flexBox content for the active page.
    DELTA_ONLY: DeltaPill + current-lap clock only. FOCUSED (driving default): DeltaPill + current-lap
    hero co-equal (D-02, two large HEADING readouts) with a small footer (TextStyle.META/BODY) of
    last / best / speed. TELEMETRY: FOCUSED plus a lap-count readout. Use `TextStyle.HEADING` for hero,
    `BODY`/`META` for footer per UI-SPEC. Non-timing states: when `model.isIdle` render the D-13 idle
    screen ("Waiting for GPS" → "Ready — start timing" copy + fix state/accuracy); when
    `model.isStaleFix` show the GPS warning glyph and render speed/delta dimmed as `--` while the clock
    keeps its value (D-15); when `model.isNeutralDelta` the DeltaPill shows `--` (D-14). Sector flash
    (D-04): when the model's clock slot carries the sector-split label (flash window active), render that
    label in the current-lap clock position instead of the running clock — no separate always-on sector
    row. Then replace 07-03's placeholder `sendContent { text(...) }` in `GlassesBridge` with
    `sendContent { HudRenderer.render(model) }`, keeping the frame-dedupe and 500ms cadence.
  </action>
  <verify>
    <automated>./gradlew :androidApp:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - HudRenderer produces distinct content for DELTA_ONLY, FOCUSED, and TELEMETRY with the required readouts (FOCUSED footer = last/best/speed; TELEMETRY adds lapCount).
    - Idle (D-13), stale-fix (D-15, dimmed `--` + glyph, clock retained), neutral pill (D-14), and sector flash (D-04, split in clock slot) each render distinctly.
    - The bridge loop calls `HudRenderer.render(model)` (no placeholder text remains); dedupe + 500ms preserved.
  </acceptance_criteria>
  <done>All three pages and every non-timing state render through the bridge's 2 Hz loop.</done>
</task>

<task type="auto">
  <name>Task 3: Instrumented render smoke (MR-02 per-page elements)</name>
  <read_first>
    - C:\Users\16079\Code\meta-wearables-dat-android\plugins\mwdat-android\skills\mockdevice-testing\SKILL.md
    - androidApp/src/androidTest/kotlin/com/huanfuli/lapsight/glasses/GlassesLifecycleTest.kt (test base from 07-03)
    - androidApp/src/main/kotlin/com/huanfuli/lapsight/glasses/hud/HudRenderer.kt (Task 2)
    - .planning/phases/07-phone-to-glasses-dat-display-bridge/07-VALIDATION.md (MR-02 test map)
  </read_first>
  <action>
    Add `HudRenderSmokeTest` (androidTest) that, for a representative `HudModel` per page and per
    non-timing state, drives the renderer through a MockDeviceKit display session and asserts the
    emitted content tree contains the required elements: DELTA_ONLY has the delta pill + clock;
    FOCUSED adds last/best/speed; TELEMETRY adds lapCount; the idle screen carries the UI-SPEC copy;
    the stale-fix screen carries the GPS glyph with `--` speed/delta; the sector-flash model shows the
    split label in the clock slot. Assert the delta pill uses CARD + CARET_DOWN/UP (or none for neutral).
    Inspect the content description the DSL produces (or the sent-content capture the mock exposes) rather
    than pixels.
  </action>
  <verify>
    <automated>./gradlew :androidApp:connectedAndroidTest --tests "com.huanfuli.lapsight.glasses.HudRenderSmokeTest"</automated>
  </verify>
  <acceptance_criteria>
    - The smoke test asserts each page's required elements and the D-13/D-14/D-15/D-04 states.
    - `./gradlew :androidApp:connectedAndroidTest` passes for HudRenderSmokeTest on an API 29+ emulator.
  </acceptance_criteria>
  <done>Per-page rendering (MR-02) and non-timing states are verified against the DAT content tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS fix freshness → rendered delta/speed | stale data must be visually distinct, never shown as live |
| driver attention → HUD density | passive, glanceable content only (MR-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering (data integrity) | stale delta/speed rendered as live (D-15) | mitigate | HudRenderer dims speed/delta to `--` and shows the GPS glyph when `isStaleFix`; clock keeps its own value; smoke test asserts it |
| T-07-09 | (Safety, MR-03) | HUD legibility/passivity on 600×600 | mitigate | Glanceable HEADING hero + minimal footer; no interaction required to display; real-hardware legibility gate deferred to 07-05 human-verify |
| T-07-10 | Tampering | rendering with a color the SDK lacks (D-03) | mitigate | Palette limited to NONE/CARD/PRIMARY/SECONDARY + CARET icons; acceptance_criteria forbids other color enums (Pitfall 1) |
</threat_model>

<verification>
- `./gradlew :androidApp:assembleDebug` green.
- `./gradlew :androidApp:connectedAndroidTest --tests "*HudRenderSmokeTest*"` green.
- `./gradlew :shared:testAndroidHostTest` still green.
</verification>

<success_criteria>
- All three pages plus idle/stale/neutral/sector-flash render with the correct readouts (MR-02).
- The delta pill encodes ahead/behind without color, per the amended D-03.
</success_criteria>

<output>
Create `.planning/phases/07-phone-to-glasses-dat-display-bridge/07-04-SUMMARY.md` when done
(note any sector-flash duration / page-scope observations for the deferred tuning item).
</output>
